{
  "css": "\n:root{--bg:#050509;--s1:#0c0c15;--s2:#131320;--brd:#222240;--brd2:#333358;--tx:#d0d0e6;--tx2:#8888aa;--tx3:#5e5e80;--w:#f2f2ff;--js:#f7df1e;--react:#61dafb;--ng:#dd0031;--green:#34d399;--red:#f87171;--yellow:#fbbf24;--purple:#a78bfa;--blue:#60a5fa;--pink:#f472b6;--code-bg:#08080f;--r:10px}\n*{margin:0;padding:0;box-sizing:border-box}html{scroll-behavior:smooth}\nbody{font-family:'Manrope',sans-serif;background:var(--bg);color:var(--tx);line-height:1.7;font-size:14.5px;-webkit-font-smoothing:antialiased}\n::-webkit-scrollbar{width:5px}::-webkit-scrollbar-track{background:var(--bg)}::-webkit-scrollbar-thumb{background:var(--purple);border-radius:9px}\n.hero{padding:2.5rem 1.5rem 1.2rem;text-align:center;position:relative;overflow:hidden}\n.hero::before{content:'';position:absolute;inset:0;background:radial-gradient(ellipse 80% 60% at 50% -10%,rgba(167,139,250,.1),transparent 60%);pointer-events:none}\n.hero h1{font-family:'Literata',serif;font-size:clamp(1.8rem,5vw,3rem);font-weight:900;color:var(--w);letter-spacing:-.03em;line-height:1.15;position:relative}\n.hero .sub{color:var(--tx2);margin-top:.5rem;font-size:.85rem}\n.tabs{display:flex;justify-content:center;gap:.4rem;padding:.55rem .8rem;flex-wrap:wrap;border-bottom:1px solid var(--brd);position:sticky;top:0;z-index:1000;background:rgba(5,5,9,.92);backdrop-filter:blur(20px)}\n.tab{font-family:'Manrope',sans-serif;font-size:.76rem;font-weight:700;padding:.45rem 1rem;border-radius:8px;border:1px solid var(--brd);background:var(--s1);color:var(--tx2);cursor:pointer;transition:.2s}\n.tab:hover{border-color:var(--tx3);color:var(--tx)}.tab.active{color:var(--w);border-color:var(--purple);background:rgba(167,139,250,.1)}\n.mn{max-width:880px;margin:0 auto;padding:1.5rem 1.25rem 5rem}\n.tech{display:none;animation:fu .3s ease}.tech.active{display:block}\n@keyframes fu{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}\n.si{font-size:.88rem;color:var(--tx2);margin-bottom:1rem;padding-bottom:.8rem;border-bottom:1px solid var(--brd)}\n.qa{background:var(--s1);border:1px solid var(--brd);border-radius:var(--r);margin-bottom:.4rem;overflow:hidden}\n.qh{padding:.7rem .9rem;cursor:pointer;display:flex;align-items:flex-start;gap:.55rem;user-select:none;transition:background .2s}\n.qh:hover{background:var(--s2)}\n.lv{font-family:'JetBrains Mono',monospace;font-size:.5rem;font-weight:600;padding:.1rem .4rem;border-radius:20px;text-transform:uppercase;letter-spacing:.04em;flex-shrink:0;margin-top:.15rem}\n.lv.b{background:rgba(52,211,153,.1);color:var(--green)}.lv.i{background:rgba(251,191,36,.1);color:var(--yellow)}.lv.a{background:rgba(248,113,113,.1);color:var(--red)}\n.qt{font-weight:600;font-size:.8rem;color:var(--w);flex:1}\n.tg{color:var(--tx3);font-size:.95rem;flex-shrink:0;transition:transform .3s}\n.qa.open .tg{transform:rotate(45deg)}\n.qb{max-height:0;overflow:hidden;transition:max-height .5s ease}\n.qa.open .qb{max-height:15000px}\n.qi{padding:0 .9rem .9rem}.qi p{margin-bottom:.3rem}\n.fb{display:flex;gap:.3rem;margin-bottom:.6rem;flex-wrap:wrap}\n.ft{background:var(--s1);border:1px solid var(--brd);color:var(--tx3);font-family:'Manrope',sans-serif;font-size:.68rem;font-weight:500;padding:.3rem .65rem;border-radius:20px;cursor:pointer;transition:.2s}\n.ft:hover{border-color:var(--tx3)}.ft.active{background:var(--purple);border-color:var(--purple);color:#fff}\n.ct{font-size:.72rem;color:var(--tx3);margin-bottom:.5rem}.ct b{color:var(--purple)}\n/* Angular Q&A classes from existing file */\n.qa-h,.qa-header{padding:.7rem .9rem;cursor:pointer;display:flex;align-items:flex-start;gap:.55rem;user-select:none;transition:background .2s}\n.qa-h:hover{background:var(--s2)}\n.qa-a,.qa-answer{max-height:0;overflow:hidden;transition:max-height .5s ease}\n.qa.open .qa-a,.qa.open .qa-answer{max-height:15000px}\n.qa-ai,.qa-answer-inner{padding:0 .9rem .9rem}.qa-ai p,.qa-answer-inner p{margin-bottom:.3rem}\n.qa-question,.qq{font-weight:600;font-size:.8rem;color:var(--w);flex:1}\n.qa-toggle{color:var(--tx3);font-size:.95rem;flex-shrink:0;transition:transform .3s}\n.qa.open .qa-toggle{transform:rotate(45deg)}\n.qa-level{font-family:'JetBrains Mono',monospace;font-size:.5rem;font-weight:600;padding:.1rem .4rem;border-radius:20px;text-transform:uppercase;letter-spacing:.04em;flex-shrink:0;margin-top:.15rem}\n.qa-level.beginner,.lvl.b{background:rgba(52,211,153,.1);color:var(--green)}\n.qa-level.intermediate,.lvl.i{background:rgba(251,191,36,.1);color:var(--yellow)}\n.qa-level.advanced,.lvl.a{background:rgba(248,113,113,.1);color:var(--red)}\n.lvl{font-family:'JetBrains Mono',monospace;font-size:.5rem;font-weight:600;padding:.1rem .4rem;border-radius:20px;text-transform:uppercase;letter-spacing:.04em;flex-shrink:0;margin-top:.15rem}\n.fbtn,.filter-btn{background:var(--s1);border:1px solid var(--brd);color:var(--tx3);font-family:'Manrope',sans-serif;font-size:.68rem;font-weight:500;padding:.3rem .65rem;border-radius:20px;cursor:pointer;transition:.2s}\n.fbtn:hover,.filter-btn:hover{border-color:var(--tx3)}.fbtn.active,.filter-btn.active{background:var(--purple);border-color:var(--purple);color:#fff}\n.fbar,.filter-bar{display:flex;gap:.3rem;margin-bottom:.6rem;flex-wrap:wrap}\n.counter{font-size:.72rem;color:var(--tx3);margin-bottom:.5rem}.counter b{color:var(--purple)}\n.analogy,.an{background:rgba(167,139,250,.06);border-left:3px solid var(--purple);border-radius:0 8px 8px 0;padding:.7rem .85rem;margin:.4rem 0;font-size:.78rem}\n.analogy::before,.an::before{content:'💡 Think of it this way';display:block;font-weight:700;font-size:.6rem;color:var(--purple);text-transform:uppercase;letter-spacing:.05em;margin-bottom:.2rem}\n.tip,.tp{background:rgba(52,211,153,.06);border-left:3px solid var(--green);border-radius:0 8px 8px 0;padding:.7rem .85rem;margin:.4rem 0;font-size:.78rem}\n.tip::before,.tp::before{content:'🔥 Pro Tip';display:block;font-weight:700;font-size:.6rem;color:var(--green);text-transform:uppercase;letter-spacing:.05em;margin-bottom:.2rem}\n.warn,.wr{background:rgba(251,191,36,.06);border-left:3px solid var(--yellow);border-radius:0 8px 8px 0;padding:.7rem .85rem;margin:.4rem 0;font-size:.78rem}\n.warn::before,.wr::before{content:'⚠️ Common Mistake';display:block;font-weight:700;font-size:.6rem;color:var(--yellow);text-transform:uppercase;letter-spacing:.05em;margin-bottom:.2rem}\n.deep,.dp{background:rgba(96,165,250,.06);border-left:3px solid var(--blue);border-radius:0 8px 8px 0;padding:.7rem .85rem;margin:.4rem 0;font-size:.78rem}\n.deep::before,.dp::before{content:'🔬 Under the Hood';display:block;font-weight:700;font-size:.6rem;color:var(--blue);text-transform:uppercase;letter-spacing:.05em;margin-bottom:.2rem}\npre{background:var(--code-bg);border:1px solid var(--brd);border-radius:8px;padding:.85rem;overflow-x:auto;margin:.4rem 0;font-size:.72rem;line-height:1.55;tab-size:2}\ncode{font-family:'JetBrains Mono',monospace;font-size:.72rem}\np code,li code{background:var(--s2);padding:.08em .3em;border-radius:4px;color:var(--pink);font-size:.72em}\nul,ol{padding-left:1.2rem;margin:.3rem 0}li{margin-bottom:.2rem}p{margin-bottom:.35rem}strong{color:var(--w)}\n.tag{display:inline-block;font-size:.6rem;font-weight:600;padding:.1rem .4rem;border-radius:12px;margin-right:.2rem}.tag-r{background:rgba(221,0,49,.06);color:var(--ng)}.tag-g{background:rgba(52,211,153,.08);color:var(--green)}.tag-y{background:rgba(251,191,36,.08);color:var(--yellow)}.tag-p{background:rgba(167,139,250,.08);color:var(--purple)}.tag-b{background:rgba(96,165,250,.08);color:var(--blue)}\n.card{background:var(--s1);border:1px solid var(--brd);border-radius:var(--r);padding:1.3rem;margin-bottom:.8rem}.card:hover{border-color:var(--brd2)}.card h2{font-family:'Literata',serif;font-size:1.15rem;font-weight:700;color:var(--w);margin-bottom:.5rem}.card h3{font-size:.88rem;font-weight:700;color:var(--green);margin:1rem 0 .4rem}.card h4{font-size:.8rem;font-weight:600;color:var(--purple);margin:.8rem 0 .3rem}\n.grid2{display:grid;grid-template-columns:1fr 1fr;gap:.6rem;margin:.4rem 0}\n.vis-count{color:var(--purple)}\n.section{display:none;animation:fu .3s ease}.section.active{display:block}.section-intro{font-size:.88rem;color:var(--tx2);margin-bottom:1rem;padding-bottom:.8rem;border-bottom:1px solid var(--brd)}\n.sub-nav{overflow-x:auto;white-space:nowrap;padding:0;border-bottom:1px solid var(--brd);position:sticky;top:48px;z-index:900;background:var(--bg);backdrop-filter:blur(20px);margin:0 -1.25rem 1.5rem;padding:0 1.25rem}\n.sni,.nav-inner{display:flex;gap:0;max-width:1300px;margin:0 auto;height:45px}\n.sn{background:0;border:0;color:var(--tx3);font-family:'Manrope',sans-serif;font-size:.68rem;font-weight:700;padding:.7rem 1rem;cursor:pointer;border-bottom:2px solid transparent;transition:.2s;white-space:nowrap;display:inline-flex;align-items:center;height:100%}\n.sn:hover{color:var(--tx)}\n.sn.active{color:var(--purple);border-bottom-color:var(--purple)}\n.cd{background:var(--s1);border:1px solid var(--brd);border-radius:var(--r);padding:1.3rem;margin-bottom:.8rem}.cd:hover{border-color:var(--brd2)}.cd h2{font-family:'Literata',serif;font-size:1.15rem;font-weight:700;color:var(--w);margin-bottom:.5rem}.cd h3{font-size:.88rem;font-weight:700;color:var(--green);margin:1rem 0 .4rem}\n.k,.kw{color:#c792ea}.s,.st{color:#c3e88d}.c,.cm{color:#546e7a}.f,.fn{color:#82aaff}.d,.dc{color:#ffcb6b}.n,.nu{color:#f78c6c}\n.sc{display:none;animation:fu .3s ease}.sc.active{display:block}\n@media(max-width:640px){.cd,.card{padding:1rem}pre{padding:.7rem;font-size:.66rem}.qh,.qa-h{padding:.6rem .75rem}.tab{padding:.35rem .7rem;font-size:.7rem}}\n",
  "scripts": "",
  "body": "<div class=\"hero\"><h1>JavaScript Mastery Bible</h1><p class=\"sub\">ES2025, async patterns & core foundations</p></div>\n<div class=\"tech active\" id=\"t-javascript\">\n<nav class=\"sub-nav\"><div class=\"sni\">\n<button class=\"sn\" data-s=\"ts-expert\">TypeScript Expert Patterns</button>\n<button class=\"sn\" data-s=\"js-modern\">Modern JS & Web APIs</button>\n<button class=\"sn\" data-s=\"js-memory\">Memory & Performance Mastery</button>\n<button class=\"sn active\" data-s=\"js-iq\">Interview Q&amp;A (194)</button>\n<button class=\"sn\" data-s=\"js-ts\">TypeScript Fundamentals</button>\n</div></nav>\n<div class=\"sc\" id=\"js-ts\">\n  <h2>TypeScript Fundamentals</h2>\n  <p class=\"section-desc\">Extending JavaScript with static typing to build robust, scalable applications.</p>\n\n  <div class=\"card\">\n    <div class=\"card-title\">Types vs Interfaces</div>\n    <div class=\"card-content\">\n      <p>Both are used to define custom structures in TypeScript, but they have subtle differences.</p>\n      <ul>\n        <li><b>Interface:</b> Strictly for object shapes. Can be merged via \"Declaration Merging\" (if you declare the same interface twice, TS combines their properties). Ideal for public API definitions.</li>\n        <li><b>Type Aliases (`type`):</b> More flexible. Can define primitives, unions, intersections, and mapped types. Cannot be declaration-merged. Ideal for internal component definitions and complex utility types.</li>\n      </ul>\n      <div class=\"code-block\">\n<pre><code>// Interface (Object Shape)\ninterface User { id: number; name: string; }\n\n// Type (Union)\ntype Status = \"idle\" | \"loading\" | \"success\" | \"error\";\n\n// Intersection (Combining shapes)\ntype AdminUser = User & { role: \"admin\" | \"superadmin\" };\n</code></pre>\n      </div>\n    </div>\n  </div>\n\n  <div class=\"card\">\n    <div class=\"card-title\">Generics & Utility Types</div>\n    <div class=\"card-content\">\n      <p>Generics allow you to write reusable code that works across multiple types.</p>\n      <ul>\n        <li><b>`T` as an Argument:</b> Think of a generic `<T>` like a type variable passed into a function.</li>\n        <li><b>Partial&lt;T&gt;:</b> Makes all properties in T optional. Useful for update/patch payloads.</li>\n        <li><b>Omit&lt;T, K&gt;:</b> Creates a type by removing specific keys `K` from `T`.</li>\n        <li><b>Record&lt;K, T&gt;:</b> Creates an object type where keys are of type `K` and values are of type `T`.</li>\n      </ul>\n      <div class=\"code-block\">\n<pre><code>// Generic Function: infers type T from the input array\nfunction getFirstItem&lt;T&gt;(items: T[]): T | undefined {\n  return items[0];\n}\n\nconst num = getFirstItem([1, 2, 3]); // Hover: num is number\nconst str = getFirstItem([\"a\", \"b\"]); // Hover: str is string\n</code></pre>\n      </div>\n    </div>\n  </div>\n\n  <div class=\"card\">\n    <div class=\"card-title\">Discriminated Unions (Tagged Unions)</div>\n    <div class=\"card-content\">\n      <p>A pattern used to safely exhaust all possible variations of a type by sharing a single common literal property (the \"discriminator\").</p>\n      <div class=\"code-block\">\n<pre><code>type Action = \n  | { type: \"LOADING\" } \n  | { type: \"SUCCESS\"; payload: string[] } \n  | { type: \"ERROR\"; errorMsg: string };\n\nfunction reducer(action: Action) {\n  // TypeScript knows exactly what properties are available\n  // based on the 'type' property.\n  switch (action.type) {\n    case \"LOADING\":\n      return 'Loading...';\n    case \"SUCCESS\":\n      return action.payload.length; // Safe to access payload\n    case \"ERROR\":\n      return action.errorMsg; // Safe to access errorMsg\n  }\n}\n</code></pre>\n      </div>\n    </div>\n  </div>\n</div>\n\n<div class=\"sc\" id=\"js-memory\">\n  <div class=\"s-header\">\n    <h2>Memory & Performance Mastery</h2>\n    <p>Architecture of the V8 engine, Garbage Collection internals, and high-performance threading.</p>\n  </div>\n\n  <div class=\"grid\">\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg expert\">EXPERT</span>\n        <h3>V8 Engine Architecture</h3>\n      </div>\n      <p>Modern JS engines like V8 (Chrome/Node) use a sophisticated pipeline to achieve near-native speeds:</p>\n      <ul>\n        <li><strong>Ignition:</strong> A baseline bytecode interpreter that executes code quickly with minimal overhead.</li>\n        <li><strong>TurboFan:</strong> The optimizing JIT compiler. It uses profiling data from Ignition to compile \"Hot\" functions into highly optimized machine code.</li>\n        <li><strong>Speculative Optimization:</strong> V8 assumes types based on previous execution. If a function always receives integers, it stays optimized; if a string is suddenly passed, it triggers a \"Deoptimization\" (bailout).</li>\n      </ul>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg expert\">EXPERT</span>\n        <h3>Garbage Collection Internals</h3>\n      </div>\n      <p>V8 uses a <strong>Generational GC</strong> strategy based on the \"Infant Mortality\" hypothesis (most objects die young):</p>\n      <ul>\n        <li><strong>Young Generation (New Space):</strong> Small (1-64MB), fast area for new objects. Uses \"Scavenge\" algorithm with parallel processing.</li>\n        <li><strong>Old Generation (Old Space):</strong> Where long-lived objects are moved. Uses \"Mark-Sweep-Compact\" which is more thorough but slower.</li>\n        <li><strong>Orinoco:</strong> The modern parallel/incremental/concurrent engine that performs GC work on background threads to minimize main-thread \"Stop-the-World\" pauses.</li>\n      </ul>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg advanced\">ADVANCED</span>\n        <h3>Memory Management Tools</h3>\n      </div>\n      <p>Strategies for handling large data sets without memory leaks:</p>\n      <pre><code>// WeakRef: Reference without preventing GC\nlet heavyObj = { data: new Array(1000000) };\nconst ref = new WeakRef(heavyObj);\n\n// FinalizationRegistry: Cleanup callback\nconst registry = new FinalizationRegistry((heldValue) => {\n  console.log(`${heldValue} was collected!`);\n});\nregistry.register(heavyObj, \"Big Data\");\n\nheavyObj = null; // Eligible for GC</code></pre>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg expert\">EXPERT</span>\n        <h3>Advanced Threading: Workers</h3>\n      </div>\n      <p>Moving heavy computation off the main thread to ensure 60fps responsiveness:</p>\n      <ul>\n        <li><strong>Web Workers:</strong> Truly parallel execution using separate OS threads. Communicates via structured cloning.</li>\n        <li><strong>SharedArrayBuffer:</strong> Allows memory sharing between threads. Must be used with <strong>Atomics</strong> to prevent race conditions.</li>\n        <li><strong>SharedWorkers:</strong> Accessible from multiple windows/tabs from the same origin, ideal for coordinating state across tabs.</li>\n      </ul>\n    </div>\n  </div>\n</div>\n\n<div class=\"sc\" id=\"js-modern\">\n  <h2>Modern JS & Web APIs</h2>\n  <p class=\"section-desc\">Harnessing the latest ECMAScript features and modern browser APIs.</p>\n\n  <div class=\"card\">\n    <div class=\"card-title\">ECMAScript 2024+ (ES15)</div>\n    <div class=\"card-content\">\n      <ul>\n        <li><b>Object.groupBy / Map.groupBy:</b> Natively groups an array of objects into an object (or Map) based on a string returned by a callback. Replaces the need for Lodash `_.groupBy`.</li>\n        <li><b>Promise.withResolvers:</b> Returns an object containing a new Promise and its `resolve` and `reject` functions. Fixes the old awkward pattern of extracting resolvers from the Promise constructor.</li>\n        <li><b>Array.prototype.with:</b> Safe array mutation. `arr.with(index, value)` returns a *new* array with the element at `index` replaced by `value`, leaving the original array intact. Great for React/Redux!</li>\n        <li><b>Temporal API (Draft):</b> The modern replacement for the broken `Date` object, providing full timezone awareness, calendar math, and immutable date objects.</li>\n      </ul>\n      <div class=\"code-block\">\n<pre><code>// Example of Promise.withResolvers\nconst { promise, resolve, reject } = Promise.withResolvers();\n\nsetTimeout(() => resolve(\"Done!\"), 1000);\npromise.then(console.log); // \"Done!\" after 1s\n</code></pre>\n      </div>\n    </div>\n  </div>\n\n  <div class=\"card\">\n    <div class=\"card-title\">Modern Web APIs</div>\n    <div class=\"card-content\">\n      <ul>\n        <li><b>IntersectionObserver:</b> Asynchronously observe changes in the intersection of a target element with an ancestor or the viewport. Crucial for lazy-loading images and infinite scrolling without hurting performance via massive scroll event listeners.</li>\n        <li><b>ResizeObserver:</b> Reports changes to the dimensions of an element's content or border box. Provides a performant way to build truly responsive elements independently of the viewport (container queries in JS).</li>\n        <li><b>MutationObserver:</b> Watches the DOM tree for changes (nodes added/removed, attributes modified) and fires a callback. Used internally by frameworks, but vital for writing extensions or observing 3rd party injects.</li>\n        <li><b>Web Workers:</b> Runs scripts in background threads. The worker thread can perform computations without interfering with the user interface (main thread). Cannot access the DOM directly.</li>\n      </ul>\n    </div>\n  </div>\n\n  <div class=\"card\">\n    <div class=\"card-title\">JS Design Patterns</div>\n    <div class=\"card-content\">\n      <ul>\n        <li><b>Singleton:</b> Ensures a class has only one single instance and provides a global access point to it (e.g., establishing a single database connection or global store).</li>\n        <li><b>Observer / Pub-Sub:</b> A subject maintains a list of dependents (observers) and notifies them automatically of state changes (e.g., Redux, RxJS Subjects, DOM Event listeners).</li>\n        <li><b>Factory:</b> A function or method that creates objects without specifying the exact class of object that will be created. Simplifies complex object initialization.</li>\n        <li><b>Proxy:</b> The modern ES6 `Proxy` object lets you wrap an object and intercept fundamental operations (reading/writing properties). Used extensively by Vue 3 and Zustand to track reactive state dependencies.</li>\n      </ul>\n    </div>\n  </div>\n</div>\n\n<div class=\"sc\" id=\"ts-expert\">\n  <div class=\"s-header\">\n    <h2>TypeScript Expert Patterns</h2>\n    <p>Advanced type gymnastics, branded types, and sophisticated generic architectures for 100% type safety.</p>\n  </div>\n\n  <div class=\"grid\">\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg expert\">EXPERT</span>\n        <h3>Type Gymnastics: Recursion</h3>\n      </div>\n      <p>Using recursive conditional types to create deep, complex object transformations:</p>\n      <pre><code>// Deep Readonly Pattern\ntype DeepReadonly&lt;T&gt; = {\n  readonly [P in keyof T]: T[P] extends object \n    ? DeepReadonly&lt;T[P]&gt; \n    : T[P];\n};\n\n// JSON Safety Pattern\ntype JSONValue =\n  | string | number | boolean | null\n  | { [key: string]: JSONValue }\n  | JSONValue[];</code></pre>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg expert\">EXPERT</span>\n        <h3>Branded Types (Nominal Simulation)</h3>\n      </div>\n      <p>TS is structural, but branding ensures data integrity for IDs, Emails, and sensitive primitives:</p>\n      <pre><code>type Brand&lt;K, T&gt; = K &amp; { __brand: T };\n\ntype UserID = Brand&lt;string, \"UserID\"&gt;;\ntype OrderID = Brand&lt;string, \"OrderID\"&gt;;\n\nconst u: UserID = \"123\" as UserID;\nconst o: OrderID = u; // Error: OrderID !== UserID</code></pre>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg expert\">EXPERT</span>\n        <h3>Mapped Type Remapping</h3>\n      </div>\n      <p>Using the <code>as</code> clause to dynamically rename keys based on templates:</p>\n      <pre><code>interface User {\n  id: number;\n  name: string;\n}\n\ntype Getters&lt;T&gt; = {\n  [K in keyof T as `get${Capitalize&lt;string &amp; K&gt;}`]: () => T[K]\n};\n\ntype UserGetters = Getters&lt;User&gt;;\n// { getName: () => string; getId: () => number; }</code></pre>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"c-header\">\n        <span class=\"badg advanced\">ADVANCED</span>\n        <h3>Satisfies vs Explicit Annotations</h3>\n      </div>\n      <p>The <code>satisfies</code> operator (TS 4.9+) allows validation WITHOUT widening inferred types:</p>\n      <pre><code>type Palette = Record&lt;string, string | number[]&gt;;\n\nconst colors = {\n  primary: \"#00f\",\n  secondary: [0, 0, 255]\n} satisfies Palette;\n\n// colors.primary remains \"#00f\" (literal)\n// NOT widened to string | number[]</code></pre>\n    </div>\n  </div>\n</div>\n\n<div class=\"sc active\" id=\"js-iq\">\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What are the primitive data types in JavaScript?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>String, Number, Boolean, BigInt, Symbol, Undefined, and Null. They are immutable and compared by value.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is the difference between `==` and `===`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`==` checks for equality with type coercion (e.g. `1 == \"1\"` is true). `===` is strict equality and checks if both the value and the type are identical.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is a closure?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A function bundled together with its lexical environment. A closure gives you access to an outer function's scope from an inner function, even after the outer function has returned.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What does \"this\" refer to in JavaScript?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`this` refers to the object that is executing the current function. Its value is determined exactly by *how* a function is called, not where it is defined (except for arrow functions).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is the difference between var, let, and const?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`var` is function-scoped and can be re-declared. `let` is block-scoped and can be reassigned. `const` is block-scoped and its reference cannot be reassigned (though objects/arrays inside it can be mutated).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">How does the map() array method work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It creates a new array populated with the results of calling a provided function on every element in the calling array. It does not mutate the original array.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">How does the filter() array method work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It creates a new array with all elements that pass the test implemented by the provided function.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What does `async` and `await` do?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`async` makes a function return a Promise. `await` makes an async function pause execution and wait for a Promise to resolve, making asynchronous code look synchronous.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">Why do we use strict mode (`\"use strict\"`)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It catches common coding bloopers (like using undeclared variables), disables features that are confusing, and prepares code for future ECMAScript versions.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is an immediately invoked function expression (IIFE)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A function that runs as soon as it is defined: `(function() { ... })();`. Used to create a new, private block scope to avoid polluting the global namespace.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is the event loop?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>The secret behind JavaScript's asynchronous behavior. It continually checks if the call stack is empty. If it is, it takes the first callback from the microtask/macrotask queue and pushes it to the stack for execution.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is the difference between macrotasks and microtasks?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Microtasks (Promises, `queueMicrotask`, MutationObserver) have higher priority. The event loop empties the entire microtask queue before picking up the next macrotask (setTimeout, setInterval, user events).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is Debouncing?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A pattern ensuring a function is only fired once a certain amount of time has passed since it was last invoked. Useful for search box inputs so you don't hit the API on every keystroke.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is Throttling?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A pattern ensuring a function is called at most once in a specified time period. Useful for scroll or resize events so the browser doesn't crash from continuous execution.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">Explain `.bind()`, `.call()`, and `.apply()`.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>They explicitly set the value of `this`. `call` takes comma-separated arguments. `apply` takes an array of arguments. `bind` returns a *new* function with `this` permanently bound without immediately executing it.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">Why do Arrow Functions behave differently with `this`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Arrow functions do not have their own `this`. They inherit `this` from the surrounding lexical context at the time they are defined. You cannot `bind`, `call`, or `apply` a new `this` to an arrow function.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is prototypal inheritance?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>In JS, objects inherit properties and methods from a prototype object. If a property isn't found on an object, JS looks up the prototype chain until it finds it or reaches `null`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What causes memory leaks in JS?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Common causes: Accidental global variables, forgotten timers/intervals (`setInterval`), closures holding onto large outer scope variables, and detached DOM elements referenced in JS objects.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is a WeakMap or WeakSet?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Similar to Map/Set, but JS objects used as keys are held \"weakly\". This means if there are no other references to the object key, it can be garbage collected, preventing memory leaks. They cannot be iterated.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is the difference between Deep Copy and Shallow Copy?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A shallow copy copies only top-level properties; nested objects share the same reference. A deep copy creates completely new references for all nested objects (e.g., using `structuredClone(obj)`).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">Explain the concept of Currying.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Converting a function that takes multiple arguments into a sequence of functions that each take a single argument: `f(a,b,c)` becomes `f(a)(b)(c)`. Highly useful in functional composition.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">How does Service Worker caching differ from standard HTTP caching?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>HTTP caching is controlled by server headers. Service Worker caching is controlled programmatically via JavaScript using the Cache API, allowing granular offline-first strategies, bypassing the network completely.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is a SharedWorker vs WebWorker?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A standard Web Worker is accessible only to the script that spawned it. A SharedWorker can be accessed by multiple scripts, even if they are in different windows, iframes, or tabs on the exact same origin.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is CORS (Cross-Origin Resource Sharing)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A security mechanism where the browser prevents a frontend JS app to make API requests to a different domain, unless the target domain explicitly returns HTTP headers letting the browser know it's permitted.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do JavaScript Generators (`function*`) work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>They are functions that can be paused and exited, and later re-entered, preserving context (variable bindings) across re-entrances. They return an Iterator object, progressing only when `.next()` is called. They use the `yield` keyword.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What does `Object.defineProperty()` do?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It defines a new property directly on an object, or modifies an existing one, allowing strict configuration of metadata like `enumerable`, `configurable`, `writable`, or setting custom `get` and `set` traps.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How does Proxy essentially work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It allows you to create a wrapper for another object, intercepting and redefining fundamental operations for it like property lookup, assignment, enumeration, and function invocation. `const p = new Proxy(target, handler)`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is the purpose of `Symbol`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A built-in object whose constructor returns a guaranteed unique identifier. Useful to add hidden properties to an object that won't conflict with other keys or show up in standard `for...in` loops.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is the spread operator vs rest parameter?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Syntax is identical (`...`). Spread EXPANDS an array/object into individual elements (e.g., `let newArr = [...old]`). Rest COLLECTS multiple elements and condenses them into a single array element (e.g., in function args: `function f(...args)`).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is TypeScript?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A structural, strongly typed superset of JavaScript developed by Microsoft. It compiles to plain JavaScript and adds static typing, identifying errors at compile-time instead of runtime.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is the difference between implicit and explicit typing?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Explicit: You declare the type: `let count: number = 5;`. Implicit (Inference): TypeScript figures it out automatically: `let count = 5;` (TS knows it's a number).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is the \"any\" type?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It allows any type of value, effectively opting out of type-checking completely for that variable. It ruins TypeScript's benefits and should be strictly avoided in production code.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is the \"unknown\" type?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A safer alternative to `any`. You can assign anything to `unknown`, but to operate on it, TS forces you to perform a type check (type narrowing/guard) first to prove you know its structural shape.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"basic\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv basic\">BASIC</span>\n                    <span class=\"qt\">What is the \"void\" vs \"never\" type?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`void` is used for a function that returns nothing (implicitly returns undefined). `never` is used for a function that WILL NEVER return anything (e.g., it always throws an Error, or has an infinite `while(true)` loop).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is Duck Typing / Structural Typing in TS?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>TypeScript focuses on the SHAPE of data. If an object has all the properties required by an Interface, TS accepts it, regardless of whether it explicitly \"implements\" that interface. \"If it walks like a duck and quacks like a duck...\"</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is an Interface in TypeScript?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A way to name an object type. Interfaces can be extended with the `extends` keyword and can be merged automatically if declared twice.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is a Type Alias (`type`)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Creates a new name for any type. Unlike Interfaces, it can describe primitives, unions, and intersections. In modern TS, objects defined via `type` vs `interface` are almost functionally identical.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is a Tuple?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>An array with a fixed number of elements whose types are known. Example: `let person: [string, number] = [\"Alice\", 25];`</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is an Enum?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A way to give more friendly names to sets of numeric or string constants. However, TS Enums actively generate JS runtime code (unlike Interfaces), so string literal unions (`type Status = \"OK\" | \"FAIL\"`) are often heavily preferred instead.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What are Generics?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Generics provide a way to make components work over a variety of types rather than a single one. It creates a variable for a type, e.g., `<T>`. `function identity<T>(arg: T): T`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What is a Type Guard (Type Narrowing)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Using standard JS checks (like `typeof x === \"string\"`, `instanceof Array`, or `in` operator) allowing TypeScript to infer the variable's specific type within a narrower conditional block.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is a custom Type Predicate?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A function that returns a boolean, where the return type is explicitly cast as `parameterName is Type`. It tells the TS compiler to narrow the type if the function evaluates to true. Ex: `function isString(v: any): v is string`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is Intersection vs Union?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Union (`|`): a value that can be one of several types. Intersection (`&`): a value that combines multiple types into one single type, meaning the object must have ALL the properties of the intersected types.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What are Utility Types in TS?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Built-in generic tools that transform types. Examples: `Partial<T>` (all props optional), `Readonly<T>` (all props readonly), `Pick<T, K>` (select specific props), `Omit<T, K>` (remove specific props).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is keyof?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>An operator that returns a Union type composed of the names of the keys of an Object type. E.g., `type Keys = keyof { name: string; age: number }` results in `\"name\" | \"age\"`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is typeof (in TS type context)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Allows you to extract the TS type from an existing runtime JavaScript variable. `let obj = {x: 1}; type ObjType = typeof obj;`</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What are Conditional Types?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>They select one of two possible types based on a condition expressed as a type relationship test: `T extends U ? X : Y`. Extremely powerful in building dynamic utility types.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the `infer` keyword?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Used strictly within Conditional Types to pause resolution and declare a new, dynamically inferred type variable. Essential for extracting the return type of a function: `T extends (...args: any[]) => infer R ? R : any`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What does `strict: true` do in tsconfig.json?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It enables a broad suite of strict type-checking behavior (including `noImplicitAny`, `strictNullChecks`, `strictFunctionTypes`, and `strictBindCallApply`). Highly recommended for all enterprise projects.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is a Discriminated Union?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A pattern combining Union types, type guards, and a common shared property (the discriminator) to guarantee exhaustive structural safety (often used in Redux actions or state machines).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is Declaration Merging?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>The compiler merges two separate declarations declared with the exact same name into a single definition. This is why Interfaces are the standard for public APIs (consumers can easily augment your `Window` interface, for example).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is Type Variance (Covariance, Contravariance)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Advanced concepts defining how complex types (like arrays or functions) relate to each other based on their internal structural types. TS functions are contravariant in parameters, but covariant in return types to ensure safety.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"advanced\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv advanced\">ADVANCED</span>\n                    <span class=\"qt\">What is \"Index Signature\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Syntax allowing you to type objects with dynamic or unknown keys but a specific value type. `interface Dict { [key: string]: number; }`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"intermediate\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv intermediate\">INTERMEDIATE</span>\n                    <span class=\"qt\">What does `as const` do (Const Assertions)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Normally, TS widens string literals. `const obj = { a: \"hello\" }` implies `a` is a `string`. Suffixing it with `as const` implies `a` is specifically the strict read-only literal type `\"hello\"`. Deeply useful for config arrays and objects.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is `Promise.withResolvers()`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A new ES2024 feature that returns an object containing a new Promise and its `resolve` and `reject` functions. It eliminates the need to manually extract those functions from the Promise executor callback.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do `Object.groupBy()` and `Map.groupBy()` work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>They allow grouping elements of an iterable based on a string or object key provided by a callback function. `Map.groupBy` is preferred when the keys should be complex objects rather than just strings.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the \"Task\" vs \"Microtask\" execution order in the Event Loop?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>The loop executes ONE macrotask, then empties the ENTIRE microtask queue. If a microtask adds more microtasks, they are also executed before the next macrotask. This can lead to \"microtask starvation\" if not careful.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the difference between `WeakRef` and `FinalizationRegistry`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`WeakRef` lets you hold a reference to an object without preventing it from being garbage collected. `FinalizationRegistry` lets you request a callback when an object has been garbage collected, useful for cleaning up non-JS resources.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How does V8's \"TurboFan\" compiler optimize code?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It uses Speculative Optimization. It profiles the types passing through your functions (using \"Inline Caches\"). If types are consistent (monomorphic), it compiles highly optimized machine code. If types change (polymorphic/megamorphic), it de-optimizes back to the \"Ignition\" interpreter.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Temporal Dead Zone\" (TDZ) in depth?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>The period between a block's entry and the point where a variable is initialized. Accessing `let` or `const` in this zone throws a `ReferenceError`, unlike `var` which would just return `undefined`. This ensures variables are never used before their literal definition.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What are Atomics in JavaScript?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A global object providing atomic operations (like `add`, `sub`, `and`, `or`) for `SharedArrayBuffer`. It ensures that multi-threaded data operations in Web Workers are thread-safe and predictable without race conditions.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do decorators work in native JavaScript (TC39)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Native decorators (Stage 3+) allow class, method, and field annotations. They are functions that receive metadata about the target and can wrap or replace it. This differs from legacy \"experimental\" TypeScript decorators which were based on an older proposal.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain \"Shadow Realms\" (Proposed).</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A new global environment that provides a completely isolated JS execution context. It allows running code with its own global object and built-ins, preventing side effects or data leakage into the main execution realm.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is `Array.fromAsync()`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A recent addition that creates a new array from an async iterable, iterable, or array-like object. It awaits each element if necessary, making it superior to `Promise.all(arr.map(async...))` in some sequential scenarios.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do you detect memory leaks using Chrome DevTools?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Capture a \"Heap Snapshot\". Look for objects that are \"Detached\" (not in DOM but still referenced in JS). Use the \"Allocation Instrumentation\" timeline to see objects allocated but never released during specific interactions.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain the \"Pipeline Operator\" (`|>`) proposal.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It facilitates functional pipe-lining by piping the result of one expression as an argument to the next function: `val |> f |> g` is equivalent to `g(f(val))`. It significantly improves readability in nested logic.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Explicit Resource Management\" (`using`) in JS?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Introduced in ES2024, it allows for scoping resources (like file handles or DB connections) that must be closed or cleaned up. Using the `using` keyword ensures the `[Symbol.dispose]` method is called automatically when leaving scope.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How does the \"Module Preload\" link relation help performance?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It tells the browser to fetch and parse a JS module (and its entire dependency tree) early, without executing it immediately, reducing the waterfall effect in large ES Module architectures.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Hydration\" in a JavaScript context?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>The process where client-side JavaScript attaches event listeners to static HTML sent from the server (SSR), making it interactive. Optimization techniques like \"Resumability\" (Qwik) or \"Progressive Hydration\" aim to reduce this costly step.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is a \"Branded Type\" (Nominal Typing)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>TS is structural, but branding simulates nominal typing by adding a unique \"tag\" property: `type Email = string & { __brand: \"Email\" }`. This prevents passing a generic string into a function expecting an Email.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain \"Recursive Types\" in TS.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A type that references itself. Useful for deep object trees or JSON structures: `type JSONValue = string | number | boolean | null | JSONObject | JSONArray;`. TS 4.1+ improved support for deep recursion limits.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do \"Template Literal Types\" work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>They allow building types based on string literals: `type Direction = \"top\" | \"bottom\"; type Padded = `padding-${Direction}`;`. They enable massive type safety for CSS classes, event names, and i18n keys.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Distributive Conditional Types\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>When a conditional type (`T extends U ? X : Y`) is checking a Union type `T`, TS automatically distributes the check over each member of the union. Use `[T] extends [U]` to disable this \"distribution\".</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do you extract the return type of a function dynamicallly?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Using the `ReturnType<T>` utility, which internally uses the `infer` keyword: `T extends (...args: any) => infer R ? R : any`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the `satisfies` operator?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Introduced in TS 4.9, it validates that an expression matches a type WITHOUT widening or changing the inferred type. `const colors = { red: \"#f00\" } satisfies Record<string, string>;` keeps the specific key `\"red\"` instead of widening to `string`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain \"Assertion Functions\" in TS.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Functions that throw an error if a condition isn't met. By using the `asserts condition` return type, TS knows the condition is guaranteed to be true for the rest of the scope if the function returns normally.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Module Augmentation\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A way to add declarations to an existing module. Useful when using 3rd party libraries that have missing or outdated types: `declare module \"some-lib\" { ... }`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What are \"Global Type Declarations\" (`.d.ts`)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Files that provide type information for code that already exists in JavaScript but doesn't have types. They are purely for the compiler and emit no JS code.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How does \"Const Type Parameters\" (`<const T>`) work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Introduced in TS 5.0, it allows generics to be inferred as literal `const` values by default, avoiding the need for the user to append `as const` manually to arguments.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Exhaustiveness Checking\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A pattern using the `never` type in a `switch` default case to ensure all possible members of a Union type are handled. If a new member is added to the union, TS will alert you that the code is no longer exhaustive.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain \"Covariance\" vs \"Contravariance\" in TS.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Covariance: `T` can be replaced by a more specific type (Standard for properties). Contravariance: `T` can be replaced by a more general type (Standard for function parameters). TS enforces these rules to ensure type safety across inheritance.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the \"Omit\" vs \"Exclude\" utility type?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`Exclude<Union, Members>` removes members from a UNION type. `Omit<Object, Keys>` removes specific properties from an OBJECT type.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do you handle \"Branded Strings\" with Template Literals?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>By creating a type that combines a string with a unique intersection: `type UserID = `user_${string}` & { __brand: \"UserID\" }`. This ensures any ID used must match the prefix AND be explicitly cast/verified.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Mapped Type Remapping\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Using the `as` clause inside a mapped type to change key names: `type Getters<T> = { [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K] }`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the purpose of `requestIdleCallback`?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It allows scheduling background and low-priority work during the browser's idle periods, without affecting latency-critical events like animation and input response. Ideal for non-essential telemetry or pre-fetching.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How does `Atomics.wait()` and `Atomics.notify()` work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>They provide a way for workers to sleep until a value in a `SharedArrayBuffer` changes. `wait` blocks the thread until notified, while `notify` wakes up threads waiting on that specific index. Essential for low-level synchronization.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain the \"Same-Origin Policy\" (SOP) vs CORS.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>SOP is a fundamental browser security policy that prevents scripts on one origin from accessing data on another origin. CORS is a mechanism that allows servers to explicitly opt-in to sharing resources across origins.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is a \"User-Agent Client Hints\" (UA-CH)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A modern, more privacy-preserving alternative to the `User-Agent` string. It allows browsers to selectively share device/browser metadata with servers via HTTP headers or JS API, reducing passive fingerprinting.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do \"Private Class Features\" (#prefix) differ from `private` keyword in TS?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`#private` is a native JS feature that provides hard runtime encapsulation; the field is truly inaccessible outside the class. TS `private` is a compile-time check only; the field is still accessible in the emitted JavaScript.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"BigInt\" and why can't it be mixed with \"Number\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>`BigInt` handles integers larger than $2^{53}-1$. Mixing them with standard `Number` is prohibited because it would lead to silent precision loss during implicit type conversion.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain \"Top-level await\" and its impact on module loading.</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>It allows using `await` at the top level of a module. While convenient, it makes the module (and its importers) wait for the promise to resolve before execution continues, which can delay application startup if used poorly.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Structural\" vs \"Nominal\" typing in depth?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Structural (TS): Types are compatible if their members match. Nominal (Java/C#): Types are only compatible if they have the same name/declaration. TS uses structural typing for flexibility, but can simulate nominal typing via branding.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Type Argument Inference\" for functions?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>The process where TS automatically determines the types for generic parameters based on the values passed to the function. If inference fails, you must provide them explicitly: `f<string>(\"val\")`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How do you handle \"Recursive Conditional Types\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>By having a conditional type reference itself in its branches. TS 4.1+ allows this as long as there is a clear base case, enabling complex types like `DeepPartial<T>` or `DeepReadonly<T>`.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is the \"NoInfer\" intrinsic type?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Introduced in TS 5.4, it prevents a generic type parameter from being a candidate for type inference in a specific location, forcing TS to use the inference from other locations instead.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">Explain \"String Mapping Types\" (Uppercase, Lowercase, etc.).</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Intrinsic type operators that transform string literal types. Invaluable when used with Template Literal Types to enforce casing conventions across API keys or file paths.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is a \"Global augmentation\" vs \"Module augmentation\"?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>Module: Adds to a specific exported module. Global: Adds to the global namespace (e.g., adding a new property to the `Window` object or `ProcessEnv`).</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">How does \"Discriminated Union\" Exhaustiveness with `default` work?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>By assigning the `default` case to a variable of type `never`. If a new case is added to the union but not handled in the switch, the assignment `const _exhaustiveCheck: never = member` will cause a compile error.</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div class=\"qa\" data-l=\"expert\">\n                  <div class=\"qh\">\n                    <span class=\"badg lv expert\">EXPERT</span>\n                    <span class=\"qt\">What is \"Metadata Reflection\" (reflect-metadata)?</span>\n                    <span class=\"to tg\">+</span>\n                  </div>\n                  <div class=\"qa-b qb\">\n                    <div class=\"qi\">\n                      <p>A polyfill/proposal that allows attaching and retrieving metadata at runtime on classes and members. Heavily used in frameworks like NestJS or Angular for dependency injection.</p>\n                    </div>\n                  </div>\n                </div>\n\n<h2 style=\"color:var(--js);font-family:'Literata',serif;font-size:1.5rem;margin-bottom:.3rem\">JavaScript Interview Q&amp;A</h2>\n<p class=\"si\">95 core questions — basic to very advanced. Click any question to reveal the answer.</p>\n<div class=\"fb\">\n<button class=\"ft active\" data-f=\"all\">All (95)</button>\n<button class=\"ft\" data-f=\"b\">Basic</button>\n<button class=\"ft\" data-f=\"i\">Intermediate</button>\n<button class=\"ft\" data-f=\"a\">Advanced</button>\n</div>\n<div class=\"ct\">Showing <b>95</b> questions</div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is JavaScript?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A high-level, interpreted, dynamically-typed programming language primarily used for web development. It runs in browsers and on servers (Node.js). It's single-threaded but non-blocking via the event loop.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are the 8 data types in JavaScript?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Primitives (7):</strong> string, number, bigint, boolean, undefined, symbol, null. <strong>Non-primitive (1):</strong> object (includes arrays, functions, dates, RegExp, etc.).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between == and ===?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>==</code> compares after type coercion (<code>'5'==5</code> is true). <code>===</code> compares value AND type (<code>'5'===5</code> is false). Always use <code>===</code> to avoid unexpected bugs.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between null and undefined?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>undefined</code>: declared but not assigned, missing parameter, missing property. <code>null</code>: intentionally empty, explicitly assigned. <code>typeof null</code> is <code>'object'</code> (famous bug). <code>null == undefined</code> is true, <code>null === undefined</code> is false.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are var, let, and const?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>var</code>: function-scoped, hoisted as undefined, can redeclare. <code>let</code>: block-scoped, TDZ, can reassign. <code>const</code>: block-scoped, must initialize, can't reassign (but object contents ARE mutable). Rule: default to const, use let for reassignment, never use var.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is hoisting?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>JS moves declarations to the top of their scope before execution. <code>var</code> is hoisted as <code>undefined</code>. <code>let/const</code> are hoisted but NOT initialized (TDZ — ReferenceError if accessed early). Function declarations are fully hoisted. Function expressions are NOT.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are falsy values?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Eight values: <code>false, 0, -0, 0n, \"\", null, undefined, NaN</code>. Everything else is truthy — including <code>\"0\", \"false\", [], {}</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between function declaration and expression?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Declaration: <code>function foo(){}</code> — fully hoisted, callable before definition. Expression: <code>const foo = function(){}</code> — NOT hoisted, only callable after assignment. Arrow functions are always expressions.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are arrow functions?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Shorter syntax <code>()=&gt;{}</code>. Key differences: no own <code>this</code> (lexical), no <code>arguments</code> object, can't be used as constructors, no <code>prototype</code>. Use for callbacks; use regular functions for methods and constructors.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the spread operator?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>...</code> expands iterables. Arrays: <code>[...a,...b]</code> merge, <code>[...arr]</code> copy. Objects: <code>{...o1,...o2}</code> merge. Function args: <code>fn(...args)</code>. Rest (collecting): <code>function(...args)</code>, <code>const {a,...rest}=obj</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is destructuring?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Extract values from arrays/objects into variables. Object: <code>const {name, age=25} = user</code>. Array: <code>const [first,,third] = arr</code>. Nested: <code>const {address:{city}} = user</code>. Rename: <code>const {name:userName} = user</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is template literal?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Backtick strings supporting interpolation <code>`Hello ${name}`</code>, multi-line strings, and tagged templates <code>tag`text ${expr}`</code> where tag is a function.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the typeof operator?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Returns type as string: <code>typeof 'hi'→'string'</code>, <code>typeof 42→'number'</code>, <code>typeof true→'boolean'</code>, <code>typeof undefined→'undefined'</code>, <code>typeof null→'object'</code> (bug!), <code>typeof {}→'object'</code>, <code>typeof []→'object'</code>, <code>typeof function(){}→'function'</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is NaN?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>'Not a Number' — result of invalid math (<code>0/0</code>, <code>parseInt('abc')</code>). <code>typeof NaN</code> is <code>'number'</code>. <code>NaN !== NaN</code> is true (the only value not equal to itself). Check with <code>Number.isNaN(x)</code> (not global <code>isNaN</code> which coerces).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between for...in and for...of?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>for...in</code>: iterates object <strong>keys</strong> (includes prototype chain — guard with hasOwnProperty). <code>for...of</code>: iterates <strong>values</strong> of iterables (arrays, strings, Maps, Sets — NOT plain objects). Use <code>Object.entries()</code> + <code>for...of</code> for objects.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are truthy and falsy values used for?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Implicit boolean conversion in conditions: <code>if(value)</code>, <code>value &amp;&amp; doSomething()</code>, <code>value || default</code>, ternary <code>value ? a : b</code>. Common gotcha: <code>0</code> and <code>''</code> are falsy but might be valid values — use <code>??</code> for null/undefined only.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is scope in JavaScript?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Where variables are accessible. <strong>Global:</strong> everywhere. <strong>Function:</strong> inside the function (var). <strong>Block:</strong> inside <code>{}</code> (let/const). Scope chain: inner functions can access outer scope variables (not vice versa). This creates closures.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a callback function?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A function passed as an argument to another function, to be called later. Examples: <code>setTimeout(callback, 1000)</code>, <code>arr.map(callback)</code>, <code>btn.addEventListener('click', callback)</code>. Callbacks enable async patterns but can lead to 'callback hell' (nested callbacks).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the DOM?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Document Object Model — a tree representation of HTML that JavaScript can manipulate. Methods: <code>getElementById</code>, <code>querySelector</code>, <code>createElement</code>, <code>appendChild</code>, <code>addEventListener</code>. The browser builds the DOM from HTML, then renders it visually.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is JSON?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>JavaScript Object Notation — a text format for data exchange. <code>JSON.stringify(obj)</code> converts to string. <code>JSON.parse(str)</code> converts back. Limitations: no functions, no undefined, no NaN/Infinity, no circular references, dates become strings.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between slice, splice, and split?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>slice(start,end)</code>: returns new array/string portion, doesn't mutate. <code>splice(start,deleteCount,...items)</code>: modifies original array in-place. <code>split(separator)</code>: string method, splits into array. Modern: use <code>toSpliced()</code> for immutable splice.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are higher-order functions?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Functions that take functions as arguments or return functions. Examples: <code>map</code>, <code>filter</code>, <code>reduce</code>, <code>forEach</code>, <code>sort</code>, <code>find</code>, <code>some</code>, <code>every</code>. They enable functional programming patterns and method chaining.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is map, filter, reduce?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>map(fn)</code>: transforms each element, returns new array of same length. <code>filter(fn)</code>: keeps elements where fn returns true, returns shorter array. <code>reduce(fn, init)</code>: accumulates all elements into single value. All return new arrays (don't mutate original).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is optional chaining (?.) ?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Short-circuits to undefined if a property is null/undefined: <code>user?.address?.city</code> instead of <code>user &amp;&amp; user.address &amp;&amp; user.address.city</code>. Works on properties, arrays <code>arr?.[0]</code>, and methods <code>fn?.()</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is nullish coalescing (??)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>??</code> returns the right side only when left is <code>null</code> or <code>undefined</code> (not for <code>0</code> or <code>''</code>). Compare: <code>0 || 'default'</code> → <code>'default'</code> (0 is falsy). <code>0 ?? 'default'</code> → <code>0</code> (0 is not null/undefined). Use <code>??</code> when 0 or empty string are valid values.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a Promise?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>An object representing eventual completion/failure of an async operation. Three states: pending → fulfilled (resolved) or rejected. Created with <code>new Promise((resolve, reject) =&gt; {})</code>. Consumed with <code>.then()</code>, <code>.catch()</code>, <code>.finally()</code>. Enables cleaner async code than callbacks.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is async/await?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Syntactic sugar for Promises. <code>async function</code> always returns a Promise. <code>await</code> pauses execution until Promise resolves. Use <code>try/catch</code> for error handling. Makes async code look synchronous and readable.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between let and const for objects?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const</code> prevents reassignment of the variable binding, NOT mutation of the value. <code>const obj = {}</code> → <code>obj.name = 'hi'</code> works (mutation). <code>obj = {}</code> fails (reassignment). To truly freeze: <code>Object.freeze(obj)</code> — but it's shallow.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are default parameters?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>function greet(name = 'World') {}</code> — default used when argument is <code>undefined</code> (not null). Works with destructuring: <code>function({x = 0, y = 0} = {}) {}</code>. Evaluated left-to-right, later params can reference earlier ones.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is event bubbling?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>When an event fires on an element, it 'bubbles' up through its ancestors: target → parent → grandparent → document. You can stop it with <code>e.stopPropagation()</code>. The opposite (capturing) goes top-down. Default is bubbling phase.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between innerHTML, textContent, and innerText?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>innerHTML</code>: gets/sets HTML markup (parses tags, XSS risk). <code>textContent</code>: gets/sets raw text of all elements (fastest, includes hidden). <code>innerText</code>: gets visible text only (triggers reflow, slower). Prefer <code>textContent</code> for setting text safely.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is Array.isArray()?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>The reliable way to check if a value is an array. <code>typeof []</code> returns <code>'object'</code> (not helpful). <code>[] instanceof Array</code> fails across frames/iframes. <code>Array.isArray([]) → true</code>, <code>Array.isArray({}) → false</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a pure function?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A function that: 1) always returns the same output for the same inputs, and 2) has no side effects (doesn't modify external state). <code>const add = (a,b) =&gt; a+b</code> is pure. <code>Math.random()</code> and functions that modify global variables are impure.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the rest parameter?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>function sum(...nums) {}</code> collects remaining arguments into an array. Unlike <code>arguments</code> (array-like, not a real array), rest params ARE real arrays. Must be the last parameter. Works in destructuring: <code>const [first, ...rest] = arr</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is String interpolation?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Embedding expressions in template literals: <code>`Hello ${name}, you are ${age} years old`</code>. Any valid JS expression works inside <code>${}</code> — function calls, ternaries, arithmetic. Much cleaner than string concatenation with <code>+</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain closures with a practical example.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A closure is a function that retains access to its outer scope's variables even after the outer function returns. Example: <code>function counter() { let n = 0; return { inc: () =&gt; ++n, get: () =&gt; n }; }</code> — the returned object 'closes over' n, keeping it alive and private. Uses: data privacy, factories, memoization, event handlers with state.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain the event loop in detail.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>JS is single-threaded. The event loop: <strong>1)</strong> Execute sync code on call stack. <strong>2)</strong> When stack is empty, process ALL microtasks (Promise.then, queueMicrotask, MutationObserver). <strong>3)</strong> Process ONE macrotask (setTimeout, setInterval, I/O, UI rendering). <strong>4)</strong> Repeat. Key: microtasks always run before macrotasks. Nested microtasks run immediately too (can starve macrotasks).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain 'this' — all rules with priority.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Priority (highest to lowest): <strong>1) new</strong>: this = new object. <strong>2) bind/call/apply</strong>: this = specified object. <strong>3) Object method</strong>: this = calling object. <strong>4) Default</strong>: this = window (or undefined in strict mode). <strong>Arrow functions</strong>: no own this — always inherits from enclosing scope. This is why arrows are perfect for callbacks but wrong for object methods.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the Temporal Dead Zone (TDZ)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>The period between entering a block and the let/const declaration. Accessing the variable during TDZ throws ReferenceError. Exists because let/const ARE hoisted (the engine knows about them) but NOT initialized (they have no value yet). var doesn't have TDZ — it's hoisted and initialized as undefined.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is event delegation?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Attach ONE listener to a parent instead of listeners on each child. Use <code>e.target</code> to determine which child was clicked. Benefits: works for dynamically added elements, fewer listeners, less memory. Pattern: <code>parent.addEventListener('click', e =&gt; { if(e.target.matches('.item')) handleClick(e.target); })</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is Promise.all vs allSettled vs race vs any?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>all([p1,p2])</strong>: resolves when ALL resolve, rejects on FIRST rejection. <strong>allSettled</strong>: waits for ALL, returns [{status,value/reason}], never rejects. <strong>race</strong>: returns FIRST to settle (resolve or reject). <strong>any</strong>: returns FIRST to resolve, only rejects if ALL fail (AggregateError). Use all for parallel dependent tasks, allSettled for independent tasks, race for timeouts, any for fastest mirror.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain prototypal inheritance.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Every object has [[Prototype]] link to another object. Property lookup walks the chain: object → prototype → prototype's prototype → Object.prototype → null. <code>Object.create(proto)</code> creates object with custom prototype. ES6 classes are syntactic sugar — <code>extends</code> sets up the chain, <code>super()</code> calls parent constructor. <code>instanceof</code> checks the chain.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Shallow copy vs deep copy — methods and gotchas.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Shallow</strong>: copies first level only. <code>{...obj}</code>, <code>Object.assign({},obj)</code>, <code>[...arr]</code>. Nested objects share references. <strong>Deep</strong>: copies ALL levels. <code>structuredClone(obj)</code> (modern, handles dates/regex/maps/circular refs). <code>JSON.parse(JSON.stringify(obj))</code> loses functions, undefined, dates, Infinity, NaN, circular refs. Always prefer structuredClone.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the difference between call, apply, and bind?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>All set <code>this</code> explicitly. <code>call(thisArg, a, b)</code>: invokes immediately, individual args. <code>apply(thisArg, [a,b])</code>: invokes immediately, array args. <code>bind(thisArg, a)</code>: returns NEW function with bound this (doesn't invoke). Bind is for creating callbacks with specific context. Fun fact: <code>Math.max.apply(null, arr)</code> was the old way to spread — now use <code>Math.max(...arr)</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is a closure and how does it cause memory leaks?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A closure retains references to outer variables. Memory leak: if a closure references a large object that's no longer needed, the GC can't collect it because the closure keeps it alive. Example: event listener closures that aren't removed. Fix: remove listeners on cleanup, null out references, use WeakRef for caches.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain the classic for loop + var + setTimeout problem.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>for(var i=0;i&lt;3;i++) setTimeout(()=&gt;console.log(i),100)</code> prints 3,3,3. Why: var is function-scoped — all callbacks share the SAME i, which is 3 after the loop finishes. Fixes: <strong>1)</strong> Use <code>let</code> (block-scoped, new binding per iteration). <strong>2)</strong> IIFE: <code>((j)=&gt;setTimeout(()=&gt;console.log(j),100))(i)</code>. <strong>3)</strong> <code>setTimeout(console.log, 100, i)</code> (third arg).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are Map and Set? When use over Object/Array?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Map</strong>: any key type, ordered, iterable, .size, no prototype pollution. Use when: keys are objects/functions, you need ordered iteration, frequent add/delete. <strong>Set</strong>: unique values only, .has() is O(1). Use for: deduplication <code>[...new Set(arr)]</code>, membership testing. Object/Array are still better for JSON data and simple cases.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is debounce vs throttle?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Debounce</strong>: waits until user STOPS doing something for N ms. Use: search input, resize handler. <strong>Throttle</strong>: executes at most once every N ms. Use: scroll handler, rate-limiting API calls. Debounce delays, throttle caps. Both use closures + setTimeout internally.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the difference between Object.keys, values, entries?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>Object.keys(obj)</code>: array of own enumerable string keys. <code>Object.values(obj)</code>: array of own enumerable values. <code>Object.entries(obj)</code>: array of [key, value] pairs. None include Symbol keys or prototype properties. For Maps: use <code>map.keys()</code>, <code>map.values()</code>, <code>map.entries()</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is a generator function?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>function* gen() { yield 1; yield 2; }</code> — can pause and resume. Returns an iterator with <code>.next()</code> → <code>{value, done}</code>. Use cases: lazy evaluation, infinite sequences, implementing custom iterables, controlled async flow. <code>yield*</code> delegates to another generator.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain async iteration (for await...of).</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>for await (const item of asyncIterable) {}</code> — iterates over async iterables (streams, paginated APIs). The iterable's <code>[Symbol.asyncIterator]()</code> returns objects with async <code>next()</code> methods. Use with async generators: <code>async function* fetchPages() { while(hasMore) { yield await fetchPage(); } }</code></p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is memoization?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Caching function results based on inputs to avoid redundant computation. Implementation: <code>function memoize(fn) { const cache = new Map(); return (...args) =&gt; { const key = JSON.stringify(args); if(cache.has(key)) return cache.get(key); const result = fn(...args); cache.set(key, result); return result; }; }</code> Uses closures to persist the cache.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are tagged template literals?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>tag`text ${expr}`</code> where tag is a function receiving strings array and interpolated values. Used by: styled-components (<code>css`color: red`</code>), GraphQL (<code>gql`query{}`</code>), i18n, SQL sanitization. The tag function can transform or validate the template. You can return anything, not just strings.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the difference between Error types?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>Error</code>: base class. <code>TypeError</code>: wrong type (<code>null.prop</code>). <code>ReferenceError</code>: undeclared variable. <code>SyntaxError</code>: parse error. <code>RangeError</code>: value out of range. <code>URIError</code>: bad URI. Custom: <code>class AppError extends Error { constructor(msg,code) { super(msg); this.code=code; } }</code></p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is Symbol and when do you use it?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Unique, immutable identifier: <code>const id = Symbol('description')</code>. Every Symbol() is unique. Not visible in <code>for...in</code>, <code>Object.keys()</code>, <code>JSON.stringify()</code>. Use cases: private-ish object keys, avoiding name collisions in libraries, well-known symbols (<code>Symbol.iterator</code>, <code>Symbol.toPrimitive</code>).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain Object.freeze, seal, preventExtensions.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>Object.freeze()</code>: no add, no delete, no modify — completely locked (shallow). <code>Object.seal()</code>: no add, no delete, CAN modify existing. <code>Object.preventExtensions()</code>: no add, CAN delete and modify. All are shallow — nested objects remain mutable. <code>Object.isFrozen/isSealed/isExtensible</code> to check.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the arguments object?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Array-like object available in regular functions containing all passed arguments. NOT available in arrow functions. NOT a real array (no map/filter). Convert: <code>[...arguments]</code> or <code>Array.from(arguments)</code>. Modern replacement: rest parameters <code>(...args)</code> — always use rest instead.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is strict mode?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Enabled with <code>'use strict'</code>. Effects: prevents accidental globals (undeclared = error), disables with statement, makes eval safer, prevents duplicate params, this is undefined (not window) in regular functions, prohibits deleting non-configurable properties. Modules and classes are always strict.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is currying?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Transforming <code>f(a,b,c)</code> into <code>f(a)(b)(c)</code>. Each call returns a new function until all arguments are collected. Use: partial application <code>const add5 = curry(add)(5)</code>, creating reusable utilities, point-free programming. Libraries like lodash/ramda provide curry helpers.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the Proxy object?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Intercepts operations on objects: get, set, delete, has, apply, construct, etc. <code>new Proxy(target, handler)</code>. Real uses: validation (reject invalid values), reactive systems (Vue.js), logging/debugging, access control, default values for missing properties, negative array indexing.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is Reflect?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Built-in object with methods matching Proxy trap names: <code>Reflect.get()</code>, <code>Reflect.set()</code>, <code>Reflect.has()</code>, etc. Provides the default behavior that Proxy traps can call. More reliable than direct operations — <code>Reflect.defineProperty()</code> returns boolean instead of throwing like <code>Object.defineProperty()</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">How does garbage collection work in JavaScript?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>JS uses <strong>mark-and-sweep</strong>: the GC starts from roots (global, stack), marks all reachable objects, then sweeps (frees) unreachable ones. Common leak patterns: forgotten timers/intervals, detached DOM references in closures, growing arrays/maps never cleaned, circular references (handled by modern GC but still add pressure).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are WeakMap and WeakSet?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>WeakMap</strong>: keys must be objects, weakly held — GC can collect keys if no other references exist. Not iterable, no .size. Use: private data per object, caching without leaks. <strong>WeakSet</strong>: objects only, weakly held. Use: tracking visited objects, tagging objects without preventing GC.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the difference between synchronous and asynchronous iteration?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Sync</strong>: <code>for...of</code> with <code>[Symbol.iterator]</code>, yields values immediately. <strong>Async</strong>: <code>for await...of</code> with <code>[Symbol.asyncIterator]</code>, yields Promises. Async iterators are created with <code>async function*</code> generators. Use for: reading streams, paginated APIs, processing data as it arrives.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the new keyword doing under the hood?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1)</strong> Creates a new empty object. <strong>2)</strong> Sets its [[Prototype]] to the constructor's .prototype. <strong>3)</strong> Calls the constructor with <code>this</code> = the new object. <strong>4)</strong> Returns the object (unless constructor explicitly returns an object). That's why arrow functions can't be constructors — they have no .prototype and can't bind this.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is structuredClone?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Modern deep cloning: <code>structuredClone(obj)</code>. Handles: nested objects, arrays, Maps, Sets, Dates, RegExp, ArrayBuffers, circular references. Does NOT handle: functions, DOM nodes, Error objects, property descriptors, prototype chain. Available in all modern browsers and Node.js 17+.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain Promise chaining vs async/await.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>fetchUser().then(u =&gt; fetchPosts(u.id)).then(posts =&gt; render(posts)).catch(handleError)</code> vs <code>try { const u = await fetchUser(); const posts = await fetchPosts(u.id); render(posts); } catch(e) { handleError(e); }</code> — async/await is cleaner for sequential operations. Promise.all + await is best for parallel operations.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is Object.defineProperty?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Defines or modifies a property with fine-grained control: <code>Object.defineProperty(obj, 'name', { value: 'Alice', writable: false, enumerable: true, configurable: false })</code>. Also supports getter/setter: <code>{ get() { return this._name; }, set(v) { this._name = v; } }</code>. Foundation for Vue 2's reactivity system.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are Iterables and Iterators?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Iterable</strong>: object with <code>[Symbol.iterator]()</code> method (strings, arrays, Maps, Sets). <strong>Iterator</strong>: object with <code>.next()</code> → <code>{value, done}</code>. Custom: <code>const iterable = { [Symbol.iterator]() { let i=0; return { next() { return i&lt;3 ? {value:i++,done:false} : {done:true}; } }; } }</code>. Enables <code>for...of</code>, spread, destructuring.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the difference between setTimeout(fn,0) and queueMicrotask(fn)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>setTimeout(fn,0)</code>: adds to macrotask queue — runs after ALL microtasks and any pending rendering. <code>queueMicrotask(fn)</code>: adds to microtask queue — runs before any macrotasks, after current synchronous code. Promise.then callbacks are also microtasks. This order difference matters for timing-sensitive code.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is a Web Worker?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Runs JS in a background thread — true parallelism. Created with <code>new Worker('worker.js')</code>. Communicate via <code>postMessage/onmessage</code>. Workers can't access DOM, window, or document. Use for: heavy computation, data processing, image manipulation. <code>SharedWorker</code> is shared across tabs. <code>Service Worker</code> handles network requests (offline, caching).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are ES2023-2024 new features?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>ES2023</strong>: <code>Array.findLast/findLastIndex</code>, <code>toSorted/toReversed/toSpliced/with</code> (immutable array methods), hashbang grammar. <strong>ES2024</strong>: <code>Object.groupBy()</code>, <code>Map.groupBy()</code>, <code>Promise.withResolvers()</code>, well-formed Unicode strings, Atomics.waitAsync. These are actively asked in senior interviews.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the module system (import/export)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>ES Modules: <code>export const x = 1</code>, <code>export default fn</code>, <code>import {x} from './mod'</code>, <code>import fn from './mod'</code>. Live bindings (exports reflect current value). Static analysis (tree-shakable). <code>import()</code> for dynamic/lazy loading. CommonJS (<code>require/module.exports</code>) is Node.js legacy — synchronous, not tree-shakable.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the difference between deep equality and shallow equality?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Shallow</strong>: compares first-level values with <code>===</code>. <code>{a:1} !== {a:1}</code> (different references). <strong>Deep</strong>: recursively compares all nested values. No built-in deep equality in JS. Options: <code>JSON.stringify(a) === JSON.stringify(b)</code> (order-dependent, loses types), lodash <code>_.isEqual()</code>, custom recursive function. React uses <code>Object.is()</code> for state comparison.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is short-circuit evaluation?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>&amp;&amp;</code>: returns first falsy value (or last if all truthy). <code>||</code>: returns first truthy value (or last if all falsy). Uses: <code>user &amp;&amp; user.name</code> (safe access), <code>name || 'Anonymous'</code> (default). Gotcha: <code>0 || 'default'</code> gives <code>'default'</code> because 0 is falsy — use <code>??</code> for null/undefined only.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement debounce from scratch.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>function debounce(fn, delay) { let timer; return (...args) =&gt; { clearTimeout(timer); timer = setTimeout(() =&gt; fn(...args), delay); }; }</code> Uses closure to persist timer. Each call resets the timer. The function only fires after the user stops calling for the specified delay. Can be enhanced with leading/trailing options and cancel method.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement throttle from scratch.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>function throttle(fn, limit) { let inThrottle; return (...args) =&gt; { if (!inThrottle) { fn(...args); inThrottle = true; setTimeout(() =&gt; inThrottle = false, limit); } }; }</code> Ensures function runs at most once per interval. First call executes immediately. Uses closure for the flag. Can be enhanced with trailing call option.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement a simple Promise from scratch.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Core: store state (pending/fulfilled/rejected), value, and callback queue. <code>resolve()</code> transitions state and runs stored callbacks. <code>.then()</code> returns a new Promise (for chaining), stores callbacks if pending, runs immediately if already resolved. Key: callbacks must be async (microtask) per spec. This demonstrates understanding of the Promise specification (Promises/A+).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement curry function.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const curry = (fn) =&gt; { const arity = fn.length; return function curried(...args) { return args.length &gt;= arity ? fn(...args) : (...more) =&gt; curried(...args, ...more); }; }</code> Recursively collects arguments until enough are gathered. <code>curry(add)(1)(2)</code> and <code>curry(add)(1,2)</code> both work. Placeholder support adds complexity.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement Function.prototype.bind polyfill.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>Function.prototype.myBind = function(context, ...boundArgs) { const fn = this; return function(...args) { return fn.apply(context, [...boundArgs, ...args]); }; }</code> Uses closure to capture context and partial args. Real bind also handles 'new' calls and preserves prototype chain — this simplified version covers 90% of interview expectations.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain the JavaScript engine pipeline.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1) Parsing</strong>: source → AST (Abstract Syntax Tree). <strong>2) Interpretation</strong>: AST → bytecode (executed immediately by interpreter). <strong>3) Profiling</strong>: engine monitors 'hot' functions (called frequently). <strong>4) JIT Compilation</strong>: hot functions compiled to optimized machine code. <strong>5) Deoptimization</strong>: if assumptions are wrong (e.g., type changed), falls back to interpreter. V8 uses Ignition (interpreter) + TurboFan (compiler).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What are memory leaks in JavaScript? How to detect and fix?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Common leaks: <strong>1)</strong> Forgotten timers/intervals (clear them!). <strong>2)</strong> Detached DOM references in closures. <strong>3)</strong> Closures retaining large objects unnecessarily. <strong>4)</strong> Growing caches without eviction. <strong>5)</strong> Event listeners not removed. Detect: Chrome DevTools Memory tab — take heap snapshots, compare, find growing objects. Fix: cleanup functions, WeakMap/WeakRef for caches, remove listeners, null out references.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain the Proxy/Reflect API with real use cases.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Proxy</strong> intercepts 13 operations: get, set, has, deleteProperty, apply, construct, etc. <strong>Reflect</strong> provides default behavior for each trap. Use cases: <strong>1) Validation</strong>: reject invalid values in set trap. <strong>2) Reactive systems</strong>: trigger UI updates on property change (Vue 3's reactivity). <strong>3) Negative indexing</strong>: <code>arr[-1]</code>. <strong>4) Auto-vivification</strong>: auto-create nested objects on access. <strong>5) Access logging</strong>: track all property reads/writes.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain JavaScript module systems (ESM vs CJS vs AMD vs UMD).</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>ESM</strong>: <code>import/export</code>, static analysis, tree-shakable, async loading, live bindings. <strong>CJS</strong>: <code>require/module.exports</code>, Node.js legacy, synchronous, copies values. <strong>AMD</strong>: <code>define([],(deps)=&gt;{})</code>, browser async loading (RequireJS), mostly obsolete. <strong>UMD</strong>: wrapper that works in all systems (CJS/AMD/global). Modern: use ESM everywhere. Node.js supports both via .mjs extension or package.json <code>\"type\":\"module\"</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is tail call optimization (TCO)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>When the last action of a function is a function call, the engine can reuse the current stack frame instead of adding a new one — prevents stack overflow for recursive functions. Only Safari implements TCO in strict mode. Other engines don't. Workaround: convert recursion to iteration, or use trampolining: <code>function trampoline(fn) { let result = fn(); while(typeof result === 'function') result = result(); return result; }</code></p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement EventEmitter from scratch.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>class EventEmitter { constructor() { this.events = {}; } on(event, fn) { (this.events[event] ??= []).push(fn); return this; } off(event, fn) { this.events[event] = this.events[event]?.filter(f =&gt; f !== fn); } emit(event, ...args) { this.events[event]?.forEach(fn =&gt; fn(...args)); } once(event, fn) { const wrapper = (...args) =&gt; { fn(...args); this.off(event, wrapper); }; this.on(event, wrapper); } }</code> Demonstrates closures, this binding, and the observer pattern.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the difference between concurrency and parallelism in JS?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Concurrency</strong>: multiple tasks make progress (event loop — one thread, interleaved execution). <strong>Parallelism</strong>: multiple tasks run simultaneously (Web Workers — multiple threads). JS main thread is concurrent, not parallel. Web Workers enable true parallelism. SharedArrayBuffer + Atomics enable shared memory between workers (with synchronization).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain WeakRef and FinalizationRegistry.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>WeakRef</strong>: holds weak reference to object. <code>ref.deref()</code> returns object or undefined if GC'd. Use for caches that shouldn't prevent garbage collection. <strong>FinalizationRegistry</strong>: register callbacks that fire when objects are GC'd. <code>const registry = new FinalizationRegistry(heldValue =&gt; { cleanup(heldValue); }); registry.register(obj, 'cleanup-token')</code>. Both are non-deterministic — GC timing is unpredictable.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement deep clone without structuredClone.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>function deepClone(obj, seen = new WeakMap()) { if (obj === null || typeof obj !== 'object') return obj; if (seen.has(obj)) return seen.get(obj); if (obj instanceof Date) return new Date(obj); if (obj instanceof RegExp) return new RegExp(obj); const clone = Array.isArray(obj) ? [] : {}; seen.set(obj, clone); for (const key of Reflect.ownKeys(obj)) { clone[key] = deepClone(obj[key], seen); } return clone; }</code> Handles circular refs with WeakMap, preserves Date/RegExp, uses Reflect.ownKeys for symbols.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the Temporal API (Stage 3)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Modern date/time API replacing the broken Date object. <code>Temporal.Now.instant()</code>, <code>Temporal.PlainDate.from('2025-01-15')</code>, <code>Temporal.ZonedDateTime</code>. Features: immutable, timezone-aware, calendar support, duration arithmetic, no month-indexing issues (January is 1 not 0). Still Stage 3 but polyfillable. Will eventually obsolete moment.js and date-fns for most use cases.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Implement pipe and compose functions.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const pipe = (...fns) =&gt; (x) =&gt; fns.reduce((v, fn) =&gt; fn(v), x);</code> Left-to-right composition. <code>const compose = (...fns) =&gt; (x) =&gt; fns.reduceRight((v, fn) =&gt; fn(v), x);</code> Right-to-left. Usage: <code>const transform = pipe(trim, lowercase, capitalize); transform(' HELLO ')</code>. Fundamental functional programming pattern. TC39 has a pipeline operator proposal (<code>x |&gt; fn</code>).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain the Iterator and Iterable protocols in depth.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Iterable</strong>: has <code>[Symbol.iterator]()</code> returning an iterator. <strong>Iterator</strong>: has <code>.next()</code> returning <code>{value, done}</code>. Custom: make any object iterable. Async versions use <code>[Symbol.asyncIterator]()</code> and <code>async next()</code>. Generators auto-implement both protocols. Consumed by: <code>for...of</code>, spread, destructuring, <code>Array.from()</code>, <code>Promise.all()</code>, <code>yield*</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the structured clone algorithm?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>The algorithm behind <code>structuredClone()</code>, <code>postMessage()</code>, IndexedDB, and history API. Handles: objects, arrays, Maps, Sets, Dates, RegExp, ArrayBuffers, Blobs, Files, ImageData, circular references. Does NOT handle: functions, DOM nodes, property descriptors, getters/setters, prototype chain, Error objects (in some engines). It's what makes Web Worker communication possible.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain AbortController and AbortSignal.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const controller = new AbortController(); fetch(url, {signal: controller.signal}).catch(e =&gt; { if(e.name === 'AbortError') console.log('Cancelled'); }); controller.abort();</code> Cancels fetch requests, event listeners, and any async operation that accepts a signal. Multiple operations can share one signal. Use in React cleanup: <code>useEffect(() =&gt; { const ctrl = new AbortController(); fetch(url, {signal: ctrl.signal}); return () =&gt; ctrl.abort(); }, [url])</code></p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What are SharedArrayBuffer and Atomics?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>SharedArrayBuffer</strong>: memory shared between main thread and workers (unlike regular ArrayBuffer which is transferred/copied). <strong>Atomics</strong>: provides thread-safe operations on shared memory — <code>Atomics.add()</code>, <code>Atomics.load()</code>, <code>Atomics.store()</code>, <code>Atomics.wait()/notify()</code> for synchronization. Required for high-performance parallel computing in JS. Requires cross-origin isolation headers (COOP/COEP) due to Spectre mitigations.</p></div></div></div>\n\n</div>\n\n<div class=\"tech\" id=\"t-react\">\n<h2 style=\"color:var(--react);font-family:'Literata',serif;font-size:1.5rem;margin-bottom:.3rem\">React</h2>\n<p class=\"si\">58 questions — basic to very advanced. Click any question to reveal the answer.</p>\n<div class=\"fb\">\n<button class=\"ft active\" data-f=\"all\">All (58)</button>\n<button class=\"ft\" data-f=\"b\">Basic</button>\n<button class=\"ft\" data-f=\"i\">Intermediate</button>\n<button class=\"ft\" data-f=\"a\">Advanced</button>\n</div>\n<div class=\"ct\">Showing <b>58</b> questions</div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is React?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A JavaScript library (not framework) for building user interfaces with components. Created by Meta. Uses a Virtual DOM for efficient updates. Declarative: you describe WHAT the UI should look like, React figures out HOW to update the screen.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is JSX?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Syntax extension that lets you write HTML-like code in JavaScript. Compiles to <code>React.createElement()</code>. Rules: single root element, close all tags, camelCase attributes (<code>className</code>, <code>onClick</code>, <code>htmlFor</code>), expressions in <code>{}</code>, style takes an object.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the Virtual DOM?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A lightweight JavaScript copy of the real DOM. When state changes, React creates a new virtual DOM, diffs it with the previous one (reconciliation), and updates only changed real DOM nodes. Faster than manipulating DOM directly because DOM operations are expensive.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a component?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A reusable, self-contained piece of UI. Function components are the standard: a JavaScript function that returns JSX. Receives <code>props</code> (inputs) and can manage <code>state</code> (internal data). Class components are legacy.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are props?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Data passed from parent to child component. Read-only (never modify). Flow one direction (down). Access via function parameters: <code>function Card({ title, children }) {}</code>. Default values via destructuring: <code>{ count = 0 }</code>. <code>children</code> is a special prop for nested content.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is state?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Internal, mutable data managed by a component. Created with <code>useState(initialValue)</code>. Changing state triggers a re-render. State updates are batched and async. Must be updated immutably (new objects/arrays, not mutations). Use functional updates when deriving from previous state: <code>setState(prev =&gt; prev + 1)</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between state and props?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Props</strong>: passed by parent, read-only, flow down. <strong>State</strong>: owned by component, mutable via setter, triggers re-render when changed. Props are like function arguments; state is like local variables.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What are hooks?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Functions that let you use React features in function components. Core: <code>useState</code>, <code>useEffect</code>, <code>useContext</code>, <code>useRef</code>, <code>useMemo</code>, <code>useCallback</code>, <code>useReducer</code>, <code>useId</code>. Rules: call at top level only (not in loops/conditions), call in React functions only.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is useState?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const [value, setValue] = useState(initial)</code>. Returns current value and setter function. Setter can take new value or function (for derived state). Lazy init with function: <code>useState(() =&gt; expensiveCompute())</code>. State updates are batched — multiple setStates result in one re-render.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is useEffect?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Performs side effects: API calls, subscriptions, DOM manipulation. <code>useEffect(() =&gt; { /* effect */ return () =&gt; { /* cleanup */ } }, [deps])</code>. No array: runs every render. Empty array: runs once on mount. Dependencies: runs when they change. Cleanup runs before re-run and on unmount.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the key prop in lists?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A unique identifier that helps React track which items changed, moved, or were removed during re-renders. Must be stable (not random), unique among siblings. Use item IDs, never array indexes (causes bugs with reordering/deletion). Without keys, React recreates all DOM nodes.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is conditional rendering?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Showing different UI based on conditions. Patterns: <strong>1)</strong> if/else before return. <strong>2)</strong> Ternary: <code>{x ? <a> : <b>}</b></a></code><a><b>. <strong>3)</strong> Short-circuit: <code>{x &amp;&amp; </code></b></a><b><code><a>}</a></code><a>. <strong>4)</strong> Return null for nothing. Gotcha: <code>{0 &amp;&amp; </code></a><code><a>}</a></code><a> renders '0', use <code>{x &gt; 0 &amp;&amp; </code></a><code><a>}</a></code><a>.</a></b></p></div></div></div><b><a>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a Fragment?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>&lt;&gt;...&lt;/&gt;</code> or <code>&lt;React.Fragment&gt;</code> — groups elements without adding DOM wrapper. Use when JSX requires a single root element but you don't want an extra div. Only <code>React.Fragment</code> (not shorthand) accepts a key prop.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between controlled and uncontrolled components?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Controlled</strong>: React state drives the input value. <code>&lt;input value={name} onChange={e =&gt; setName(e.target.value)}/&gt;</code>. <strong>Uncontrolled</strong>: DOM manages value, access via ref. Controlled = more control, easier validation. Uncontrolled = simpler, less code.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is event handling in React?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>camelCase events: <code>onClick</code>, <code>onChange</code>, <code>onSubmit</code>. Pass function reference, not call: <code>onClick={handleClick}</code> not <code>onClick={handleClick()}</code>. React uses SyntheticEvents (cross-browser wrapper). <code>e.preventDefault()</code> and <code>e.stopPropagation()</code> work normally.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is lifting state up?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Moving state to the nearest common parent when multiple children need the same data. Parent owns the state and passes it down as props + passes setter/handler functions for children to update it. This is React's primary data sharing pattern before Context.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is prop drilling?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Passing props through multiple intermediate components that don't use them, just to reach a deeply nested child. Problem: clutters intermediate components, hard to maintain. Solutions: Context API, composition (children pattern), state management libraries (Zustand, Redux).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the children prop?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Special prop containing whatever is placed between component opening/closing tags. <code>&lt;Card&gt;content here&lt;/Card&gt;</code> — 'content here' is <code>props.children</code>. Can be text, elements, components, even functions (render props). Type: <code>React.ReactNode</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is React.StrictMode?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Development-only wrapper that helps find problems. It: <strong>1)</strong> double-invokes render/effects to find impure renders, <strong>2)</strong> warns about deprecated APIs, <strong>3)</strong> detects unexpected side effects. Does NOT affect production. Wrap your app root: <code>&lt;StrictMode&gt;&lt;App/&gt;&lt;/StrictMode&gt;</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between class and function components?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Function components: simpler, use hooks for state/effects, no <code>this</code> confusion, easier to test. Class components: use lifecycle methods (componentDidMount etc.), <code>this.state</code>, <code>this.setState()</code>. Since React 16.8+ (hooks), function components can do everything class components can. Use function components exclusively for new code.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain the useEffect dependency array in detail.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>No array</strong>: runs after every render (usually wrong). <strong>[]</strong>: runs once on mount, cleanup on unmount. <strong>[a, b]</strong>: runs when a or b change (compared with <code>Object.is</code>). Common bugs: missing deps (stale closures), object/array deps creating new references each render (memoize them). The exhaustive-deps ESLint rule catches most issues.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">When should you NOT use useEffect?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1)</strong> Deriving state from props — compute during render or useMemo. <strong>2)</strong> Resetting state when props change — use <code>key</code> prop to remount. <strong>3)</strong> Handling user events — use event handlers directly. <strong>4)</strong> Data fetching — use Server Components, TanStack Query, or SWR. <strong>5)</strong> Updating state based on other state — compute during render. useEffect is for syncing with external systems only.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is useMemo vs useCallback?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>useMemo(() =&gt; value, [deps])</code> caches a computed VALUE. <code>useCallback(fn, [deps])</code> caches a FUNCTION reference. <code>useCallback(fn, deps)</code> ≡ <code>useMemo(() =&gt; fn, deps)</code>. Use useMemo for expensive calculations. Use useCallback for functions passed to React.memo'd children. With React 19 Compiler: rarely needed manually.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is useRef and when to use it?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1)</strong> Access DOM elements: <code>&lt;input ref={inputRef}/&gt;</code>, <code>inputRef.current.focus()</code>. <strong>2)</strong> Mutable value that doesn't trigger re-renders (unlike state): timers, previous values, render count. <strong>3)</strong> Store latest callback to avoid stale closures. Key: changing <code>.current</code> does NOT cause re-render. Available across renders.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain React Context — usage and limitations.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Share data across component tree without prop drilling. <code>createContext(default)</code> → <code>&lt;Provider value={...}&gt;</code> → <code>useContext(ctx)</code>. <strong>Limitation:</strong> ALL consumers re-render when value changes, even if they use only part of it. Mitigation: split into multiple contexts, memoize values, use external state for frequent changes (Zustand, Jotai).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What causes unnecessary re-renders?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1)</strong> Parent re-renders → all children re-render (default). <strong>2)</strong> New object/array/function references in props. <strong>3)</strong> Context value changes. Prevention: <strong>1)</strong> React.memo for pure children. <strong>2)</strong> Move state down. <strong>3)</strong> Children-as-props pattern. <strong>4)</strong> React Compiler (auto-memoization). <strong>5)</strong> Split context. <strong>6)</strong> External state for frequent updates.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is React.memo?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>HOC that skips re-render if props haven't changed (shallow comparison). <code>const Memoized = React.memo(Component)</code>. Custom comparator: <code>React.memo(Component, (prev, next) =&gt; prev.id === next.id)</code>. Only works if props are primitives or stable references. With React Compiler: less needed since it auto-memoizes.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain useReducer vs useState.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Both manage state. Use <code>useReducer</code> when: state has multiple sub-values, next state depends on previous, transitions are complex, you want to pass dispatch instead of multiple setters. Pattern: <code>const [state, dispatch] = useReducer(reducer, initial)</code> where reducer is <code>(state, action) =&gt; newState</code>. Like a mini Redux inside one component.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are custom hooks?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Functions starting with <code>use</code> that encapsulate reusable stateful logic. Can use other hooks inside. Examples: <code>useDebounce</code>, <code>useFetch</code>, <code>useLocalStorage</code>, <code>useMediaQuery</code>, <code>useOnClickOutside</code>. They don't share state between components — each component using the hook gets its own state. The #1 pattern for code reuse in React.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is React.lazy and Suspense?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>React.lazy(() =&gt; import('./Heavy'))</code> code-splits a component — loaded on demand. <code>&lt;Suspense fallback={&lt;Loading/&gt;}&gt;&lt;Heavy/&gt;&lt;/Suspense&gt;</code> shows fallback while loading. Reduces initial bundle size. In Next.js, this pairs with <code>loading.tsx</code> for route-level splitting. Multiple Suspense boundaries = granular loading states.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the children-as-props pattern?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Pass components as children or named props to avoid re-renders: <code>&lt;Layout sidebar={&lt;Sidebar/&gt;}&gt;&lt;Main/&gt;&lt;/Layout&gt;</code>. When Layout's state changes, sidebar and children DON'T re-render because they were created in the parent scope. This is a powerful optimization pattern without React.memo.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain error boundaries.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Components that catch JavaScript errors in their child tree. Still class-based: <code>static getDerivedStateFromError(error)</code> updates state, <code>componentDidCatch(error, info)</code> logs. Usage: <code>&lt;ErrorBoundary&gt;&lt;App/&gt;&lt;/ErrorBoundary&gt;</code>. Don't catch: event handlers, async code, SSR, errors in the boundary itself. Libraries like react-error-boundary provide hooks-based API.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the useId hook?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const id = useId()</code> generates unique IDs stable across server/client (no hydration mismatch). Use for: accessibility attributes (<code>htmlFor</code>/<code>id</code>), <code>aria-describedby</code>. Returns a string like <code>':r1:'</code>. Don't use for list keys or CSS selectors. Generates matching IDs on server and client.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is React Portal?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>createPortal(element, domNode)</code> renders children into a different DOM node outside the parent hierarchy. Use for: modals, tooltips, popovers, notifications — things that need to 'break out' of parent overflow/z-index. Events still bubble through the React tree (not DOM tree). The portal component still follows React's lifecycle normally.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the compound component pattern?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Components that work together sharing implicit state via Context. Example: <code>&lt;Tabs&gt;&lt;Tabs.Tab id='a'&gt;A&lt;/Tabs.Tab&gt;&lt;Tabs.Panel id='a'&gt;Content&lt;/Tabs.Panel&gt;&lt;/Tabs&gt;</code>. Tabs provides Context, Tab/Panel consume it. Creates clean, composable APIs like HTML select/option. Uses dot notation (<code>Tabs.Tab</code>) for namespacing.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain state management options in React (2026).</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Local:</strong> useState (single component). <strong>Shared:</strong> lift state up + props. <strong>Global:</strong> Context + useReducer (infrequent changes). <strong>Client state:</strong> Zustand (simple) or Jotai (atomic). <strong>Server state:</strong> TanStack Query (caching, refetching, mutations). <strong>Complex:</strong> Redux Toolkit (large enterprise). 2026 best practice: TanStack Query for server data + Zustand for UI state.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is useTransition?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Marks state updates as non-urgent, allowing urgent updates (typing, clicking) to interrupt them. <code>const [isPending, startTransition] = useTransition(); startTransition(() =&gt; setFilteredList(filter(items)))</code>. The UI stays responsive during expensive calculations. React 19: supports async functions in startTransition.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is useDeferredValue?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const deferredQuery = useDeferredValue(query)</code> defers re-rendering with a new value. The component renders with the old value first (stays responsive), then re-renders with the new value in the background. Similar to useTransition but for values you can't wrap in startTransition (e.g., from props).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">How do you handle forms in React?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1) Controlled:</strong> useState per field — full control, verbose. <strong>2) Uncontrolled + FormData:</strong> ref or onSubmit FormData — simpler. <strong>3) React Hook Form:</strong> performant, minimal re-renders. <strong>4) Server Actions (React 19):</strong> <code>&lt;form action={serverFn}&gt;</code> — progressive enhancement. <strong>5) Zod + Server Actions:</strong> type-safe server validation. Modern approach: Server Actions for mutations, React Hook Form for complex client validation.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is forwardRef and why does React 19 change it?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Wraps component to accept ref from parent: <code>const Input = forwardRef((props, ref) =&gt; &lt;input ref={ref}/&gt;)</code>. In React 19: <code>ref</code> is just a regular prop — no forwardRef needed! <code>function Input({ref, ...props}) { return &lt;input ref={ref}/&gt; }</code>. This simplifies code significantly.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the render props pattern?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Pass a function as prop that returns JSX: <code>&lt;Mouse render={({x,y}) =&gt; &lt;Cursor x={x} y={y}/&gt;}/&gt;</code>. The component controls state/logic, the render prop controls presentation. Mostly replaced by custom hooks which are cleaner, but still used in some libraries (e.g., Formik's <code>&lt;Field&gt;</code>). Asked in interviews for legacy knowledge.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain React's reconciliation algorithm.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>When state changes, React diffs the new virtual DOM with the previous. Rules: <strong>1)</strong> Different element types → destroy and rebuild entire subtree. <strong>2)</strong> Same type → update attributes, recurse children. <strong>3)</strong> Lists use keys to match children across renders. O(n) complexity vs O(n³) naive diff. This is why keys matter — without them, React can't efficiently track list item changes.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the Fiber architecture?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>React's internal reconciliation engine (since v16). Breaks rendering into small units of work (fiber nodes in a linked list tree) that can be paused, prioritized, and resumed. Enables: concurrent rendering, priority-based updates (user input &gt; data fetch &gt; offscreen), Suspense, startTransition. Key shift: rendering is no longer synchronous and blocking.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain React Server Components in depth.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Components that run ONLY on the server. JS never sent to client. Can access databases, file systems, secrets directly. Zero bundle size impact. Cannot use useState, useEffect, onClick — no interactivity. Client Components opt in with <code>'use client'</code>. Key difference from SSR: SSR renders everything to HTML then hydrates ALL components on client. RSC keeps server components on server permanently. Client Components can render Server Components as children but not vice versa.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the React Compiler?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Build-time tool that automatically memoizes components and values — equivalent of useMemo/useCallback/React.memo inserted by compiler. Analyzes code following Rules of React (pure renders, no side effects). Impact: these hooks become rare escape hatches, less code to write, fewer re-render bugs. Enforces best practices at compile time. Still opt-in but becoming the default.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain Server Actions in React 19.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Async functions marked with <code>'use server'</code> that run on server but are callable from client. For data mutations: form submissions, database writes. Work with <code>&lt;form action={...}&gt;</code> pattern. Progressive enhancement (work without JS). Integrate with <code>useActionState</code> (pending/error states), <code>useOptimistic</code> (instant UI updates). Replace API routes for most mutations.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is useActionState?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const [state, formAction, isPending] = useActionState(actionFn, initialState)</code>. Manages form action state: tracks pending state, error state, and return value. The action function receives previous state + FormData. Returns updated state. Used with Server Actions for progressive form handling with loading indicators and error display.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is useOptimistic?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const [optimisticState, addOptimistic] = useOptimistic(state, updateFn)</code>. Shows optimistic UI immediately before server confirms. If server action fails, automatically reverts. Use for: like buttons, todo toggles, adding comments — any mutation where instant feedback improves UX. The updateFn creates the temporary optimistic state.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the use() hook?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>const value = use(promiseOrContext)</code>. Reads Promises or Context in render. Unlike other hooks, CAN be called conditionally. For Promises: suspends component until resolved (works with Suspense). For Context: replaces useContext, but more flexible. Enables reading data inside conditions/loops where useContext isn't allowed.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How does React handle concurrent rendering?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>React can prepare multiple versions of UI simultaneously. <code>startTransition</code> marks updates as non-urgent — React can interrupt them for urgent updates (typing, clicking). <code>Suspense</code> shows fallbacks while async content loads. The Fiber architecture enables pausing/resuming render work. Result: UI stays responsive during expensive operations. This is opt-in — wrap updates in startTransition or use Suspense.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain hydration and hydration errors.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Hydration</strong>: React attaches event listeners and state to server-rendered HTML. The server HTML must match what React would render on the client. <strong>Hydration errors</strong>: mismatch between server and client HTML. Common causes: using browser APIs during render (window, localStorage), timestamps, random values. Fix: useEffect for client-only code, <code>suppressHydrationWarning</code> for known differences. React 19 shows exact diffs in error messages.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What are render phases in React?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Render phase</strong>: React calls component functions, creates virtual DOM, diffs with previous. No DOM mutations yet. Can be paused/interrupted (concurrent mode). Must be pure — no side effects. <strong>Commit phase</strong>: React applies DOM changes, runs useLayoutEffect, schedules useEffect. Synchronous, cannot be interrupted. Understanding this separation is key to avoiding bugs with side effects during render.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How would you architect a large-scale React application?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Structure:</strong> Feature-based folders, shared ui/ components, typed API layer. <strong>State:</strong> TanStack Query (server) + Zustand (client). <strong>Routing:</strong> Next.js App Router. <strong>Data:</strong> Server Components for reads, Server Actions for writes. <strong>Styling:</strong> Tailwind CSS + CSS variables. <strong>Testing:</strong> Vitest + Testing Library for units, Playwright for E2E. <strong>Performance:</strong> React Compiler, code splitting, image optimization, PPR. <strong>Monitoring:</strong> Error boundaries + Sentry.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the difference between useEffect, useLayoutEffect, and useInsertionEffect?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>useEffect</code>: runs asynchronously AFTER paint (browser has rendered). Use for: data fetching, subscriptions. <code>useLayoutEffect</code>: runs synchronously AFTER DOM mutations but BEFORE paint. Use for: DOM measurements, preventing visual flicker. <code>useInsertionEffect</code>: runs before DOM mutations. Use for: CSS-in-JS libraries injecting style tags. Priority: useInsertionEffect → DOM mutation → useLayoutEffect → paint → useEffect.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What are React 19's document metadata features?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Components can render <code>&lt;title&gt;</code>, <code>&lt;meta&gt;</code>, <code>&lt;link&gt;</code> directly — they're hoisted to <code>&lt;head&gt;</code> automatically. No need for react-helmet or similar libraries. Works with SSR. Example: <code>function BlogPost({post}) { return &lt;&gt;&lt;title&gt;{post.title}&lt;/title&gt;&lt;article&gt;...&lt;/article&gt;&lt;/&gt; }</code>. Style precedence is also managed automatically.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How do you test React components effectively?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Unit:</strong> Vitest/Jest + Testing Library. Test behavior, not implementation: <code>render(&lt;Counter/&gt;); fireEvent.click(getByText('+1')); expect(getByText('Count: 1')).toBeInTheDocument()</code>. <strong>Integration:</strong> Test component interactions, mocked API calls. <strong>E2E:</strong> Playwright/Cypress for critical user flows. Avoid: testing implementation details (state values, method calls). Test what the user sees and does.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is streaming SSR and selective hydration?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Streaming</strong>: Server sends HTML in chunks as components render (using Suspense boundaries), instead of waiting for the entire page. User sees content progressively. <strong>Selective hydration</strong>: React hydrates components in priority order — components the user interacts with get hydrated first. Combined: fast initial content + responsive interactions without waiting for full hydration.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain React's batching behavior.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>React 18+: ALL state updates are batched by default (not just event handlers). Multiple setState calls in the same synchronous scope result in ONE re-render. This works in setTimeout, Promises, native event handlers too (React 17 only batched in React event handlers). To force immediate re-render: <code>flushSync(() =&gt; setState(...))</code> — but rarely needed.</p></div></div></div>\n\n</a></b></div><b><a>\n\n<div class=\"tech\" id=\"t-nextjs\">\n<h2 style=\"color:var(--w);font-family:'Literata',serif;font-size:1.5rem;margin-bottom:.3rem\">Next.js</h2>\n<p class=\"si\">53 questions — basic to very advanced. Click any question to reveal the answer.</p>\n<div class=\"fb\">\n<button class=\"ft active\" data-f=\"all\">All (53)</button>\n<button class=\"ft\" data-f=\"b\">Basic</button>\n<button class=\"ft\" data-f=\"i\">Intermediate</button>\n<button class=\"ft\" data-f=\"a\">Advanced</button>\n</div>\n<div class=\"ct\">Showing <b>53</b> questions</div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>A React framework by Vercel that adds: file-based routing, SSR, SSG, ISR, Server Actions, API routes, middleware, image/font optimization, and built-in Tailwind/CSS support. It's the recommended way to build production React apps in 2026. Current version: 16.2.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the App Router?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Next.js routing system using the <code>app/</code> directory. File conventions: <code>page.tsx</code> = route UI, <code>layout.tsx</code> = shared wrapper, <code>loading.tsx</code> = Suspense fallback, <code>error.tsx</code> = error boundary, <code>not-found.tsx</code> = 404. Routes are defined by folder structure: <code>app/about/page.tsx</code> → <code>/about</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the Pages Router?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Legacy routing using <code>pages/</code> directory. All components are Client Components. Data fetching via <code>getServerSideProps</code>/<code>getStaticProps</code>. No Server Components. Still supported but App Router is recommended for new projects. Migration guide available.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a layout in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Shared UI that wraps child pages. Defined in <code>layout.tsx</code>. Key feature: layouts DON'T re-render on navigation — state persists (sidebar stays open). Root layout is required (wraps entire app with html/body). Nested layouts are composable: <code>app/dashboard/layout.tsx</code> wraps all dashboard pages.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between Server and Client Components?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Server (default)</strong>: runs on server, zero JS to client, can access DB directly, can't use useState/useEffect/onClick. <strong>Client</strong>: marked with <code>'use client'</code>, includes interactivity, JS sent to browser. Default to Server; opt into Client only for interactivity.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a dynamic route?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Route with variable segments: <code>app/blog/[slug]/page.tsx</code> → <code>/blog/any-slug</code>. Access params: <code>const { slug } = await params</code>. Catch-all: <code>[...slug]</code> matches <code>/a/b/c</code>. Optional catch-all: <code>[[...slug]]</code> also matches <code>/</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the loading.tsx file?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Automatically creates a Suspense boundary around the page. Shows while the page is loading (Server Component fetching data). No manual Suspense needed. Renders instantly from cache on navigation while the new content loads. Creates a smooth loading experience.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the error.tsx file?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Automatically creates a React error boundary. Catches errors in the page and child components. Receives <code>error</code> and <code>reset</code> props. Must be a Client Component (<code>'use client'</code>). Isolates errors — rest of the app continues working. <code>global-error.tsx</code> catches root layout errors.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the not-found.tsx file?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Shown when <code>notFound()</code> is called or a route doesn't exist. Customize per route segment. The root <code>app/not-found.tsx</code> handles all unmatched URLs (404 page).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the Link component?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>import Link from 'next/link'</code>. Client-side navigation without full page reload. Prefetches linked pages in the background (viewport/hover). Replaces <code>&lt;a&gt;</code> tags for internal navigation. Supports <code>href</code>, <code>replace</code> (no history entry), <code>scroll</code> (control scroll position), <code>prefetch</code> (true/false).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the Image component?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>import Image from 'next/image'</code>. Auto-optimizes: converts to WebP/AVIF, lazy loads, prevents layout shift (CLS), responsive sizing. Props: <code>src</code>, <code>width</code>, <code>height</code>, <code>alt</code> (required), <code>priority</code> (above fold), <code>placeholder='blur'</code>, <code>sizes</code> (responsive). Much better than plain <code>&lt;img&gt;</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the Metadata API?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>SEO metadata in App Router. Static: <code>export const metadata = { title: '...', description: '...' }</code>. Dynamic: <code>export async function generateMetadata({ params }) { ... }</code>. Supports: title template, OpenGraph, Twitter cards, robots, sitemap, icons. Replaces react-helmet entirely.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a route handler (API route)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>app/api/users/route.ts</code> — server-side endpoint. Export named functions: <code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code>. Receives <code>Request</code> object, returns <code>Response</code>. Runs on server only. Use for: external API consumption, webhooks, third-party integrations.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is a Server Action?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Async function marked with <code>'use server'</code> that runs on the server. Used for data mutations (form submissions, DB writes). Can be called from forms: <code>&lt;form action={createUser}&gt;</code>. No API route needed. Supports progressive enhancement (works without JS). The modern way to handle mutations in Next.js.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the difference between template.tsx and layout.tsx?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>layout.tsx</code>: persists across navigation, state maintained, component instance reused. <code>template.tsx</code>: re-mounts on every navigation, fresh state. Use layout for most cases. Use template when you need: route transition animations, useEffect on every navigation, or reset state per page.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is next/font?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Automatic font optimization. <code>import { Inter } from 'next/font/google'</code>. Self-hosts fonts (no external requests), eliminates layout shift with <code>size-adjust</code>, supports variable fonts. Generates CSS variables for use in Tailwind. Zero CLS font loading out of the box.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">How do you add global CSS in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Import in root layout: <code>import './globals.css'</code>. Supports: global CSS, CSS Modules (<code>*.module.css</code>), Tailwind CSS (built-in), Sass (<code>npm install sass</code>). CSS Modules are scoped to component. Tailwind is the most popular choice in 2026.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the next.config.js file?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Configuration file at project root. Controls: redirects, rewrites, headers, image domains, environment variables, experimental features, Turbopack settings, output mode (standalone, export). Use <code>next.config.ts</code> for TypeScript support (Next.js 15+).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is static export in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>output: 'export'</code> in next.config — generates pure static HTML/CSS/JS (no server needed). Deployed to any static host (GitHub Pages, S3, Netlify). Limitations: no SSR, no API routes, no middleware, no ISR. Good for: documentation sites, landing pages, portfolios.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qh\"><span class=\"lv b\">B</span><span class=\"qt\">What is the useRouter hook?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>import { useRouter } from 'next/navigation'</code> (App Router). Methods: <code>router.push('/path')</code> (navigate), <code>router.replace('/path')</code> (no history), <code>router.back()</code>, <code>router.forward()</code>, <code>router.refresh()</code> (refetch server data). Must be in a Client Component.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain SSG vs SSR vs ISR vs CSR.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>SSG</strong>: HTML at build time, served from CDN. Fastest. For static content. <strong>SSR</strong>: HTML per request. For dynamic/personalized content. <strong>ISR</strong>: Static + revalidation — pages regenerate in background after a time interval. Best of both. <strong>CSR</strong>: Client-side only, via 'use client' + useEffect. Use CSR only when Server Components won't work (heavy client interaction, third-party widgets).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is Partial Prerendering (PPR)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Next.js 16's most powerful feature. Renders static parts at build time (served from CDN instantly) while dynamic parts stream via Suspense. Example: product page where header/grid are static (instant) but cart/recommendations are dynamic (streamed). Eliminates the old static-vs-dynamic tradeoff. Uses Suspense boundaries to define the split.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is middleware in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Code that runs BEFORE request completes. <code>middleware.ts</code> at project root. Runs on Edge Runtime. Can: redirect, rewrite URLs, set headers, check auth, handle geolocation, A/B testing. Use <code>config.matcher</code> to specify routes. Cannot: access databases directly (Edge limitations). Use for request-level logic, not heavy computation.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">Explain data fetching in Server Components.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Server Components can fetch data directly — no useEffect, no client-side loading states. <code>async function Page() { const data = await db.query(...); return &lt;List data={data}/&gt; }</code>. Parallel: <code>const [a,b] = await Promise.all([...]);</code>. Sequential: when one fetch depends on another. Data fetching happens on the server, reducing client bundle and waterfall requests.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is revalidation? (revalidatePath vs revalidateTag)</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>revalidatePath('/blog')</code>: clears cache for a specific path. <code>revalidateTag('posts')</code>: clears all entries tagged with 'posts'. Use in Server Actions after mutations. Time-based: <code>fetch(url, { next: { revalidate: 3600 } })</code> — stale after 1 hour. On-demand is more precise; time-based is simpler. Both trigger background regeneration.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are route groups?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Folders wrapped in parentheses: <code>(marketing)</code>, <code>(shop)</code>. Don't affect URL. Use to: organize routes logically, apply different layouts to different sections without changing URLs. <code>app/(marketing)/about/page.tsx</code> → <code>/about</code> (not <code>/marketing/about</code>). Useful for separating auth layout from main layout.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are parallel routes?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Named slots with <code>@folder</code> convention. Layout receives them as props: <code>function Layout({children, modal, sidebar})</code>. Each slot renders independently. Use for: modals that work with URL, dashboards with independent sections, split-view UIs. Requires <code>default.tsx</code> for unmatched slots.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What are intercepting routes?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Show a route as a modal/overlay without full navigation. Convention: <code>(..)photo/[id]</code> intercepts <code>/photo/:id</code> one level up. On click: shows photo in modal. On direct URL/refresh: shows full page. Used for: photo galleries (Instagram-style), preview modals, quick views. Combines with parallel routes for the modal pattern.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">How does authentication work in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Three layers: <strong>1) Middleware</strong>: check session cookie, redirect unauthenticated users (fast, Edge). <strong>2) Server Components</strong>: <code>const user = await getUser()</code>, redirect if not logged in. <strong>3) Client</strong>: Context/Zustand for client auth state. Libraries: Auth.js (NextAuth), Clerk, Lucia. Server-first approach keeps sensitive logic off client.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the 'use cache' directive?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Next.js 16: ALL code is dynamic by default. Cache explicitly with <code>'use cache'</code> on pages, components, or functions. Compiler auto-generates cache keys. Control lifetime: <code>cacheLife('hours')</code>. Replaces old implicit caching (Next.js 15 cached fetch by default — confusing). Much more predictable.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is generateStaticParams?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Pre-generates static pages for dynamic routes at build time. <code>export async function generateStaticParams() { const posts = await getPosts(); return posts.map(p =&gt; ({ slug: p.slug })); }</code>. Combined with the page component, this creates static HTML for each slug. The equivalent of getStaticPaths from Pages Router.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is streaming in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Sends HTML in chunks as components render, instead of waiting for everything. Enabled automatically with <code>loading.tsx</code> and Suspense boundaries. User sees content progressively. Non-blocking: fast components render immediately, slow ones stream in later. Works with SSR and RSC. Dramatically improves Time to First Byte and First Contentful Paint.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the redirect() function?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Server-side redirect: <code>import { redirect } from 'next/navigation'; redirect('/login')</code>. Works in Server Components and Server Actions. Throws internally (must not be in try/catch). For conditional redirects: check auth, then redirect. Client-side: use <code>useRouter().push()</code>. For permanent redirects: <code>permanentRedirect()</code> (308 status).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">How does error handling work in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>error.tsx</code>: catches rendering errors per route segment, shows fallback UI with reset button. <code>global-error.tsx</code>: catches root layout errors (replaces entire HTML). <code>not-found.tsx</code>: handles notFound() calls. Server Actions: return error objects, display in UI via useActionState. API routes: return Response with error status. Error boundaries recover gracefully without full page crash.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the Adapter API (Next.js 16)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Stable interface allowing any hosting provider to deploy Next.js fully. Previously, advanced features (ISR, middleware) worked best on Vercel. Now Cloudflare, AWS, Netlify can implement the adapter to support all features. Addresses 'vendor lock-in' criticism. OpenNext is a community adapter for AWS. Makes Next.js truly platform-agnostic.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is Turbopack?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Rust-based bundler replacing webpack. Default in Next.js 16. 5-10x faster Fast Refresh during dev, 2-5x faster production builds. File system caching: compile artifacts stored on disk for faster restarts. Server Fast Refresh (16.2): fine-grained server-side hot reloading. 200+ bug fixes in 16.2. The performance difference is immediately noticeable.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the Next.js MCP server?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Model Context Protocol integration for AI-assisted development. Next.js DevTools MCP exposes tools for AI agents: <code>get_routes</code> (list all routes), debugging info, diagnostics. 16.2 adds AGENTS.md in create-next-app, browser log forwarding to terminal for AI debugging. Treats AI agents as first-class users of the framework.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">How do you handle environment variables in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><code>.env.local</code> for local dev, <code>.env.production</code> for prod. Server-only by default. Prefix with <code>NEXT_PUBLIC_</code> to expose to client (embedded in JS bundle). Access: <code>process.env.DATABASE_URL</code> (server), <code>process.env.NEXT_PUBLIC_API_URL</code> (client). Never put secrets in NEXT_PUBLIC_ variables.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is the instrumentation.ts file?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Runs once when server starts. Use for: initializing monitoring (OpenTelemetry, Sentry), setting up database connections, registering custom metrics. <code>export async function register() { /* setup code */ }</code>. Runs in both Node.js and Edge runtimes. The right place for one-time server initialization.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qh\"><span class=\"lv i\">I</span><span class=\"qt\">What is sitemap.ts and robots.ts?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Generate SEO files dynamically. <code>app/sitemap.ts</code>: <code>export default function sitemap(): MetadataRoute.Sitemap { return [{url:'...', lastModified:new Date()}] }</code>. <code>app/robots.ts</code>: <code>export default function robots(): MetadataRoute.Robots { return {rules:{allow:'/'}} }</code>. Auto-generated at build/request time.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain the rendering lifecycle of a Next.js request.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1)</strong> Request hits server. <strong>2)</strong> Middleware runs (if matched). <strong>3)</strong> Route is matched to page. <strong>4)</strong> Layout hierarchy is resolved. <strong>5)</strong> Server Components render (can fetch data, access DB). <strong>6)</strong> RSC payload is generated. <strong>7)</strong> HTML is streamed to client (with Suspense boundaries). <strong>8)</strong> Client receives HTML, shows content immediately. <strong>9)</strong> React hydrates Client Components. <strong>10)</strong> Page is fully interactive.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How does Next.js caching work internally? (all 4 layers)</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1) Request Memoization</strong>: deduplicates identical fetch calls in same render pass. <strong>2) Data Cache</strong>: persists fetch results across requests/deploys ('use cache' or revalidate options). <strong>3) Full Route Cache</strong>: caches rendered HTML and RSC payload for static routes. <strong>4) Router Cache</strong>: client-side cache of visited routes for instant back/forward. Each layer has its own invalidation mechanism. Understanding these layers is key to debugging cache issues.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is incremental adoption from Pages to App Router?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Both routers can coexist. Strategy: <strong>1)</strong> Start with new pages in App Router. <strong>2)</strong> Migrate shared layouts first. <strong>3)</strong> Move pages one at a time. <strong>4)</strong> Replace getServerSideProps with Server Components. <strong>5)</strong> Replace getStaticProps with generateStaticParams + 'use cache'. <strong>6)</strong> Replace API routes with Server Actions where possible. <strong>7)</strong> Remove Pages Router when done. The <code>next/link</code> and <code>next/image</code> work in both.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How do you optimize Core Web Vitals in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>LCP:</strong> Use <code>priority</code> on hero images, SSR/SSG for fast HTML, font optimization. <strong>FID/INP:</strong> Minimize client JS, use Server Components, code-split with lazy. <strong>CLS:</strong> next/image prevents shift, next/font eliminates FOIT, proper Suspense fallbacks with matching dimensions. <strong>Tools:</strong> Lighthouse, web-vitals library, Vercel Speed Insights. Next.js defaults handle most optimizations automatically.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain Server Component serialization and the RSC payload.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>When a Server Component renders, its output is serialized into an RSC payload — a special JSON-like format that describes the component tree. This payload is streamed to the client where React deserializes it and renders the actual DOM. The payload includes: rendered HTML strings, references to Client Component bundles, and serialized props passed from Server to Client Components. Props must be serializable (no functions, classes, or DOM nodes).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the CVE-2025-55182 (React2Shell)?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Critical vulnerability (CVSS 10.0) in React Server Component deserialization allowing unauthenticated remote code execution. Discovered Nov 2025, fixed Dec 3. Next.js 15.x and 16.x affected. Exploitation observed within hours of disclosure by state-nexus threat groups. Lesson: SSR introduces server-side attack surfaces — keep dependencies updated, pin versions, monitor advisories.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How does next/image optimization work under the hood?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Images are optimized on-demand (first request) and cached. Pipeline: <strong>1)</strong> Request with width/quality params. <strong>2)</strong> Server resizes/compresses image. <strong>3)</strong> Converts to modern format (WebP/AVIF based on Accept header). <strong>4)</strong> Caches result with TTL. <strong>5)</strong> Serves cached version for subsequent requests. Supports: remote images (configure domains), blur placeholder (base64 data URL), responsive srcset generation. Eliminates manual image optimization workflows.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">Explain the proxy.ts feature in Next.js 16.</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Replaces middleware for network proxy use cases. Runs before the request reaches your application code. Clarifies the network boundary — middleware was overloaded with both request modification and proxying concerns. <code>proxy.ts</code> handles URL rewriting, request forwarding, and header manipulation at the network level. Middleware still exists for Edge-specific logic (auth checks, A/B testing).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How do you implement real-time features in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1) Server-Sent Events:</strong> Stream from route handler with <code>ReadableStream</code>. <strong>2) WebSockets:</strong> Separate WS server (Socket.io, ws) — Next.js doesn't handle WebSocket upgrades. <strong>3) Polling:</strong> Client-side with TanStack Query's refetchInterval. <strong>4) Third-party:</strong> Pusher, Ably, Supabase Realtime. For most apps, polling with TanStack Query or SSE is sufficient. WebSockets add deployment complexity.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the difference between Edge and Node.js runtime?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Edge Runtime:</strong> lightweight, globally distributed, cold starts &lt;1ms, limited APIs (no fs, no native modules, some Node.js APIs missing). Use for: middleware, simple API routes, auth checks. <strong>Node.js Runtime (default):</strong> full Node.js APIs, database drivers, any npm package. Use for: data-heavy operations, database queries, file processing. Set per route: <code>export const runtime = 'edge'</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How do you handle internationalization (i18n) in Next.js?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>1) next-intl/next-i18next:</strong> Popular libraries with App Router support. <strong>2) Route-based:</strong> <code>app/[locale]/page.tsx</code> with middleware for locale detection. <strong>3) Middleware:</strong> detect Accept-Language header, redirect to appropriate locale. <strong>4) Server Components:</strong> pass locale to components, fetch translations on server. <strong>5) Metadata:</strong> use generateMetadata with locale param for localized SEO. Key: keep translations on server to reduce client bundle.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">What is the stable_action_id and how do Server Actions work internally?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p>Each Server Action gets a unique ID at build time. When the client submits a form, it sends a POST request to the current URL with the action ID in the body. The server matches the ID to the corresponding function and executes it. This is why Server Actions work without API routes — they're embedded in the page's server-side handler. The action ID ensures the right function runs even with multiple actions on one page.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qh\"><span class=\"lv a\">A</span><span class=\"qt\">How do you deploy Next.js to non-Vercel platforms?</span><span class=\"tg\">+</span></div><div class=\"qb\"><div class=\"qi\"><p><strong>Docker:</strong> <code>output: 'standalone'</code> in config, creates self-contained build. <strong>AWS:</strong> OpenNext adapter, deploy to Lambda/CloudFront. <strong>Cloudflare:</strong> @cloudflare/next-on-pages adapter. <strong>Netlify:</strong> Built-in Next.js adapter. <strong>Static:</strong> <code>output: 'export'</code> for any static host. The Adapter API (Next.js 16) standardizes this — any provider can implement the interface for full feature support.</p></div></div></div>\n\n</div>\n\n<div class=\"tech\" id=\"t-angular\">\n<h2 style=\"color:var(--ng);font-family:'Literata',serif;font-size:1.5rem;margin-bottom:.3rem\">Angular</h2>\n<p class=\"si\">88 questions — basic to very advanced. Click any question to reveal the answer.</p>\n<div class=\"fb\">\n<button class=\"ft active\" data-f=\"all\">All (88)</button>\n<button class=\"ft\" data-f=\"b\">Basic</button>\n<button class=\"ft\" data-f=\"i\">Intermediate</button>\n<button class=\"ft\" data-f=\"a\">Advanced</button>\n</div>\n<div class=\"ct\">Showing <b>88</b> questions</div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is Angular? How is it different from AngularJS?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Angular (2+) is a complete TypeScript-based rewrite. AngularJS used JavaScript, controllers, and $scope. Angular uses components, TypeScript, hierarchical DI, and is vastly faster due to AOT compilation. They share nothing except the name.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is a component?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A reusable UI building block combining a TypeScript class (logic), an HTML template (view), and CSS (styles), linked by the <code>@Component()</code> decorator.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What are the four types of data binding?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Interpolation</strong> <code>{{ }}</code>: display data. <strong>Property binding</strong> <code>[ ]</code>: set element properties. <strong>Event binding</strong> <code>( )</code>: react to actions. <strong>Two-way binding</strong> <code>[( )]</code>: read and write simultaneously.</p><div class=\"analogy\">[square brackets] = data goes IN. (parentheses) = data comes OUT. [(banana in a box)] = both directions.</div></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is the difference between ngOnInit and the constructor?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>The <strong>constructor</strong> is a TypeScript/JS feature. Use it only for DI. <strong>ngOnInit</strong> is an Angular lifecycle hook called after inputs are set. Use it for API calls, subscriptions, and setup logic.</p><div class=\"analogy\">Constructor = you're born (only get your name). ngOnInit = first day of school (now you can start learning).</div></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What are directives? Name the three types.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Component directives</strong> — components are directives with templates. <strong>Structural directives</strong> — add/remove DOM elements (<code>*ngIf</code>, <code>*ngFor</code>). <strong>Attribute directives</strong> — change appearance/behavior (<code>ngClass</code>, <code>ngStyle</code>).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What are pipes? Give examples.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Pipes transform displayed data in templates without changing the underlying data. Built-in: <code>date</code>, <code>currency</code>, <code>uppercase</code>, <code>lowercase</code>, <code>percent</code>, <code>json</code>, <code>async</code>, <code>slice</code>, <code>keyvalue</code>, <code>titlecase</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is a service in Angular?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A class decorated with <code>@Injectable()</code> that encapsulates reusable logic — API calls, auth, caching, logging. Typically singleton when provided in root. Services keep components lean.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is dependency injection?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A design pattern where classes receive their dependencies from the outside instead of creating them. Angular's DI system automatically creates, manages, and provides service instances.</p><div class=\"analogy\">Without DI: you cook every meal yourself. With DI: you call room service and it brings exactly what you need.</div></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is the Angular CLI?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A command-line tool (<code>ng</code>) that scaffolds projects, generates components/services, serves dev builds, runs tests, and builds for production. It enforces best practices and consistent structure.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is the purpose of NgModule?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>NgModule was the original way to group related components, directives, and pipes. It declares what belongs together, what to import, and what to export. Since Angular 14, standalone components replace the need for modules in most cases.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is interpolation?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Double curly braces <code>{{ expression }}</code> that evaluate a TypeScript expression and render the result as text in the DOM. Supports property access, ternary operators, pipe calls, and method calls (though methods should be avoided for performance).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is *ngIf and how does @if differ?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>*ngIf</code> is a structural directive that conditionally adds/removes an element from the DOM. <code>@if</code> (Angular 17+) is the new built-in control flow syntax — same function, but it's built into the template compiler (better performance, type narrowing, no extra import needed).</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is a template reference variable?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Declared with <code>#</code> syntax: <code>&lt;input #nameInput&gt;</code>. It gives you a reference to the DOM element or component instance, usable within the template: <code>(click)=\"greet(nameInput.value)\"</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is the async pipe?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>| async</code> subscribes to an Observable or Promise in the template and automatically unsubscribes when the component is destroyed. It also triggers change detection on new values. It's the preferred way to handle async data.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is a standalone component?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A component with <code>standalone: true</code> that declares its own imports directly in the decorator, without needing an NgModule. This is the default and recommended approach since Angular 14.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is the difference between [class] and [ngClass]?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>[class.active]=\"isActive\"</code> toggles a single class. <code>[ngClass]</code> accepts strings, arrays, or objects to manage multiple classes. For one class, use <code>[class.x]</code>. For complex logic, use <code>[ngClass]</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">Explain change detection in Angular.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Zone.js monitors all async operations. When any complete, Angular runs change detection from the root component downward, comparing binding values. If different, it updates the DOM. OnPush strategy limits checks to input reference changes, internal events, async pipe emissions, or manual triggers.</p><div class=\"analogy\">Default = a paranoid guard checking every room every 5 minutes. OnPush = a smart alarm that only alerts when a specific door opens.</div></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is OnPush change detection? When should you use it?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>OnPush tells Angular to skip checking this component unless: an @Input reference changes, an event fires inside it, an Observable emits via async pipe, a signal changes, or markForCheck() is called. Use it on <strong>every component</strong> — it's the single biggest performance optimisation.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">Template-driven vs reactive forms — when use which?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Template-driven</strong>: Logic in HTML via ngModel. Good for simple forms (login, search). <strong>Reactive</strong>: Logic in TypeScript via FormGroup/FormControl. Good for complex forms (multi-step, dynamic fields, custom validation). Reactive forms are easier to test because logic is in TypeScript.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">Explain switchMap vs mergeMap vs concatMap vs exhaustMap.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>switchMap</strong>: cancels previous, uses latest. (Search typeahead) <strong>mergeMap</strong>: runs all in parallel. (File uploads) <strong>concatMap</strong>: sequential queue. (Ordered saves) <strong>exhaustMap</strong>: ignores new until current finishes. (Login button — prevents double-submit)</p><div class=\"analogy\">switchMap = \"Cancel my burger, I want pizza.\" mergeMap = \"Cook burger AND pizza simultaneously.\" concatMap = \"Finish the burger, then start pizza.\" exhaustMap = \"I'm still eating, ignore new orders.\"</div></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What are route guards? List all types.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>canActivate</strong> — can user enter this route? <strong>canDeactivate</strong> — can user leave? (unsaved changes) <strong>canActivateChild</strong> — protect all children. <strong>canMatch</strong> — should this route definition even be considered? <strong>resolve</strong> — pre-fetch data before activation.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is lazy loading and why does it matter?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Lazy loading splits your app into chunks loaded on demand. Instead of downloading the entire app upfront, users only get the code for the current page. Implemented with <code>loadComponent</code> or <code>loadChildren</code>. Dramatically reduces initial load time.</p><div class=\"analogy\">Like Netflix: you stream the episode you're watching, not the entire catalog.</div></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is content projection (ng-content)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Allows parent components to inject HTML into child component slots. Single-slot uses <code>&lt;ng-content /&gt;</code>. Multi-slot uses <code>select</code> attribute to target named slots. Essential for reusable wrappers like cards, modals, and tabs.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What are interceptors? Give real examples.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>HTTP middleware that intercepts every request/response. Real uses: attach JWT tokens, global error handling (catch 401 → redirect to login), loading spinners, response caching, request retry logic, logging.</p><div class=\"analogy\">Like airport security: every passenger (request) passes through, gets checked, and potentially modified — without individual gates knowing.</div></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is ViewEncapsulation?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Emulated</strong> (default): Scopes CSS via attribute selectors — styles don't leak out, but global styles can leak in. <strong>ShadowDom</strong>: Native Shadow DOM — complete isolation. <strong>None</strong>: No encapsulation — styles become global.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">Explain the difference between Subject, BehaviorSubject, and ReplaySubject.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Subject</strong>: No initial value. Late subscribers miss past emissions. <strong>BehaviorSubject</strong>: Has current value. New subscribers get the latest immediately. <strong>ReplaySubject</strong>: Buffers N past values. New subscribers get the replay. <strong>AsyncSubject</strong>: Only emits the last value on completion.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is trackBy (track) and why does it matter?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>When Angular re-renders a list, it needs to know which items changed. Without tracking, it destroys and recreates all DOM elements. With <code>track item.id</code>, Angular reuses existing elements and only updates changed ones. Huge performance win for lists.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">How does Angular handle security (XSS, CSRF)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>XSS</strong>: Angular sanitizes all template bindings by default, stripping dangerous content from innerHTML, style, and URL bindings. <strong>CSRF</strong>: HttpClient auto-reads XSRF-TOKEN cookie and sends it as X-XSRF-TOKEN header.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is ng-container and when do you use it?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>ng-container</code> is a grouping element that doesn't get rendered to the DOM. Use it when you need a structural directive but don't want an extra wrapper element. Common uses: wrapping multiple elements with *ngIf, combining *ngIf and *ngFor on the same content.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the purpose of environment files?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Environment files store configuration that varies between builds: API URLs, feature flags, analytics keys. Angular's build system swaps them at compile time. Use <code>fileReplacements</code> in <code>angular.json</code> to map <code>environment.ts</code> to <code>environment.prod.ts</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">Pure vs impure pipes — what's the difference?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Pure</strong> (default): Only re-runs when the input reference changes. Efficient, works with immutable data. <strong>Impure</strong>: Re-runs on every change detection cycle. Needed to detect mutations inside arrays/objects, but very expensive. Prefer pure pipes + immutable updates.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">How do you share data between unrelated components?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>1. Shared service</strong> with BehaviorSubject or signal — components inject the same service and read/write to the shared state. <strong>2. NgRx/SignalStore</strong> for complex state. <strong>3. Route params/query params</strong> for URL-driven state. The service approach covers 90% of cases.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What are resolvers?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Resolvers pre-fetch data before a route activates. The component receives the data immediately, avoiding loading states. Implemented as functions returning Observables, Promises, or plain values.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is AOT vs JIT compilation?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>AOT (Ahead-of-Time)</strong>: Templates compiled during build. Smaller bundles, faster rendering, catches template errors at build time. Used in production. <strong>JIT (Just-in-Time)</strong>: Templates compiled in the browser at runtime. Larger bundles, slower startup. Only used during development (and rarely now).</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the difference between @ViewChild and @ContentChild?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>@ViewChild</code> queries elements in the component's own template. <code>@ContentChild</code> queries elements projected via <code>ng-content</code> from the parent. ViewChild is available in <code>ngAfterViewInit</code>; ContentChild in <code>ngAfterContentInit</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">How do you create a custom structural directive?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Inject <code>TemplateRef</code> and <code>ViewContainerRef</code>. Based on your condition, call <code>viewContainer.createEmbeddedView(templateRef)</code> to show or <code>viewContainer.clear()</code> to hide. Use an <code>@Input</code> setter to react to condition changes.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is pathMatch: 'full' vs 'prefix'?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>prefix</code> (default): Matches if the URL <strong>starts with</strong> the path. <code>full</code>: Matches only if the <strong>entire</strong> URL matches. Critical for redirects: <code>{ path: '', redirectTo: '/home', pathMatch: 'full' }</code> — without 'full', every URL would match the empty string prefix.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the host property in @Component?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>The <code>host</code> property lets you bind classes, attributes, styles, and events directly to the component's host element (the custom HTML tag itself), without needing a wrapper <code>&lt;div&gt;</code> in the template.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is forRoot() vs forChild() pattern?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A convention for configuring NgModules. <code>forRoot()</code> provides services as singletons in the root module. <code>forChild()</code> provides only declarations, no singleton services. Prevents duplicate service instances in lazy-loaded modules. With standalone, <code>provideRouter()</code> and <code>provideHttpClient()</code> replace this pattern.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain Angular's hierarchical DI system in depth.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Two parallel trees: <strong>Environment injector tree</strong> (Root → Platform → lazy route injectors) and <strong>Element injector tree</strong> (mirrors component tree). Resolution walks up the element tree first, then the environment tree. Modifiers: <code>@Self</code>, <code>@SkipSelf</code>, <code>@Optional</code>, <code>@Host</code> control where to look. Providers can use <code>useClass</code>, <code>useValue</code>, <code>useFactory</code>, <code>useExisting</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What are Angular Signals and how do they change the framework?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Signals are synchronous reactive primitives: <code>signal()</code> (writable), <code>computed()</code> (derived), <code>effect()</code> (side effects). Unlike RxJS, they always have a current value and don't need subscriptions. They enable fine-grained reactivity where only components reading a changed signal update. Long-term, they'll replace Zone.js entirely.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain Zone.js and what happens without it.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Zone.js monkey-patches all async APIs (setTimeout, Promise, addEventListener, XHR, fetch). It creates execution contexts that track async tasks, allowing Angular to know when to run change detection. Without it (zoneless mode), you rely on signals and explicit notifications. Benefits: ~15KB smaller bundle, cleaner stack traces, no monkey-patching quirks.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How does SSR and hydration work in Angular?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Angular Universal renders on the server → sends HTML to browser → fast FCP + SEO. <strong>Hydration</strong> (v16+): Instead of destroying server DOM and rebuilding, Angular \"attaches\" to existing nodes. <strong>Incremental hydration</strong> (v19+): Combines @defer with SSR — server renders HTML, but JS for deferred blocks loads only when triggered.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How would you optimize a large Angular application?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>1.</strong> OnPush everywhere. <strong>2.</strong> Lazy load routes. <strong>3.</strong> @defer for heavy components. <strong>4.</strong> track by ID in @for. <strong>5.</strong> Virtual scrolling for long lists. <strong>6.</strong> Preload lazy chunks. <strong>7.</strong> Signals over BehaviorSubject. <strong>8.</strong> SSR with hydration. <strong>9.</strong> Bundle analysis. <strong>10.</strong> Web Workers for heavy computation. <strong>11.</strong> Avoid function calls in templates. <strong>12.</strong> Use CDK's scrolling and drag-drop modules.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is the inject() function vs constructor injection?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>inject()</code> works in field initializers, constructors, and factory functions. Advantages: enables composable utility functions, works in functional guards/interceptors, cleaner syntax. Limitation: can only be called during construction (injection context), not in methods called later.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is the difference between markForCheck() and detectChanges()?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>markForCheck()</code>: Marks this component and all ancestors as dirty. Change detection will check them on the next cycle. Respects the normal top-down CD flow. <code>detectChanges()</code>: Immediately triggers CD for this component and its children, right now. Use sparingly — it breaks the normal flow and can cause issues.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain InjectionToken and when you'd use it.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>InjectionToken</code> provides DI for non-class values (strings, numbers, objects, functions). Use cases: API URLs, configuration objects, feature flags, abstract interfaces. Tokens can include a factory function for tree-shakable defaults.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How does Angular animation work under the hood?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Angular animations use the Web Animations API under the hood. The <code>@angular/animations</code> module provides a declarative DSL: <code>trigger</code> defines an animation name, <code>state</code> defines named states with styles, <code>transition</code> defines how to animate between states, <code>animate</code> specifies duration/easing/delay. Complex features include <code>query</code> (animate children), <code>stagger</code> (delayed sequential animations), and <code>group</code> (parallel animations).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain deferrable views (@defer) and their impact on performance.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>@defer</code> enables component-level code splitting in templates. The deferred component's code is split into a separate chunk and loaded when triggered (viewport, idle, interaction, hover, timer, or boolean condition). Supports @placeholder, @loading (with minimum duration to prevent flicker), and @error blocks. Replaces complex lazy loading patterns for many use cases.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is the resource() API?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>resource()</code> is a signal-based API for async data fetching. It takes a <code>request</code> function (returns params derived from signals) and a <code>loader</code> function (fetches data). It auto-refetches when request signal dependencies change and provides <code>.value()</code>, <code>.isLoading()</code>, <code>.error()</code>, and <code>.status()</code> signals. <code>rxResource()</code> is the Observable-based variant.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is linkedSignal?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>linkedSignal</code> (Angular 19+) creates a writable signal that auto-resets when its source signal changes, but can also be set manually. Use case: a selected tab that resets to the first tab when the tab list changes, but the user can still click to select a different one.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How would you handle complex state management?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Level 1 — Service + Signals:</strong> For most apps. Simple, testable. <strong>Level 2 — NgRx SignalStore:</strong> Structured store with features. <strong>Level 3 — NgRx Store (Redux):</strong> Actions, reducers, effects, selectors. Maximum predictability for very large apps. Don't reach for NgRx until services become hard to reason about.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What are Angular schematics?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Schematics are code generators/transformers used by the CLI. They describe changes to a file tree (create, modify, delete files). Libraries use <code>ng add</code> schematics for auto-setup, and <code>ng update</code> schematics for migrations. You can create custom schematics for your team's patterns.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain how Angular handles i18n.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Angular's built-in i18n uses <code>i18n</code> attribute on elements. At build time, Angular extracts marked text into XLIFF/XMB files for translation. It builds a separate bundle per locale — each is a fully compiled app. Supports pluralization and ICU expressions. Alternative: runtime libraries like ngx-translate or Transloco for single-bundle translations.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What design patterns do you use in Angular architecture?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Smart/Dumb</strong> — Separate data from presentation. <strong>Facade</strong> — Simplify complex service interactions. <strong>Repository</strong> — Abstract data access for testability. <strong>Adapter</strong> — Transform API data at the boundary. <strong>Strategy</strong> — Swap implementations via InjectionTokens. <strong>Observer</strong> — RxJS Observables/Subjects. <strong>Mediator</strong> — Central service coordinating sibling components.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is a multi-provider and when would you use it?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Setting <code>multi: true</code> on a provider lets multiple providers for the same token coexist. Instead of overriding, Angular collects all into an array. Use cases: multiple HTTP interceptors, multiple validators, plugin systems where multiple implementations register themselves.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How do you implement a custom form control (ControlValueAccessor)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Implement <code>ControlValueAccessor</code> interface with 4 methods: <code>writeValue()</code> — receive value from form. <code>registerOnChange()</code> — store callback to push changes. <code>registerOnTouched()</code> — store callback for blur/touched. <code>setDisabledState()</code> — handle disabled. Register as <code>NG_VALUE_ACCESSOR</code> provider. This makes custom components work with ngModel and formControl.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain ExpressionChangedAfterItHasBeenChecked error.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Angular runs change detection twice in dev mode (the second is a verification pass). If a binding's value changes between the two runs, you get this error. Common causes: modifying state in ngAfterViewInit, modifying parent state from child lifecycle hooks. Fixes: use setTimeout, move logic to ngOnInit, or use signals which batch updates correctly.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is withComponentInputBinding() and why is it useful?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A router feature that automatically binds route params, query params, and resolved data to matching component <code>@Input()</code> properties. Instead of injecting ActivatedRoute and extracting params manually, the router does it for you. Cleaner, less boilerplate, works with signal inputs too.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is the Renderer2 and when would you use it?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>Renderer2</code> is an abstraction over DOM manipulation. Instead of directly accessing <code>nativeElement</code>, use Renderer2 methods (<code>addClass</code>, <code>setStyle</code>, <code>setAttribute</code>, <code>listen</code>). Benefits: platform-agnostic (works in SSR, Web Workers), testable, and secure (avoids direct DOM access that bypasses sanitization).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What are Angular elements (custom elements)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Angular Elements lets you package Angular components as standard Web Components (Custom Elements). They can be used in any HTML page, even without Angular. Use <code>createCustomElement()</code> from <code>@angular/elements</code>. Great for: micro-frontends, embeddable widgets, gradual migration from other frameworks.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the DestroyRef and takeUntilDestroyed?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>DestroyRef</code> (Angular 16+) provides a way to register cleanup callbacks. <code>takeUntilDestroyed()</code> is a pipe operator that automatically completes an Observable when the component/service is destroyed. It's the cleanest way to handle unsubscription, replacing the Subject + takeUntil pattern.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the difference between providedIn: 'root' and providing in a component?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>providedIn: 'root'</code>: One singleton instance for the entire app. Tree-shakable if unused. <code>providers: [Service]</code> in a component: Creates a new instance for each instance of that component and its descendants. Use component-level for services that should NOT be shared globally.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">How do you handle errors in HTTP requests?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>Component level:</strong> <code>catchError</code> operator in the pipe chain. <strong>Global level:</strong> Error interceptor that catches specific status codes (401 → login redirect, 500 → error page). <strong>Retry:</strong> <code>retry({ count: 3, delay: 1000 })</code> for transient failures. Always transform errors into user-friendly messages.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is *ngFor and how does @for differ?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>*ngFor</code> is a structural directive for iterating arrays. <code>@for</code> (Angular 17+) is the new built-in control flow — same purpose but compiled by Angular's template compiler for better performance. <code>@for</code> requires <code>track</code> and supports <code>@empty</code> blocks.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is two-way data binding and how does it work?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Two-way binding combines property binding and event binding. <code>[(ngModel)]=\"name\"</code> is shorthand for <code>[ngModel]=\"name\" (ngModelChange)=\"name = $event\"</code>. It syncs the model and the view — when either changes, the other updates.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is ngTemplateOutlet?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>ngTemplateOutlet</code> instantiates an ng-template with an optional context object. It lets you render templates dynamically and pass data via context. Use cases: customisable table rows, configurable list item templates, dynamic form layouts.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the async/await equivalent in Angular (with Observables)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Use <code>firstValueFrom()</code> or <code>lastValueFrom()</code> to convert Observables to Promises: <code>const user = await firstValueFrom(this.http.get('/api/user'))</code>. However, the RxJS pipe approach is usually better because it supports cancellation, retry, and composition.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is a dynamic component and how do you create one?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Use <code>ViewContainerRef.createComponent()</code> to instantiate components programmatically at runtime. Pass a component class reference. Set inputs via <code>componentRef.setInput()</code>. Use cases: modals, dynamic form fields, plugin architectures. With @defer, many dynamic component use cases are now simpler.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is tree-shaking and how does Angular support it?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Tree-shaking removes unused code from the final bundle. Angular supports it through: <code>providedIn: 'root'</code> (services only included if imported), standalone components (only bundled when imported), and AOT compilation (dead code analysis at build time). The build tool (esbuild/webpack) performs the actual tree-shaking.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How do you implement a global error handler?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Provide a custom <code>ErrorHandler</code>: <code>{ provide: ErrorHandler, useClass: GlobalErrorHandler }</code>. The <code>handleError(error)</code> method catches all unhandled errors. Use it for: logging to external services (Sentry, DataDog), showing user-friendly error toasts, tracking error metrics. Combine with HTTP error interceptor for API errors.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the router-outlet?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>&lt;router-outlet&gt;</code> is a placeholder directive where the router renders matched components. You can have multiple named outlets for auxiliary routes: <code>&lt;router-outlet name=\"sidebar\"&gt;</code>. The component rendered inside changes based on the current URL.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the difference between forwardRef and regular references?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>forwardRef()</code> lets you reference a class that hasn't been defined yet (declared later in the file). TypeScript/JavaScript normally requires classes to be defined before they're referenced. Common use: circular dependencies, NG_VALUE_ACCESSOR providers referencing the component class being defined.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">Explain the difference between effect() and computed() in signals.</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><code>computed()</code> derives a new value from other signals — it's pure, has no side effects, and is lazy (only recalculates when read). <code>effect()</code> runs side effects when signals change — logging, API calls, DOM manipulation. Key rule: never write to signals inside computed(), but you can inside effect() (though it's discouraged).</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is signal-based change detection (zoneless)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>In zoneless Angular, there's no Zone.js. Change detection is driven entirely by signals. When a signal changes, Angular schedules an update only for components that read that signal. This is true fine-grained reactivity — no more checking the entire tree. Enable with <code>provideExperimentalZonelessChangeDetection()</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the CDK (Component Dev Kit)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>The CDK provides behavior primitives without opinionated styling: virtual scrolling, drag-and-drop, overlays (dialogs, tooltips), accessibility (focus trap, live announcer), clipboard, layout (breakpoint observer). It's the foundation Angular Material is built on, but you can use it with any design system.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How would you implement micro-frontends with Angular?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Options: <strong>1. Module Federation</strong> (webpack) — share dependencies at runtime between separately deployed Angular apps. <strong>2. Angular Elements</strong> — wrap components as Web Components, embed in any shell app. <strong>3. Native Federation</strong> — framework-agnostic alternative using import maps. <strong>4. Single-SPA</strong> — orchestrate multiple framework apps. Challenges: shared state, routing coordination, dependency versioning.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What are Signal Forms in Angular 21? How do they differ from Reactive Forms?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Signal Forms (experimental in v21) replace RxJS-based form state with signals. Instead of <code>new FormGroup()</code>, you use a <code>form()</code> function that wraps a signal model. Form values, validity, dirty/touched state are all signals — read them directly with <code>.value()</code>, <code>.valid()</code>. No more <code>valueChanges.subscribe()</code> or <code>takeUntilDestroyed()</code>. They combine the simplicity of template-driven forms with the power of reactive forms while being fully type-safe.</p><div class=\"analogy\">Reactive Forms = you subscribe to a newspaper and wait for delivery. Signal Forms = the newspaper is always on your desk, automatically updated — you just look at it whenever you want.</div></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What does \"zoneless Angular\" mean and why is it the default in v21?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Zoneless means Angular no longer depends on Zone.js to detect changes. Instead, change detection is driven entirely by signals. When a signal changes, only components reading that signal update. Benefits: ~15KB smaller bundle, faster INP (Interaction to Next Paint), native async/await without issues, cleaner stack traces. Angular 21 makes this the default for new projects because all signal primitives became stable in v20.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">How do you migrate an existing Angular app from Zone.js to zoneless?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>1.</strong> Run <code>ng generate @angular/core:zoneless</code> schematic. <strong>2.</strong> Ensure all components use <code>OnPush</code> change detection. <strong>3.</strong> Replace manual state mutations with signals. <strong>4.</strong> Replace <code>.subscribe()</code> in components with <code>async</code> pipe or signal-based patterns. <strong>5.</strong> Replace <code>provideZoneChangeDetection()</code> with <code>provideZonelessChangeDetection()</code>. <strong>6.</strong> Remove <code>zone.js</code> from polyfills. <strong>7.</strong> Test thoroughly — any component relying on Zone.js auto-detection will need manual updates.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is Vitest and why did Angular 21 adopt it over Karma?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Vitest is a Vite-native test runner that's significantly faster than Karma/Jasmine. Angular 21 makes it the default because: it runs zoneless by default (matching the new runtime), supports modern features like fake timers, snapshots, and parallelisation, uses the intuitive <code>vi</code> mocking API, and doesn't require a browser to run. Migrate existing projects with <code>ng generate @angular/core:karma-to-vitest</code>.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What is the Angular MCP Server?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>The MCP (Model Context Protocol) Server bridges your Angular codebase with AI models like Gemini or Claude. Launched via <code>ng mcp</code>, it provides tools: <code>find_examples</code> (curated code examples), <code>get_best_practices</code> (Angular best practices), <code>ai_tutor</code> (interactive learning), <code>search_documentation</code> (official docs search), <code>onpush_zoneless_migration</code> (migration help), and <code>modernize</code> (legacy code modernisation). It enables context-aware AI code generation that understands YOUR project structure.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is @angular/aria?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>A new library (developer preview in v21) providing unstyled, accessible UI directives implementing WAI-ARIA patterns. Includes tree views, tabs, accordions, listboxes, dialogs — each with keyboard navigation, ARIA attributes, focus management, and screen reader support built in. Designed as foundations for custom component libraries, not for direct app use. Important for EU Accessibility Act compliance.</p></div></div></div>\n<div class=\"qa\" data-l=\"b\"><div class=\"qa-h\"><span class=\"lvl b\">Beginner</span><span class=\"qq\">What is Hot Module Replacement (HMR) in Angular?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>HMR updates your app in the browser instantly when you save a file, without a full page reload. Angular 19 introduced CSS and template HMR, and Angular 20 graduated template HMR to stable. This means editing a template or style file reflects immediately — no losing component state or waiting for recompilation.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What are selectorless components (expected in Angular 22)?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Currently, components require a string <code>selector: 'app-user-card'</code>. Selectorless components would remove this requirement — you'd import the component class directly in templates. Benefits: better refactorability (rename the class, all references update), type safety (no string matching), and simpler mental model. This aligns with the standalone-first architecture where components are self-describing.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">What Angular CVEs were disclosed in late 2025 and what did they affect?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>Three major CVEs: <strong>CVE-2025-59052</strong> (CVSS 7.1) — SSR race condition leaking request-scoped data between concurrent requests. <strong>CVE-2025-66035</strong> (CVSS 7.7) — HttpClient leaking XSRF tokens via protocol-relative URLs. <strong>CVE-2025-66412</strong> (CVSS High) — Stored XSS in template compiler via SVG/MathML attributes. All patched in v19.2+, v20, v21. This demonstrates why running EOL versions is a serious security risk.</p></div></div></div>\n<div class=\"qa\" data-l=\"i\"><div class=\"qa-h\"><span class=\"lvl i\">Intermediate</span><span class=\"qq\">What is the evolution of signals across Angular versions?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p><strong>v16</strong>: Signals introduced (developer preview). <strong>v17</strong>: Signal inputs, model inputs, view queries added. <strong>v18</strong>: linkedSignal, resource() added. <strong>v19</strong>: Signal primitives polished. <strong>v20</strong>: ALL signal primitives graduated to stable (signal, computed, effect, linkedSignal, signal queries, signal inputs). <strong>v21</strong>: Signal Forms (experimental), zoneless-by-default (signals drive CD). <strong>v22</strong>: Signal Forms expected to stabilise, OnPush expected as default.</p></div></div></div>\n<div class=\"qa\" data-l=\"a\"><div class=\"qa-h\"><span class=\"lvl a\">Advanced</span><span class=\"qq\">How does Angular 21 handle testing in a zoneless world?</span><span class=\"tog\">+</span></div><div class=\"qa-a\"><div class=\"qa-ai\"><p>With Vitest as default, tests run zoneless — matching the runtime. Signal-based state is synchronous, so assertions work without <code>fakeAsync</code> or <code>tick()</code> for signal updates. For async operations, Vitest's fake timers replace Angular's zone-based testing utilities. <code>fixture.detectChanges()</code> still works but is needed less often since signal-based components update more predictably. The key shift: test signal values directly rather than querying DOM.</p></div></div></div>\n\n</div>\n\n</a></b></div>"
}