export const jsQA = [
  // BEGINNER (15)
  { level: 'basic', q: 'What are the primitive data types in JavaScript?', a: 'String, Number, Boolean, BigInt, Symbol, Undefined, and Null. They are immutable and compared by value.' },
  { level: 'basic', q: 'What is the difference between null and undefined?', a: '`undefined` means a variable has been declared but not assigned a value. `null` is an assignment value that intentionally represents no value or an empty object reference.' },
  { level: 'basic', q: 'What is the difference between `==` and `===`?', a: '`==` checks for equality with type coercion (e.g. `1 == "1"` is true). `===` is strict equality and checks if both the value and the type are identical.' },
  { level: 'basic', q: 'What is hoisting?', a: 'A JavaScript mechanism where variable (`var`) and function declarations are moved to the top of their scope before code execution. Variables declared with `let` and `const` are hoisted too, but remain in a Temporal Dead Zone until initialized.' },
  { level: 'basic', q: 'What is a closure?', a: 'A function bundled together with its lexical environment. A closure gives you access to an outer function\'s scope from an inner function, even after the outer function has returned.' },
  { level: 'basic', q: 'What is the DOM?', a: 'Document Object Model. An API representing the HTML document as a tree of nodes, allowing JS to manipulate the structure, style, and content of the page.' },
  { level: 'basic', q: 'What is event bubbling?', a: 'When an event happens on an element (like a button click), it first runs the handlers on it, then on its parent, then all the way up on other ancestors. Use `event.stopPropagation()` to stop it.' },
  { level: 'basic', q: 'What does "this" refer to in JavaScript?', a: '`this` refers to the object that is executing the current function. Its value is determined exactly by *how* a function is called, not where it is defined (except for arrow functions).' },
  { level: 'basic', q: 'What is the difference between var, let, and const?', a: '`var` is function-scoped and can be re-declared. `let` is block-scoped and can be reassigned. `const` is block-scoped and its reference cannot be reassigned (though objects/arrays inside it can be mutated).' },
  { level: 'basic', q: 'How does the map() array method work?', a: 'It creates a new array populated with the results of calling a provided function on every element in the calling array. It does not mutate the original array.' },
  { level: 'basic', q: 'How does the filter() array method work?', a: 'It creates a new array with all elements that pass the test implemented by the provided function.' },
  { level: 'basic', q: 'What is a Promise?', a: 'An object representing the eventual completion (or failure) of an asynchronous operation and its resulting value. It has three states: pending, fulfilled, or rejected.' },
  { level: 'basic', q: 'What does `async` and `await` do?', a: '`async` makes a function return a Promise. `await` makes an async function pause execution and wait for a Promise to resolve, making asynchronous code look synchronous.' },
  { level: 'basic', q: 'Why do we use strict mode (`"use strict"`)?', a: 'It catches common coding bloopers (like using undeclared variables), disables features that are confusing, and prepares code for future ECMAScript versions.' },
  { level: 'basic', q: 'What is an immediately invoked function expression (IIFE)?', a: 'A function that runs as soon as it is defined: `(function() { ... })();`. Used to create a new, private block scope to avoid polluting the global namespace.' },

  // INTERMEDIATE/ADVANCED (20)
  { level: 'intermediate', q: 'What is the event loop?', a: 'The secret behind JavaScript\'s asynchronous behavior. It continually checks if the call stack is empty. If it is, it takes the first callback from the microtask/macrotask queue and pushes it to the stack for execution.' },
  { level: 'intermediate', q: 'What is the difference between macrotasks and microtasks?', a: 'Microtasks (Promises, `queueMicrotask`, MutationObserver) have higher priority. The event loop empties the entire microtask queue before picking up the next macrotask (setTimeout, setInterval, user events).' },
  { level: 'intermediate', q: 'What is Debouncing?', a: 'A pattern ensuring a function is only fired once a certain amount of time has passed since it was last invoked. Useful for search box inputs so you don\'t hit the API on every keystroke.' },
  { level: 'intermediate', q: 'What is Throttling?', a: 'A pattern ensuring a function is called at most once in a specified time period. Useful for scroll or resize events so the browser doesn\'t crash from continuous execution.' },
  { level: 'advanced', q: 'Explain `.bind()`, `.call()`, and `.apply()`.', a: 'They explicitly set the value of `this`. `call` takes comma-separated arguments. `apply` takes an array of arguments. `bind` returns a *new* function with `this` permanently bound without immediately executing it.' },
  { level: 'intermediate', q: 'Why do Arrow Functions behave differently with `this`?', a: 'Arrow functions do not have their own `this`. They inherit `this` from the surrounding lexical context at the time they are defined. You cannot `bind`, `call`, or `apply` a new `this` to an arrow function.' },
  { level: 'advanced', q: 'What is prototypal inheritance?', a: 'In JS, objects inherit properties and methods from a prototype object. If a property isn\'t found on an object, JS looks up the prototype chain until it finds it or reaches `null`.' },
  { level: 'advanced', q: 'How does garbage collection work in JavaScript?', a: 'The engine uses a "Mark and Sweep" algorithm. It starts from global "roots", marks all reachable objects, and then sweeps (deletes) any objects in memory that are unreachable.' },
  { level: 'advanced', q: 'What causes memory leaks in JS?', a: 'Common causes: Accidental global variables, forgotten timers/intervals (`setInterval`), closures holding onto large outer scope variables, and detached DOM elements referenced in JS objects.' },
  { level: 'advanced', q: 'What is a WeakMap or WeakSet?', a: 'Similar to Map/Set, but JS objects used as keys are held "weakly". This means if there are no other references to the object key, it can be garbage collected, preventing memory leaks. They cannot be iterated.' },
  { level: 'advanced', q: 'What is the difference between Deep Copy and Shallow Copy?', a: 'A shallow copy copies only top-level properties; nested objects share the same reference. A deep copy creates completely new references for all nested objects (e.g., using `structuredClone(obj)`).' },
  { level: 'advanced', q: 'Explain the concept of Currying.', a: 'Converting a function that takes multiple arguments into a sequence of functions that each take a single argument: `f(a,b,c)` becomes `f(a)(b)(c)`. Highly useful in functional composition.' },
  { level: 'advanced', q: 'How does Service Worker caching differ from standard HTTP caching?', a: 'HTTP caching is controlled by server headers. Service Worker caching is controlled programmatically via JavaScript using the Cache API, allowing granular offline-first strategies, bypassing the network completely.' },
  { level: 'advanced', q: 'What is a SharedWorker vs WebWorker?', a: 'A standard Web Worker is accessible only to the script that spawned it. A SharedWorker can be accessed by multiple scripts, even if they are in different windows, iframes, or tabs on the exact same origin.' },
  { level: 'advanced', q: 'What is CORS (Cross-Origin Resource Sharing)?', a: 'A security mechanism where the browser prevents a frontend JS app to make API requests to a different domain, unless the target domain explicitly returns HTTP headers letting the browser know it\'s permitted.' },
  { level: 'expert', q: 'How do JavaScript Generators (`function*`) work?', a: 'They are functions that can be paused and exited, and later re-entered, preserving context (variable bindings) across re-entrances. They return an Iterator object, progressing only when `.next()` is called. They use the `yield` keyword.' },
  { level: 'expert', q: 'What does `Object.defineProperty()` do?', a: 'It defines a new property directly on an object, or modifies an existing one, allowing strict configuration of metadata like `enumerable`, `configurable`, `writable`, or setting custom `get` and `set` traps.' },
  { level: 'expert', q: 'How does Proxy essentially work?', a: 'It allows you to create a wrapper for another object, intercepting and redefining fundamental operations for it like property lookup, assignment, enumeration, and function invocation. `const p = new Proxy(target, handler)`.' },
  { level: 'advanced', q: 'What is the purpose of `Symbol`?', a: 'A built-in object whose constructor returns a guaranteed unique identifier. Useful to add hidden properties to an object that won\'t conflict with other keys or show up in standard `for...in` loops.' },
  { level: 'intermediate', q: 'What is the spread operator vs rest parameter?', a: 'Syntax is identical (`...`). Spread EXPANDS an array/object into individual elements (e.g., `let newArr = [...old]`). Rest COLLECTS multiple elements and condenses them into a single array element (e.g., in function args: `function f(...args)`).' },

  // TYPESCRIPT (25)
  { level: 'basic', q: 'What is TypeScript?', a: 'A structural, strongly typed superset of JavaScript developed by Microsoft. It compiles to plain JavaScript and adds static typing, identifying errors at compile-time instead of runtime.' },
  { level: 'basic', q: 'What is the difference between implicit and explicit typing?', a: 'Explicit: You declare the type: `let count: number = 5;`. Implicit (Inference): TypeScript figures it out automatically: `let count = 5;` (TS knows it\'s a number).' },
  { level: 'basic', q: 'What is the "any" type?', a: 'It allows any type of value, effectively opting out of type-checking completely for that variable. It ruins TypeScript\'s benefits and should be strictly avoided in production code.' },
  { level: 'basic', q: 'What is the "unknown" type?', a: 'A safer alternative to `any`. You can assign anything to `unknown`, but to operate on it, TS forces you to perform a type check (type narrowing/guard) first to prove you know its structural shape.' },
  { level: 'basic', q: 'What is the "void" vs "never" type?', a: '`void` is used for a function that returns nothing (implicitly returns undefined). `never` is used for a function that WILL NEVER return anything (e.g., it always throws an Error, or has an infinite `while(true)` loop).' },
  { level: 'intermediate', q: 'What is Duck Typing / Structural Typing in TS?', a: 'TypeScript focuses on the SHAPE of data. If an object has all the properties required by an Interface, TS accepts it, regardless of whether it explicitly "implements" that interface. "If it walks like a duck and quacks like a duck..."' },
  { level: 'intermediate', q: 'What is an Interface in TypeScript?', a: 'A way to name an object type. Interfaces can be extended with the `extends` keyword and can be merged automatically if declared twice.' },
  { level: 'intermediate', q: 'What is a Type Alias (`type`)?', a: 'Creates a new name for any type. Unlike Interfaces, it can describe primitives, unions, and intersections. In modern TS, objects defined via `type` vs `interface` are almost functionally identical.' },
  { level: 'intermediate', q: 'What is a Tuple?', a: 'An array with a fixed number of elements whose types are known. Example: `let person: [string, number] = ["Alice", 25];`' },
  { level: 'intermediate', q: 'What is an Enum?', a: 'A way to give more friendly names to sets of numeric or string constants. However, TS Enums actively generate JS runtime code (unlike Interfaces), so string literal unions (`type Status = "OK" | "FAIL"`) are often heavily preferred instead.' },
  { level: 'intermediate', q: 'What are Generics?', a: 'Generics provide a way to make components work over a variety of types rather than a single one. It creates a variable for a type, e.g., `<T>`. `function identity<T>(arg: T): T`.' },
  { level: 'intermediate', q: 'What is a Type Guard (Type Narrowing)?', a: 'Using standard JS checks (like `typeof x === "string"`, `instanceof Array`, or `in` operator) allowing TypeScript to infer the variable\'s specific type within a narrower conditional block.' },
  { level: 'advanced', q: 'What is a custom Type Predicate?', a: 'A function that returns a boolean, where the return type is explicitly cast as `parameterName is Type`. It tells the TS compiler to narrow the type if the function evaluates to true. Ex: `function isString(v: any): v is string`.' },
  { level: 'advanced', q: 'What is Intersection vs Union?', a: 'Union (`|`): a value that can be one of several types. Intersection (`&`): a value that combines multiple types into one single type, meaning the object must have ALL the properties of the intersected types.' },
  { level: 'advanced', q: 'What are Utility Types in TS?', a: 'Built-in generic tools that transform types. Examples: `Partial<T>` (all props optional), `Readonly<T>` (all props readonly), `Pick<T, K>` (select specific props), `Omit<T, K>` (remove specific props).' },
  { level: 'advanced', q: 'What is keyof?', a: 'An operator that returns a Union type composed of the names of the keys of an Object type. E.g., `type Keys = keyof { name: string; age: number }` results in `"name" | "age"`.' },
  { level: 'advanced', q: 'What is typeof (in TS type context)?', a: 'Allows you to extract the TS type from an existing runtime JavaScript variable. `let obj = {x: 1}; type ObjType = typeof obj;`' },
  { level: 'expert', q: 'What are Conditional Types?', a: 'They select one of two possible types based on a condition expressed as a type relationship test: `T extends U ? X : Y`. Extremely powerful in building dynamic utility types.' },
  { level: 'expert', q: 'What is the `infer` keyword?', a: 'Used strictly within Conditional Types to pause resolution and declare a new, dynamically inferred type variable. Essential for extracting the return type of a function: `T extends (...args: any[]) => infer R ? R : any`.' },
  { level: 'advanced', q: 'What does `strict: true` do in tsconfig.json?', a: 'It enables a broad suite of strict type-checking behavior (including `noImplicitAny`, `strictNullChecks`, `strictFunctionTypes`, and `strictBindCallApply`). Highly recommended for all enterprise projects.' },
  { level: 'advanced', q: 'What is a Discriminated Union?', a: 'A pattern combining Union types, type guards, and a common shared property (the discriminator) to guarantee exhaustive structural safety (often used in Redux actions or state machines).' },
  { level: 'advanced', q: 'What is Declaration Merging?', a: 'The compiler merges two separate declarations declared with the exact same name into a single definition. This is why Interfaces are the standard for public APIs (consumers can easily augment your `Window` interface, for example).' },
  { level: 'expert', q: 'What is Type Variance (Covariance, Contravariance)?', a: 'Advanced concepts defining how complex types (like arrays or functions) relate to each other based on their internal structural types. TS functions are contravariant in parameters, but covariant in return types to ensure safety.' },
  { level: 'advanced', q: 'What is "Index Signature"?', a: 'Syntax allowing you to type objects with dynamic or unknown keys but a specific value type. `interface Dict { [key: string]: number; }`.' },
  { level: 'intermediate', q: 'What does `as const` do (Const Assertions)?', a: 'Normally, TS widens string literals. `const obj = { a: "hello" }` implies `a` is a `string`. Suffixing it with `as const` implies `a` is specifically the strict read-only literal type `"hello"`. Deeply useful for config arrays and objects.' },

  // EXPERT JS / MODERN ES2025+ (25)
  { level: 'expert', q: 'What is `Promise.withResolvers()`?', a: 'A new ES2024 feature that returns an object containing a new Promise and its `resolve` and `reject` functions. It eliminates the need to manually extract those functions from the Promise executor callback.' },
  { level: 'expert', q: 'How do `Object.groupBy()` and `Map.groupBy()` work?', a: 'They allow grouping elements of an iterable based on a string or object key provided by a callback function. `Map.groupBy` is preferred when the keys should be complex objects rather than just strings.' },
  { level: 'expert', q: 'What is the "Task" vs "Microtask" execution order in the Event Loop?', a: 'The loop executes ONE macrotask, then empties the ENTIRE microtask queue. If a microtask adds more microtasks, they are also executed before the next macrotask. This can lead to "microtask starvation" if not careful.' },
  { level: 'expert', q: 'What is the difference between `WeakRef` and `FinalizationRegistry`?', a: '`WeakRef` lets you hold a reference to an object without preventing it from being garbage collected. `FinalizationRegistry` lets you request a callback when an object has been garbage collected, useful for cleaning up non-JS resources.' },
  { level: 'expert', q: 'How does V8\'s "TurboFan" compiler optimize code?', a: 'It uses Speculative Optimization. It profiles the types passing through your functions (using "Inline Caches"). If types are consistent (monomorphic), it compiles highly optimized machine code. If types change (polymorphic/megamorphic), it de-optimizes back to the "Ignition" interpreter.' },
  { level: 'expert', q: 'What is "Temporal Dead Zone" (TDZ) in depth?', a: 'The period between a block\'s entry and the point where a variable is initialized. Accessing `let` or `const` in this zone throws a `ReferenceError`, unlike `var` which would just return `undefined`. This ensures variables are never used before their literal definition.' },
  { level: 'expert', q: 'What are Atomics in JavaScript?', a: 'A global object providing atomic operations (like `add`, `sub`, `and`, `or`) for `SharedArrayBuffer`. It ensures that multi-threaded data operations in Web Workers are thread-safe and predictable without race conditions.' },
  { level: 'expert', q: 'How do decorators work in native JavaScript (TC39)?', a: 'Native decorators (Stage 3+) allow class, method, and field annotations. They are functions that receive metadata about the target and can wrap or replace it. This differs from legacy "experimental" TypeScript decorators which were based on an older proposal.' },
  { level: 'expert', q: 'Explain "Shadow Realms" (Proposed).', a: 'A new global environment that provides a completely isolated JS execution context. It allows running code with its own global object and built-ins, preventing side effects or data leakage into the main execution realm.' },
  { level: 'expert', q: 'What is `Array.fromAsync()`?', a: 'A recent addition that creates a new array from an async iterable, iterable, or array-like object. It awaits each element if necessary, making it superior to `Promise.all(arr.map(async...))` in some sequential scenarios.' },
  { level: 'expert', q: 'How do you detect memory leaks using Chrome DevTools?', a: 'Capture a "Heap Snapshot". Look for objects that are "Detached" (not in DOM but still referenced in JS). Use the "Allocation Instrumentation" timeline to see objects allocated but never released during specific interactions.' },
  { level: 'expert', q: 'Explain the "Pipeline Operator" (`|>`) proposal.', a: 'It facilitates functional pipe-lining by piping the result of one expression as an argument to the next function: `val |> f |> g` is equivalent to `g(f(val))`. It significantly improves readability in nested logic.' },
  { level: 'expert', q: 'What is "Explicit Resource Management" (`using`) in JS?', a: 'Introduced in ES2024, it allows for scoping resources (like file handles or DB connections) that must be closed or cleaned up. Using the `using` keyword ensures the `[Symbol.dispose]` method is called automatically when leaving scope.' },
  { level: 'expert', q: 'How does the "Module Preload" link relation help performance?', a: 'It tells the browser to fetch and parse a JS module (and its entire dependency tree) early, without executing it immediately, reducing the waterfall effect in large ES Module architectures.' },
  { level: 'expert', q: 'What is "Hydration" in a JavaScript context?', a: 'The process where client-side JavaScript attaches event listeners to static HTML sent from the server (SSR), making it interactive. Optimization techniques like "Resumability" (Qwik) or "Progressive Hydration" aim to reduce this costly step.' },

  // EXPERT TYPESCRIPT & GYMNASTICS (20)
  { level: 'expert', q: 'What is a "Branded Type" (Nominal Typing)?', a: 'TS is structural, but branding simulates nominal typing by adding a unique "tag" property: `type Email = string & { __brand: "Email" }`. This prevents passing a generic string into a function expecting an Email.' },
  { level: 'expert', q: 'Explain "Recursive Types" in TS.', a: 'A type that references itself. Useful for deep object trees or JSON structures: `type JSONValue = string | number | boolean | null | JSONObject | JSONArray;`. TS 4.1+ improved support for deep recursion limits.' },
  { level: 'expert', q: 'How do "Template Literal Types" work?', a: 'They allow building types based on string literals: `type Direction = "top" | "bottom"; type Padded = `padding-${Direction}`;`. They enable massive type safety for CSS classes, event names, and i18n keys.' },
  { level: 'expert', q: 'What is "Distributive Conditional Types"?', a: 'When a conditional type (`T extends U ? X : Y`) is checking a Union type `T`, TS automatically distributes the check over each member of the union. Use `[T] extends [U]` to disable this "distribution".' },
  { level: 'expert', q: 'How do you extract the return type of a function dynamicallly?', a: 'Using the `ReturnType<T>` utility, which internally uses the `infer` keyword: `T extends (...args: any) => infer R ? R : any`.' },
  { level: 'expert', q: 'What is the `satisfies` operator?', a: 'Introduced in TS 4.9, it validates that an expression matches a type WITHOUT widening or changing the inferred type. `const colors = { red: "#f00" } satisfies Record<string, string>;` keeps the specific key `"red"` instead of widening to `string`.' },
  { level: 'expert', q: 'Explain "Assertion Functions" in TS.', a: 'Functions that throw an error if a condition isn\'t met. By using the `asserts condition` return type, TS knows the condition is guaranteed to be true for the rest of the scope if the function returns normally.' },
  { level: 'expert', q: 'What is "Module Augmentation"?', a: 'A way to add declarations to an existing module. Useful when using 3rd party libraries that have missing or outdated types: `declare module "some-lib" { ... }`.' },
  { level: 'expert', q: 'What are "Global Type Declarations" (`.d.ts`)?', a: 'Files that provide type information for code that already exists in JavaScript but doesn\'t have types. They are purely for the compiler and emit no JS code.' },
  { level: 'expert', q: 'How does "Const Type Parameters" (`<const T>`) work?', a: 'Introduced in TS 5.0, it allows generics to be inferred as literal `const` values by default, avoiding the need for the user to append `as const` manually to arguments.' },
  { level: 'expert', q: 'What is "Exhaustiveness Checking"?', a: 'A pattern using the `never` type in a `switch` default case to ensure all possible members of a Union type are handled. If a new member is added to the union, TS will alert you that the code is no longer exhaustive.' },
  { level: 'expert', q: 'Explain "Covariance" vs "Contravariance" in TS.', a: 'Covariance: `T` can be replaced by a more specific type (Standard for properties). Contravariance: `T` can be replaced by a more general type (Standard for function parameters). TS enforces these rules to ensure type safety across inheritance.' },
  { level: 'expert', q: 'What is the "Omit" vs "Exclude" utility type?', a: '`Exclude<Union, Members>` removes members from a UNION type. `Omit<Object, Keys>` removes specific properties from an OBJECT type.' },
  { level: 'expert', q: 'How do you handle "Branded Strings" with Template Literals?', a: 'By creating a type that combines a string with a unique intersection: `type UserID = `user_${string}` & { __brand: "UserID" }`. This ensures any ID used must match the prefix AND be explicitly cast/verified.' },
  { level: 'expert', q: 'What is "Mapped Type Remapping"?', a: 'Using the `as` clause inside a mapped type to change key names: `type Getters<T> = { [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K] }`.' },

  // ADDITIONAL EXPERT JS/TS (15)
  { level: 'expert', q: 'What is the purpose of `requestIdleCallback`?', a: 'It allows scheduling background and low-priority work during the browser\'s idle periods, without affecting latency-critical events like animation and input response. Ideal for non-essential telemetry or pre-fetching.' },
  { level: 'expert', q: 'How does `Atomics.wait()` and `Atomics.notify()` work?', a: 'They provide a way for workers to sleep until a value in a `SharedArrayBuffer` changes. `wait` blocks the thread until notified, while `notify` wakes up threads waiting on that specific index. Essential for low-level synchronization.' },
  { level: 'expert', q: 'Explain the "Same-Origin Policy" (SOP) vs CORS.', a: 'SOP is a fundamental browser security policy that prevents scripts on one origin from accessing data on another origin. CORS is a mechanism that allows servers to explicitly opt-in to sharing resources across origins.' },
  { level: 'expert', q: 'What is a "User-Agent Client Hints" (UA-CH)?', a: 'A modern, more privacy-preserving alternative to the `User-Agent` string. It allows browsers to selectively share device/browser metadata with servers via HTTP headers or JS API, reducing passive fingerprinting.' },
  { level: 'expert', q: 'How do "Private Class Features" (#prefix) differ from `private` keyword in TS?', a: '`#private` is a native JS feature that provides hard runtime encapsulation; the field is truly inaccessible outside the class. TS `private` is a compile-time check only; the field is still accessible in the emitted JavaScript.' },
  { level: 'expert', q: 'What is "BigInt" and why can\'t it be mixed with "Number"?', a: '`BigInt` handles integers larger than $2^{53}-1$. Mixing them with standard `Number` is prohibited because it would lead to silent precision loss during implicit type conversion.' },
  { level: 'expert', q: 'Explain "Top-level await" and its impact on module loading.', a: 'It allows using `await` at the top level of a module. While convenient, it makes the module (and its importers) wait for the promise to resolve before execution continues, which can delay application startup if used poorly.' },
  { level: 'expert', q: 'What is "Structural" vs "Nominal" typing in depth?', a: 'Structural (TS): Types are compatible if their members match. Nominal (Java/C#): Types are only compatible if they have the same name/declaration. TS uses structural typing for flexibility, but can simulate nominal typing via branding.' },
  { level: 'expert', q: 'What is "Type Argument Inference" for functions?', a: 'The process where TS automatically determines the types for generic parameters based on the values passed to the function. If inference fails, you must provide them explicitly: `f<string>("val")`.' },
  { level: 'expert', q: 'How do you handle "Recursive Conditional Types"?', a: 'By having a conditional type reference itself in its branches. TS 4.1+ allows this as long as there is a clear base case, enabling complex types like `DeepPartial<T>` or `DeepReadonly<T>`.' },
  { level: 'expert', q: 'What is the "NoInfer" intrinsic type?', a: 'Introduced in TS 5.4, it prevents a generic type parameter from being a candidate for type inference in a specific location, forcing TS to use the inference from other locations instead.' },
  { level: 'expert', q: 'Explain "String Mapping Types" (Uppercase, Lowercase, etc.).', a: 'Intrinsic type operators that transform string literal types. Invaluable when used with Template Literal Types to enforce casing conventions across API keys or file paths.' },
  { level: 'expert', q: 'What is a "Global augmentation" vs "Module augmentation"?', a: 'Module: Adds to a specific exported module. Global: Adds to the global namespace (e.g., adding a new property to the `Window` object or `ProcessEnv`).' },
  { level: 'expert', q: 'How does "Discriminated Union" Exhaustiveness with `default` work?', a: 'By assigning the `default` case to a variable of type `never`. If a new case is added to the union but not handled in the switch, the assignment `const _exhaustiveCheck: never = member` will cause a compile error.' },
  { level: 'expert', q: 'What is "Metadata Reflection" (reflect-metadata)?', a: 'A polyfill/proposal that allows attaching and retrieving metadata at runtime on classes and members. Heavily used in frameworks like NestJS or Angular for dependency injection.' },
];
