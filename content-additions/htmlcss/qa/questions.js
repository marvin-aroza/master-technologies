export const htmlCssQA = [
  // HTML BEGINNER (10)
  { level: 'basic', q: 'What is semantic HTML and why is it important?', a: 'Using HTML elements that convey the meaning of the content (e.g., `<article>`, `<footer>`, `<nav>`) instead of generic wrappers (`<div>`, `<span>`). It severely improves accessibility (for screen readers) and SEO.' },
  { level: 'basic', q: 'What is the purpose of the alt attribute on images?', a: 'It provides alternative text if the image fails to load, and it is strictly required for screen readers to describe the image to visually impaired users.' },
  { level: 'basic', q: 'What is the difference between a block-level element and an inline element?', a: 'Block elements (like `<div>` or `<p>`) take up the full width available and start on a new line. Inline elements (like `<span>` or `<a>`) only take up as much width as necessary and do not start on a new line.' },
  { level: 'basic', q: 'What does the `defer` attribute do in a `<script>` tag?', a: 'It tells the browser to download the script in the background while continuing to parse the HTML, and only execute the script *after* the HTML parsing is completely finished. Prevents render blocking.' },
  { level: 'basic', q: 'What is the DOM?', a: 'Document Object Model. An object-oriented representation of the webpage, allowing JavaScript to modify the structure, style, and content dynamically.' },
  { level: 'basic', q: 'What are `data-` attributes?', a: 'Custom attributes (e.g., `data-user-id="123"`) that allow you to store extra information on standard HTML elements without using non-standard attributes. They are easily accessible via JS `element.dataset`.' },
  { level: 'basic', q: 'What is the viewport meta tag?', a: '`<meta name="viewport" content="width=device-width, initial-scale=1">`. It is absolutely essential for responsive web design. It tells mobile browsers how to control the page\'s dimensions and scaling.' },
  { level: 'basic', q: 'How do you create an accessible form input?', a: 'By explicitly associating a `<label>` with an `<input>` using the `for` and `id` attributes: `<label for="email">Email</label><input id="email">`.' },
  { level: 'basic', q: 'What is an iframe?', a: 'Inline Frame. An HTML element used to embed another completely separate HTML document within the current page (often used for videos, maps, or 3rd party ads).' },
  { level: 'basic', q: 'What is the difference between `<strong>` and `<b>`?', a: '`<b>` simply makes text bold visually. `<strong>` makes text bold visually AND implies strong importance/urgency, which screen readers emphasize physically.' },

  // HTML/A11Y INTERMEDIATE & ADVANCED (10)
  { level: 'intermediate', q: 'What is ARIA and when should you use it?', a: 'Accessible Rich Internet Applications. It provides attributes (roles, states, properties) to modify semantics for screen readers. Rule of thumb: ONLY use ARIA when native HTML tags cannot achieve the desired behavior.' },
  { level: 'intermediate', q: 'What is the difference between `aria-hidden="true"` and `hidden`?', a: 'The HTML `hidden` attribute physically removes the element from the display (like `display: none`). `aria-hidden="true"` leaves the element visible on the screen but hides it completely from screen readers (useful for decorative icons).' },
  { level: 'advanced', q: 'Explain `tabindex`.', a: 'It controls keyboard navigation flow. `tabindex="0"` makes a non-focusable element focusable. `tabindex="-1"` removes an element from sequential keyboard navigation but keeps it focusable via JS. Values > 0 are extremely heavily discouraged as they disrupt expected flow.' },
  { level: 'advanced', q: 'What are Web Components?', a: 'A suite of technologies allowing creating reusable native custom elements. It consists of Custom Elements, Shadow DOM (styles/markup encapsulation), and HTML Templates (`<template>` and `<slot>`).' },
  { level: 'expert', q: 'What is the Shadow DOM?', a: 'A web standard that allows attaching a hidden, encapsulated DOM tree to an element. Styles defined inside a Shadow DOM do not leak out, and global styles do not leak in. Crucial for widget isolation.' },
  { level: 'intermediate', q: 'What does `rel="noopener noreferrer"` do?', a: 'When opening a link via `target="_blank"`, it prevents the new page from accessing the `window.opener` object (which is a security vulnerability) and strips the Referer header for privacy.' },
  { level: 'advanced', q: 'How does the browser parse HTML?', a: 'It converts raw bytes into characters, then into tokens (tags), then into Nodes, and finally links Nodes into the DOM Tree. Blocking scripts freeze the parser until executed.' },
  { level: 'advanced', q: 'What is preload, prefetch, and preconnect?', a: 'Resource hints. `preconnect`: "I will soon connect to this domain, do DNS/TLS handshake early". `preload`: "I NEED this asset for the current page immediately". `prefetch`: "I MIGHT need this asset for the NEXT page, download it in idle time".' },
  { level: 'expert', q: 'What is `aria-live`?', a: 'Used to announce dynamic DOM changes (like toast notifications or form errors) to screen readers without shifting focus. Options are `"polite"` (wait until user is idle) or `"assertive"` (interrupt immediately).' },
  { level: 'expert', q: 'What is critical rendering path (CRP)?', a: 'The sequence of steps the browser takes to convert HTML, CSS, and JS into pixels on the screen. Optimizing CRP involves deferring non-critical scripts, inlining critical CSS, and minimizing layout thrashing.' },

  // CSS BEGINNER (10)
  { level: 'basic', q: 'What does CSS stand for?', a: 'Cascading Style Sheets.' },
  { level: 'basic', q: 'Explain the CSS Box Model.', a: 'Every element is a rectangular box composed of four areas: Content (the actual stuff), Padding (transparent space around content), Border (around the padding), and Margin (transparent space outside the border).' },
  { level: 'basic', q: 'What is the difference between `display: none` and `visibility: hidden`?', a: '`display: none` completely removes the element from the document layout (it takes up zero space). `visibility: hidden` hides the element visually, but it still takes up its precise spatial footprint.' },
  { level: 'basic', q: 'What does `box-sizing: border-box` do?', a: 'It radically fixes the box model. Instead of padding and borders adding to the element\'s configured width/height, they are subtracted from it. `width: 100px` with `10px padding` is strictly `100px` total width.' },
  { level: 'basic', q: 'What is a CSS selector?', a: 'The part of a CSS rule that targets which HTML elements to style (e.g., class `.btn`, ID `#header`, attribute `[disabled]`).' },
  { level: 'basic', q: 'What is the primary difference between CSS Grid and Flexbox?', a: 'Flexbox is designed for 1-dimensional layouts (working either in a row OR a column). CSS Grid is designed for 2-dimensional layouts (managing rows AND columns simultaneously).' },
  { level: 'basic', q: 'How does the `z-index` property work?', a: 'It controls the vertical stacking order of elements that overlap. Important: It ONLY works if the element has a `position` value other than `static` (like relative, absolute, fixed, or sticky).' },
  { level: 'basic', q: 'What are pseudo-classes vs pseudo-elements?', a: 'Pseudo-classes select an element\'s state (e.g., `:hover`, `:focus`). Pseudo-elements create/target specific virtual parts of an element that aren\'t in the DOM (e.g., `::before`, `::first-line`).' },
  { level: 'basic', q: 'What is `rem` vs `em`?', a: '`rem` is relative strictly to the root `<html>` font-size. `em` is relative to the font-size of the element\'s direct parent, which can cause chaotic compounding issues if nested deeply.' },
  { level: 'basic', q: 'What is the cascading aspect of CSS?', a: 'If two different rules apply to the same element, the cascade determines which one wins based on Source Order, Specificity, and Importance (`!important`).' },

  // CSS INTERMEDIATE/ADVANCED (20)
  { level: 'intermediate', q: 'How does CSS Specificity work?', a: 'It\'s calculated mathematically. Inline styles > IDs > Classes/Attributes/Pseudo-classes > Elements/Pseudo-elements. E.g., `#nav .btn` beats `div div .btn`.' },
  { level: 'intermediate', q: 'What is a Stacking Context?', a: 'A three-dimensional conceptualization of HTML elements. Certain properties (like `opacity < 1`, `transform`, `z-index`, `filter`) create a new local stacking context. Z-indexes inside a stacking context cannot physically break out and overlay elements in a higher parent stacking context.' },
  { level: 'intermediate', q: 'Explain absolute vs relative positioning.', a: '`relative`: The element remains in the normal document flow, but can be visually shifted using top/left. `absolute`: The element is removed completely from normal flow and positioned relative to its closest positioned ancestor (any parent with non-static position).' },
  { level: 'intermediate', q: 'What are CSS Custom Properties (Variables)?', a: 'Defined as `--primary: red;`, used as `color: var(--primary)`. Unlike SCSS variables (which compile out statically), CSS variables live entirely in the DOM natively. They cascade, and JS can alter them dynamically.' },
  { level: 'intermediate', q: 'What does `justify-content` do in Flexbox?', a: 'It precisely aligns flex items along the main axis (horizontal in `flex-direction: row`).' },
  { level: 'advanced', q: 'What does `align-items` vs `align-content` do in Flexbox/Grid?', a: '`align-items` aligns items along the cross axis (vertical usually). `align-content` only works when there are MULTIPLE lines of items (like `flex-wrap: wrap`), determining spacing between the lines themselves.' },
  { level: 'advanced', q: 'What is BEM?', a: 'Block, Element, Modifier. A highly scalable CSS naming convention. `.card` (Block), `.card__title` (Element), `.card--dark` (Modifier). It entirely prevents CSS scoping conflicts.' },
  { level: 'advanced', q: 'Explain the CSS `:has()` selector.', a: 'The long-awaited "parent selector". `a:has(> img)` strictly targets anchor tags that contain direct child images. It eliminates the need for massive amounts of JS DOM manipulation.' },
  { level: 'expert', q: 'What are CSS Container Queries?', a: 'Instead of media queries targeting the <i>viewport</i> width, container queries (`@container`) target the <i>parent element\'s</i> width. A card component can radically re-layout itself depending on whether it\'s embedded in a narrow sidebar or a wide grid, totally redefining component reusability.' },
  { level: 'advanced', q: 'What is `CSS Subgrid`?', a: 'Allows a nested grid container to inherit its parent\'s grid tracks instead of defining its own. Essential for perfectly aligning nested headers across adjacent sibling cards.' },
  { level: 'advanced', q: 'How does `calc()` work?', a: 'It allows mathematical expressions natively mixing different CSS units. `width: calc(100vw - 50px);`. Invaluable for dynamic geometry.' },
  { level: 'expert', q: 'What is Layout Thrashing?', a: 'When JS radically toggles between reading the DOM (e.g. `offsetHeight`) and writing to the DOM (altering styles) sequentially inside a loop. This forces the browser to synchronously recalculate layout multiple times, annihilating 60fps performance. Fix: Batch all reads, then batch all writes using `requestAnimationFrame`.' },
  { level: 'advanced', q: 'What is hardware acceleration in CSS?', a: 'Properties like `transform` and `opacity` are completely offloaded from the main CPU thread to the GPU. This means animations using these properties run flawlessly at 60fps without triggering layout paints.' },
  { level: 'advanced', q: 'What does `will-change` do?', a: 'A hint to the browser that an element is about to change (e.g., `will-change: transform`). The browser preemptively allocates GPU layers to it. Overuse consumes massive memory; apply right before animation starts, remove when done.' },
  { level: 'intermediate', q: 'How do you create a triangle natively in pure CSS?', a: 'By setting an element to `width: 0; height: 0;` and using thick borders on adjacent sides, with one side colored and the perpendicular sides transparent.' },
  { level: 'intermediate', q: 'What does `minmax()` do in CSS Grid?', a: 'Defines a track\'s minimum and maximum size constraint. Extensively used in auto-responsive grids: `grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));`.' },
  { level: 'advanced', q: 'What is the utility-first CSS approach?', a: 'The philosophy popularized by Tailwind CSS. Instead of writing semantic classes `.btn-primary` with custom CSS, you apply tiny, single-function utility classes directly in markup: `bg-blue-500 px-4 py-2 rounded`. It massively increases speed and eliminates dead CSS.' },
  { level: 'expert', q: 'What are CSS `scroll-timeline` animations?', a: 'A modern feature allowing CSS animations to be directly bonded to the user\'s scroll depth instead of time, enabling totally native parallax effects without JavaScript.' },
  { level: 'expert', q: 'Explain Margin Collapsing.', a: 'When the vertical margins of adjoining blocks touch, they combine ("collapse") into a single margin whose size is the largest of the two. This does NOT happen with horizontal margins, or between flex/grid items, or if padding/borders stand between them.' },
  { level: 'advanced', q: 'What is `content-visibility: auto`?', a: 'A monumental performance property. It completely halts the rendering and layout calculations of long, off-screen content sections until the user scrolls near them, dramatically slashing initial page load work.' }
];
