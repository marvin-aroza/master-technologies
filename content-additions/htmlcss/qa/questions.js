export const htmlCssQA = [
  // HTML BEGINNER (10)
  { level: 'basic', q: 'What is semantic HTML and why is it important?', a: 'Using HTML elements that convey the meaning of the content (e.g., `<article>`, `<footer>`, `<nav>`) instead of generic wrappers (`<div>`, `<span>`). It severely improves accessibility (for screen readers) and SEO.' },
  { level: 'basic', q: 'What is the purpose of the alt attribute on images?', a: 'It provides alternative text if the image fails to load, and it is strictly required for screen readers to describe the image to visually impaired users.' },
  { level: 'basic', q: 'What is the difference between a block-level element and an inline element?', a: 'Block elements (like `<div>` or `<p>`) take up the full width available and start on a new line. Inline elements (like `<span>` or `<a>`) only take up as much width as necessary and do not start on a new line.' },
  { level: 'basic', q: 'What does the `defer` attribute do in a `<script>` tag?', a: 'It tells the browser to download the script in the background while continuing to parse the HTML, and only execute the script *after* the HTML parsing is completely finished. Prevents render blocking.' },
  { level: 'basic', q: 'What is the DOM?', a: 'Document Object Model. An object-oriented representation of the webpage, allowing JavaScript to modify the structure, style, and content dynamically.' },
  { level: 'basic', q: 'What are `data-` attributes?', a: 'Custom attributes (e.g., `data-user-id="123"`) that allow you to store extra information on standard HTML elements without using non-standard attributes. They are easily accessible via JS `element.dataset`.' },
  { level: 'basic', q: 'What is the viewport meta tag?', a: '`<meta name="viewport" content="width=device-width, initial-scale=1">`. It is absolutely essential for responsive web design. It tells mobile browsers how to control the page\'s dimensions and scaling.' },
  { level: 'basic', q: 'How do you create an accessible form input?', a: 'By explicitly associating a `<label>` with an `<input>` using the `for` and `id` attributes: `<label for="email">Email</label><input id="email">`.' },
  { level: 'basic', q: 'What is an iframe?', a: 'Inline Frame. An HTML element used to embed another completely separate HTML document within the current page (often used for videos, maps, or 3rd party ads).' },
  { level: 'basic', q: 'What is the difference between `<strong>` and `<b>`?', a: '`<b>` simply makes text bold visually. `<strong>` makes text bold visually AND implies strong importance/urgency, which screen readers emphasize physically.' },

  // HTML/A11Y INTERMEDIATE & ADVANCED (10)
  { level: 'intermediate', q: 'What is ARIA and when should you use it?', a: 'Accessible Rich Internet Applications. It provides attributes (roles, states, properties) to modify semantics for screen readers. Rule of thumb: ONLY use ARIA when native HTML tags cannot achieve the desired behavior.' },
  { level: 'intermediate', q: 'What is the difference between `aria-hidden="true"` and `hidden`?', a: 'The HTML `hidden` attribute physically removes the element from the display (like `display: none`). `aria-hidden="true"` leaves the element visible on the screen but hides it completely from screen readers (useful for decorative icons).' },
  { level: 'advanced', q: 'Explain `tabindex`.', a: 'It controls keyboard navigation flow. `tabindex="0"` makes a non-focusable element focusable. `tabindex="-1"` removes an element from sequential keyboard navigation but keeps it focusable via JS. Values > 0 are extremely heavily discouraged as they disrupt expected flow.' },
  { level: 'advanced', q: 'What are Web Components?', a: 'A suite of technologies allowing creating reusable native custom elements. It consists of Custom Elements, Shadow DOM (styles/markup encapsulation), and HTML Templates (`<template>` and `<slot>`).' },
  { level: 'expert', q: 'What is the Shadow DOM?', a: 'A web standard that allows attaching a hidden, encapsulated DOM tree to an element. Styles defined inside a Shadow DOM do not leak out, and global styles do not leak in. Crucial for widget isolation.' },
  { level: 'intermediate', q: 'What does `rel="noopener noreferrer"` do?', a: 'When opening a link via `target="_blank"`, it prevents the new page from accessing the `window.opener` object (which is a security vulnerability) and strips the Referer header for privacy.' },
  { level: 'advanced', q: 'How does the browser parse HTML?', a: 'It converts raw bytes into characters, then into tokens (tags), then into Nodes, and finally links Nodes into the DOM Tree. Blocking scripts freeze the parser until executed.' },
  { level: 'advanced', q: 'What is preload, prefetch, and preconnect?', a: 'Resource hints. `preconnect`: "I will soon connect to this domain, do DNS/TLS handshake early". `preload`: "I NEED this asset for the current page immediately". `prefetch`: "I MIGHT need this asset for the NEXT page, download it in idle time".' },
  { level: 'expert', q: 'What is `aria-live`?', a: 'Used to announce dynamic DOM changes (like toast notifications or form errors) to screen readers without shifting focus. Options are `"polite"` (wait until user is idle) or `"assertive"` (interrupt immediately).' },
  { level: 'expert', q: 'What is critical rendering path (CRP)?', a: 'The sequence of steps the browser takes to convert HTML, CSS, and JS into pixels on the screen. Optimizing CRP involves deferring non-critical scripts, inlining critical CSS, and minimizing layout thrashing.' },

  // CSS BEGINNER (10)
  { level: 'basic', q: 'What does CSS stand for?', a: 'Cascading Style Sheets.' },
  { level: 'basic', q: 'Explain the CSS Box Model.', a: 'Every element is a rectangular box composed of four areas: Content (the actual stuff), Padding (transparent space around content), Border (around the padding), and Margin (transparent space outside the border).' },
  { level: 'basic', q: 'What is the difference between `display: none` and `visibility: hidden`?', a: '`display: none` completely removes the element from the document layout (it takes up zero space). `visibility: hidden` hides the element visually, but it still takes up its precise spatial footprint.' },
  { level: 'basic', q: 'What does `box-sizing: border-box` do?', a: 'It radically fixes the box model. Instead of padding and borders adding to the element\'s configured width/height, they are subtracted from it. `width: 100px` with `10px padding` is strictly `100px` total width.' },
  { level: 'basic', q: 'What is a CSS selector?', a: 'The part of a CSS rule that targets which HTML elements to style (e.g., class `.btn`, ID `#header`, attribute `[disabled]`).' },
  { level: 'basic', q: 'What is the primary difference between CSS Grid and Flexbox?', a: 'Flexbox is designed for 1-dimensional layouts (working either in a row OR a column). CSS Grid is designed for 2-dimensional layouts (managing rows AND columns simultaneously).' },
  { level: 'basic', q: 'How does the `z-index` property work?', a: 'It controls the vertical stacking order of elements that overlap. Important: It ONLY works if the element has a `position` value other than `static` (like relative, absolute, fixed, or sticky).' },
  { level: 'basic', q: 'What are pseudo-classes vs pseudo-elements?', a: 'Pseudo-classes select an element\'s state (e.g., `:hover`, `:focus`). Pseudo-elements create/target specific virtual parts of an element that aren\'t in the DOM (e.g., `::before`, `::first-line`).' },
  { level: 'basic', q: 'What is `rem` vs `em`?', a: '`rem` is relative strictly to the root `<html>` font-size. `em` is relative to the font-size of the element\'s direct parent, which can cause chaotic compounding issues if nested deeply.' },
  { level: 'basic', q: 'What is the cascading aspect of CSS?', a: 'If two different rules apply to the same element, the cascade determines which one wins based on Source Order, Specificity, and Importance (`!important`).' },

  // CSS INTERMEDIATE/ADVANCED (20)
  { level: 'intermediate', q: 'How does CSS Specificity work?', a: 'It\'s calculated mathematically. Inline styles > IDs > Classes/Attributes/Pseudo-classes > Elements/Pseudo-elements. E.g., `#nav .btn` beats `div div .btn`.' },
  { level: 'intermediate', q: 'What is a Stacking Context?', a: 'A three-dimensional conceptualization of HTML elements. Certain properties (like `opacity < 1`, `transform`, `z-index`, `filter`) create a new local stacking context. Z-indexes inside a stacking context cannot physically break out and overlay elements in a higher parent stacking context.' },
  { level: 'intermediate', q: 'Explain absolute vs relative positioning.', a: '`relative`: The element remains in the normal document flow, but can be visually shifted using top/left. `absolute`: The element is removed completely from normal flow and positioned relative to its closest positioned ancestor (any parent with non-static position).' },
  { level: 'intermediate', q: 'What are CSS Custom Properties (Variables)?', a: 'Defined as `--primary: red;`, used as `color: var(--primary)`. Unlike SCSS variables (which compile out statically), CSS variables live entirely in the DOM natively. They cascade, and JS can alter them dynamically.' },
  { level: 'intermediate', q: 'What does `justify-content` do in Flexbox?', a: 'It precisely aligns flex items along the main axis (horizontal in `flex-direction: row`).' },
  { level: 'advanced', q: 'What does `align-items` vs `align-content` do in Flexbox/Grid?', a: '`align-items` aligns items along the cross axis (vertical usually). `align-content` only works when there are MULTIPLE lines of items (like `flex-wrap: wrap`), determining spacing between the lines themselves.' },
  { level: 'advanced', q: 'What is BEM?', a: 'Block, Element, Modifier. A highly scalable CSS naming convention. `.card` (Block), `.card__title` (Element), `.card--dark` (Modifier). It entirely prevents CSS scoping conflicts.' },
  { level: 'advanced', q: 'Explain the CSS `:has()` selector.', a: 'The long-awaited "parent selector". `a:has(> img)` strictly targets anchor tags that contain direct child images. It eliminates the need for massive amounts of JS DOM manipulation.' },
  { level: 'expert', q: 'What are CSS Container Queries?', a: 'Instead of media queries targeting the <i>viewport</i> width, container queries (`@container`) target the <i>parent element\'s</i> width. A card component can radically re-layout itself depending on whether it\'s embedded in a narrow sidebar or a wide grid, totally redefining component reusability.' },
  { level: 'advanced', q: 'What is `CSS Subgrid`?', a: 'Allows a nested grid container to inherit its parent\'s grid tracks instead of defining its own. Essential for perfectly aligning nested headers across adjacent sibling cards.' },
  { level: 'advanced', q: 'How does `calc()` work?', a: 'It allows mathematical expressions natively mixing different CSS units. `width: calc(100vw - 50px);`. Invaluable for dynamic geometry.' },
  { level: 'expert', q: 'What is Layout Thrashing?', a: 'When JS radically toggles between reading the DOM (e.g. `offsetHeight`) and writing to the DOM (altering styles) sequentially inside a loop. This forces the browser to synchronously recalculate layout multiple times, annihilating 60fps performance. Fix: Batch all reads, then batch all writes using `requestAnimationFrame`.' },
  { level: 'advanced', q: 'What is hardware acceleration in CSS?', a: 'Properties like `transform` and `opacity` are completely offloaded from the main CPU thread to the GPU. This means animations using these properties run flawlessly at 60fps without triggering layout paints.' },
  { level: 'advanced', q: 'What does `will-change` do?', a: 'A hint to the browser that an element is about to change (e.g., `will-change: transform`). The browser preemptively allocates GPU layers to it. Overuse consumes massive memory; apply right before animation starts, remove when done.' },
  { level: 'intermediate', q: 'How do you create a triangle natively in pure CSS?', a: 'By setting an element to `width: 0; height: 0;` and using thick borders on adjacent sides, with one side colored and the perpendicular sides transparent.' },
  { level: 'intermediate', q: 'What does `minmax()` do in CSS Grid?', a: 'Defines a track\'s minimum and maximum size constraint. Extensively used in auto-responsive grids: `grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));`.' },
  { level: 'advanced', q: 'What is the utility-first CSS approach?', a: 'The philosophy popularized by Tailwind CSS. Instead of writing semantic classes `.btn-primary` with custom CSS, you apply tiny, single-function utility classes directly in markup: `bg-blue-500 px-4 py-2 rounded`. It massively increases speed and eliminates dead CSS.' },
  { level: 'expert', q: 'What are CSS `scroll-timeline` animations?', a: 'A modern feature allowing CSS animations to be directly bonded to the user\'s scroll depth instead of time, enabling totally native parallax effects without JavaScript.' },
  { level: 'expert', q: 'Explain Margin Collapsing.', a: 'When the vertical margins of adjoining blocks touch, they combine ("collapse") into a single margin whose size is the largest of the two. This does NOT happen with horizontal margins, or between flex/grid items, or if padding/borders stand between them.' },
  { level: 'advanced', q: 'What is `content-visibility: auto`?', a: 'A monumental performance property. It completely halts the rendering and layout calculations of long, off-screen content sections until the user scrolls near them, dramatically slashing initial page load work.' },

  // EXPERT CSS / CSS 2025+ (30)
  { level: 'expert', q: 'What is CSS Anchor Positioning?', a: 'A new API allowing an element to be positioned relative to one or more "anchor" elements on the page, without requiring a shared parent stacking context. It replaces massive amounts of JS positioning libraries (like Floating UI) for tooltips, menus, and popovers.' },
  { level: 'expert', q: 'Explain "Relative Color Syntax" in CSS.', a: 'A feature allowing you to derive a new color from an existing one using the `from` keyword: `color: rgb(from var(--bg) r g b / 0.5)`. This enables dynamic transparency and lighting adjustments without needing Preprocessors or dozens of variables.' },
  { level: 'expert', q: 'What are CSS "Cascade Layers" (@layer)?', a: 'A system to explicitly control the order of the cascade, regardless of specificity. Layers like `base`, `utils`, and `components` can be prioritized so that even high-specificity library styles can be overridden by low-specificity custom utilities.' },
  { level: 'expert', q: 'How does the "View Transitions API" essentially work?', a: 'It allows creating smooth transitions between different DOM states (or even different pages). When `document.startViewTransition()` is called, the browser captures snapshots of the "old" and "new" states and automatically animates between them using CSS pseudo-elements.' },
  { level: 'expert', q: 'What are "Style Queries" in Container Queries?', a: 'Unlike size queries that check width/height, style queries (`@container style(--theme: dark)`) allow styling an element based on the computed value of a custom property on its container.' },
  { level: 'expert', q: 'Explain CSS "Nesting" (Native).', a: 'Native browser support for nesting selectors like `& {}`, similar to Sass. It reduces code repetition and improves readability, and unlike preprocessors, it lives entirely in the browser, reducing build step complexity.' },
  { level: 'expert', q: 'What is the "Popover API"?', a: 'A native HTML attribute (`popover`) and JS API that provides consistent, accessible behavior for top-layer elements like menus or dialogs. It handles auto-dismiss, backdrop, and stacking order automatically.' },
  { level: 'expert', q: 'How does `object-view-box` work?', a: 'Similar to SVG\'s viewBox, it allows you to "crop" a native image element (`<img>` or `<video>`) directly in CSS, displaying only a specific coordinate region without needing a wrapper with `overflow: hidden`.' },
  { level: 'expert', q: 'What is "Wide Gamut" color in CSS (oklch, lab)?', a: 'Modern color spaces that can represent 50% more colors than standard sRGB. `oklch` is particularly powerful as it organizes color by perceived Lightness, Chroma, and Hue, making accessibility calculations much more intuitive.' },
  { level: 'expert', q: 'How do "Scroll-driven Animations" work natively?', a: 'Using `scroll-timeline` or `view-timeline`, CSS animations can be linked to the progress of a scroll container or the visibility of an element within a viewport, enabling complex parallax effects without a single line of JS.' },
  { level: 'expert', q: 'Explain the "Light-Dark" color-scheme function.', a: '`color: light-dark(#fff, #000)`. It automatically picks the first color for light mode and the second for dark mode based on the user\'s system preference or the `color-scheme` property, eliminating the need for complex `@media` blocks.' },
  { level: 'expert', q: 'What is its "Scope" in CSS (@scope)?', a: 'A rule that allows scoping styles to a specific subtree of the DOM, optionally with "donut holes" (limits). It provides true native encapsulation for components without requiring Shadow DOM.' },
  { level: 'expert', q: 'How does `field-sizing: content` work?', a: 'A new property for form elements (`<textarea>`, `<input>`) that causes them to automatically resize their height or width based on the content typed by the user, replacing hacks involving hidden mirror divs.' },
  { level: 'expert', q: 'What is the "Interpolation-size" property?', a: 'Allows animating an element from a fixed size (like `height: 100px`) to an intrinsic size (like `height: auto`), which was previously impossible without JavaScript or grid-template-rows hacks.' },
  { level: 'expert', q: 'Explain "Shadow DOM Piercing" selectors.', a: 'While `:host` and `::slotted` allow some interaction, some browsers support `::part()` which lets a parent document style specific, exported parts of a shadow tree. The older `/deep/` and `::shadow` selectors are deprecated.' },
  { level: 'expert', q: 'What is "CSS Masonry" (Proposed)?', a: 'A native extension to CSS Grid (`grid-template-rows: masonry`) that automatically packs items into the "shortest" column, creating Pinterest-style layouts natively without JavaScript.' },
  { level: 'expert', q: 'How does `accent-color` improve accessibility?', a: 'It allows changing the primary brand color for native UI controls (checkboxes, radios, progress bars) with a single line, and the browser automatically ensures the checkmark or foreground color has sufficient contrast against the background.' },
  { level: 'expert', q: 'What is "Margin Trim"?', a: '`margin-trim: block` or `inline`. It automatically removes the margin from the last child element of a container that would otherwise cause unwanted overflow or spacing gaps at the end of the box.' },
  { level: 'expert', q: 'Explain the "Individual Transform Properties".', a: 'Instead of one `transform: scale(2) rotate(45deg)`, browsers now support individual `scale`, `rotate`, and `translate` properties. This allows them to be transitioned and animated independently without overriding each other.' },
  { level: 'expert', q: 'What is `image-set()` in modern CSS?', a: 'An upgraded version of `srcset` for CSS backgrounds. It allows providing multiple image formats (like avif, webp, png) and resolutions, and the browser automatically picks the best one based on support and DPI.' },
  { level: 'expert', q: 'Explain "Text Wrap: Balance" vs "Text Wrap: Pretty".', a: '`balance` targets short blocks (like headings) to ensure lines have roughly equal width. `pretty` targets longer paragraphs, specifically trying to prevent "orphans" (a single word on the last line).' },
  { level: 'expert', q: 'What is the "Dynamic Viewport Unit" (dvH, dvW)?', a: 'Unlike `vh` (which often ignores mobile address bars), `dvh` always represents the *actual* visible height of the viewport as UI elements like the keyboard or URL bar hide and show.' },
  { level: 'expert', q: 'How does `inert` attribute work?', a: 'A global HTML attribute that tells the browser to completely ignore an element and its subtree for both user interaction (clicks) and accessibility (screen readers/tabs). Ideal for closed drawers or modaly background.' },
  { level: 'expert', q: 'Explain "Native Dialog" (`<dialog>`) advantages.', a: 'Native focus trapping, auto-dismiss on `Esc`, a high-contrast `::backdrop` pseudo-element, and it always renders in the "Top Layer", meaning it transcends any `overflow: hidden` or `z-index` limits of its parents.' },
  { level: 'expert', q: 'What is "CSS Motion Path" (`offset-path`)?', a: 'Allows an element to follow a custom path (like an SVG path) during animation. You can control its progress along the path using `offset-distance` and its orientation using `offset-rotate`.' },
  { level: 'expert', q: 'What is the "Appearance" property used for?', a: '`appearance: none` removes the default native styling of UI controls (like the dropdown arrow on a `<select>`), allowing you to build or restyle them from scratch without browser-specific hacks.' },
  { level: 'expert', q: 'Explain the "Linear-Easing" function.', a: '`easing: linear(0, 0.1, 0.5, 0.9, 1)`. It allows creating custom, complex easing curves (like a spring or "bounce" effect) by defining individual progress points, which was previously only possible with JS.' },
  { level: 'expert', q: 'What is "Mix-blend-mode" in depth?', a: 'Defines how an element\'s content should blend with its direct background. Modes like `multiply`, `screen`, and `overlay` allow for complex Photoshop-like visual effects directly in the browser.' },
  { level: 'expert', q: 'How do "Search Queries" (`@media (hover: hover)`) help UX?', a: 'They allow detecting the user\'s primary input type. `hover: hover` targets devices with a precise pointer (mouse), whereas `pointer: coarse` targets touch devices, allowing you to increase hit area sizes for mobile.' },
  { level: 'expert', q: 'What is `forced-colors` media query?', a: 'Crucial for accessibility. It detects if the user has enabled high-contrast mode at the OS level (like Windows High Contrast). Styles inside this block should focus on clarity and use system colors.' },

  // EXPERT PERFORMANCE & A11Y (60)
  { level: 'expert', q: 'What is "Interaction to Next Paint" (INP)?', a: 'A new Core Web Vital that replaced FID (First Input Delay). It measures the latency of ALL user interactions (clicks, taps, keyboard) during the page life cycle, identifying the longest event processing and rendering delay to ensure responsiveness.' },
  { level: 'expert', q: 'How do you optimize for "Largest Contentful Paint" (LCP)?', a: 'By prioritizing the "LCP element" (usually a hero image or large text block). Techniques include using `fetchpriority="high"`, preloading the image, using modern formats (avif), and ensuring the server responds quickly (low TTFB).' },
  { level: 'expert', q: 'Explain "Cumulative Layout Shift" (CLS) in depth.', a: 'It measures the total sum of all unexpected layout shifts that occur during the entire lifespan of the page. Common fixes: reserve space for images/ads using aspect-ratio, use `font-display: optional` to avoid FOIT/FOUT, and never insert content above existing content except in response to a user action.' },
  { level: 'expert', q: 'What is "Priority Hints" (`fetchpriority`)?', a: 'An attribute that tells the browser the relative importance of a resource. `fetchpriority="high"` on a hero image ensures the browser fetches it before other images, regardless of where it appears in the HTML.' },
  { level: 'expert', q: 'How does `decoding="async"` work on images?', a: 'It tells the browser that the image decoding process (turning raw data into pixels) can happen off the main thread, preventing the browser from freezing while trying to paint large images.' },
  { level: 'expert', q: 'What is "Speculation Rules API"?', a: 'A modern way to programmatically tell the browser to pre-render or pre-fetch specific URLs in the background when it\'s highly likely the user will click them, leading to near-instantaneous page loads.' },
  { level: 'expert', q: 'Explain the "Back/forward Cache" (bfcache).', a: 'A browser optimization that stores a complete snapshot of a page (including the JS state) in memory when the user navigates away. When they go back, the page is restored instantly from this cache without re-executing scripts or refetching resources.' },
  { level: 'expert', q: 'How do you prevent "Layout Thrashing" in modern apps?', a: 'By batching DOM reads and writes. Use `requestAnimationFrame` for visual updates and libraries like `fastdom` to orchestrate DOM access. Modern frameworks (React, Vue) handle this automatically via their Virtual DOM / Reactive systems.' },
  { level: 'expert', q: 'What is "Resource Timing API"?', a: 'A JS API (`performance.getEntriesByType("resource")`) that provides detailed network timing data for every asset loaded. It allows developers to monitor real-world performance of images, scripts, and XHR/fetch calls.' },
  { level: 'expert', q: 'Explain "Long Animation Frame API" (LoAF).', a: 'A new API that provides much deeper insights into "Long Tasks" than the standard Long Task API. It can pinpoint exactly WHICH script or event handler caused a frame to exceed 50ms, including attribution to the specific source.' },
  { level: 'expert', q: 'How does "Content-Visibility" affect A11y?', a: '`content-visibility: auto` hides elements from the rendering tree but NOT necessarily the accessibility tree if used carelessly. Ensure that hidden content doesn\'t disrupt the screen reader experience or focus management.' },
  { level: 'expert', q: 'What is "ARIA-live" assertive vs polite?', a: '`polite`: Screen reader finishes its current sentence before announcing the change. `assertive`: Interrupts the user immediately to announce the change. Use assertive sparingly for critical, time-sensitive errors.' },
  { level: 'expert', q: 'Explain "Focus Trapping" logic.', a: 'Crucial for modals. It ensures that when a modal is open, the `Tab` and `Shift+Tab` keys cycle focus ONLY between elements INSIDE the modal, preventing the user from accidentally navigating to the background page.' },
  { level: 'expert', q: 'What is the "Reduced Motion" media query?', a: 'Detects if the user has requested "Reduced Motion" at the OS level. Use it to disable or simplify non-essential animations: `@media (prefers-reduced-motion: reduce) { ... }`.' },
  { level: 'expert', q: 'How does `aria-description` differ from `aria-label`?', a: '`aria-label` provides a primary label (name) for an element. `aria-description` provides supplemental information, similar to a tooltip, which is announced after the label.' },
  { level: 'expert', q: 'What is "Focus-visible" pseudo-class?', a: 'It allows styling the focus ring ONLY when the user is navigating via keyboard, but hiding it when they are using a mouse. This satisfies accessibility requirements without annoying mouse users.' },
  { level: 'expert', q: 'Explain "Accessible Name Computation".', a: 'The complex algorithm browsers use to determine what a screen reader should call an element. It checks `aria-labelledby`, then `aria-label`, then native labels, then `alt` / `title`, and finally the inner text.' },
  { level: 'expert', q: 'What is "Color Contrast" (WCAG 2.1) requirements?', a: 'AA requires a 4.5:1 ratio for normal text and 3:1 for large text. AAA requires 7:1. Modern tools like the Chrome DevTools "CSS Overview" can audit these automatically across a site.' },
  { level: 'expert', q: 'How do you handle "Hidden Content" for A11y?', a: '`display: none` hides from everyone. `.visually-hidden` (CSS hack) hides from sighted users but keeps it for screen readers. `aria-hidden="true"` hides from screen readers but keeps it for sighted users.' },
  { level: 'expert', q: 'What are "Landmark Roles"?', a: 'ARIA roles like `role="main"`, `role="navigation"`, `role="contentinfo"`. While native elements like `<main>` and `<nav>` provide these by default, landmarks help screen reader users jump to specific sections of a page quickly.' },
  { level: 'expert', q: 'Explain "SVGRIA" (Accessible SVGs).', a: 'To make an SVG accessible, add a `<title>` and `<desc>` tag inside it, and reference them with `role="img"` and `aria-labelledby` on the root `<svg>` tag.' },
  { level: 'expert', q: 'What is "User-Centric Performance Metrics"?', a: 'Moving beyond "Load Time" to metrics that reflect user experience: "Is it happening?" (FCP), "Is it useful?" (LCP), "Is it usable?" (TBT/INP), "Is it delightful?" (CLS).' },
  { level: 'expert', q: 'How does "Tree Shaking" impact CSS performance?', a: 'Using tools like PurgeCSS or Tailwinds JIT to scan your HTML/JS and remove any CSS selectors that aren\'t being used, resulting in significantly smaller CSS bundles and faster download/parsing.' },
  { level: 'expert', q: 'What is "Preload" priority hijacking?', a: 'Overusing `<link rel="preload">` can accidentally delay critical resources because the browser prioritizes all preloads over other initial fetches. Only preload high-importance assets like fonts or the LCP image.' },
  { level: 'expert', q: 'How do "Service Workers" improve perceived performance?', a: 'By intercepting network requests and serving cached responses instantly. "Stale-while-revalidate" strategies allow the user to see cached content immediately while the worker fetches a fresh version in the background.' },
  { level: 'expert', q: 'What is "Gzip" vs "Brotli" compression?', a: 'Brotli is a newer compression algorithm designed specifically for the web. It typically achieves 15-20% better compression than Gzip for HTML/CSS/JS files, leading to faster transfer times.' },
  { level: 'expert', q: 'Explain "Lazy Loading" on images vs iframes.', a: 'Native `loading="lazy"` on both elements defers loading until the user is close to the viewport. For iframes, this is particularly powerful as it prevents loading heavy 3rd-party assets (like YouTube players) until needed.' },
  { level: 'expert', q: 'What is "Critical CSS" inlining?', a: 'The practice of extracting the CSS required ONLY for the above-the-fold content and inlining it directly into the `<head>`. This eliminates the blocking network request for the main CSS file for initial paint.' },
  { level: 'expert', q: 'Explain "AVIF" image format benefits.', a: 'AVIF offers significantly better compression than WebP and JPEG. It supports 12-bit color, HDR, and transparency, and can often be 50% smaller than a similar quality JPEG.' },
  { level: 'expert', q: 'What is "Font Subsetting"?', a: 'Creating a custom font file that contains only the specific characters used on your site. This can reduce a 500KB font file to 20KB, greatly improving page load and reducing FOIT.' },
  { level: 'expert', q: 'How do "HTTP/3" and QUIC improve performance?', a: 'By moving from TCP to UDP-based QUIC, HTTP/3 eliminates "Head-of-Line Blocking". If a single packet is lost, it doesn\'t freeze all other streams, leading to faster performance on unreliable mobile networks.' },
  { level: 'expert', q: 'What is "Early Hints" (HTTP 103)?', a: 'An HTTP status code that allows a server to send a partial response to the browser (containing `<link rel="preload">` headers) while it is still generating the main HTML, allowing the browser to start fetching assets even earlier.' },
  { level: 'expert', q: 'How does "CSS Containment" (`contain`) help?', a: 'It allows developers to isolate parts of a page from the rest of the layout, paint, or size calculations. The browser can then skip re-calculating the entire page if something changes inside a "contained" element.' },
  { level: 'expert', q: 'What is "Z-index isolation" with `isolation: isolate`?', a: 'It creates a new stacking context on an element without needing `position: relative` and `z-index`. This ensures that children elements don\'t accidentally interact with the z-indexes of elements outside their container.' },
  { level: 'expert', q: 'Explain "Variable Fonts" performance impact.', a: 'A single font file that contains multiple variations (weights, widths, slants). While the file is larger than a single standard font, it is significantly smaller than loading 6-8 separate files for different weights.' },
  { level: 'expert', q: 'How do "Subresource Integrity" (SRI) hashes work?', a: 'A security feature that allows browsers to verify that a resource fetched from a CDN (or any 3rd party) hasn\'t been tampered with. The browser matches the hash in the `integrity` attribute against the actual file content.' },
  { level: 'expert', q: 'What is "Mixed Content" security?', a: 'When an HTTPS site tries to load assets via HTTP. Browsers block "Active" mixed content (scripts/iframes) entirely and often warn about or upgrade "Passive" mixed content (images/video).' },
  { level: 'expert', q: 'Explain "Referrer Policy" in CSS and HTML.', a: 'Controls how much referrer information (the URL the user came from) is sent with requests. `strict-origin-when-cross-origin` is the modern secure default.' },
  { level: 'expert', q: 'What is "Feature Policy" (Permissions Policy)?', a: 'An HTTP header or iframe attribute that allows the site owner to disable specific browser features (like camera, microphone, or geolocation) for their own page or embedded iframes.' },
  { level: 'expert', q: 'How does "Content Security Policy" (CSP) prevent XSS?', a: 'By explicitly defining which origins and script types are allowed to execute. A strict CSP can block all inline scripts and 3rd-party domains by default, making XSS attacks almost impossible.' },
  { level: 'expert', q: 'What is "Prefers-Color-Scheme" in depth?', a: 'A media query that lets you respond to the user\'s OS-level light/dark mode preference. Best practice: use CSS variables to define theme colors and toggle them inside this media query.' },
  { level: 'expert', q: 'Explain "Preload" for Dynamic Imports.', a: 'Using `<link rel="modulepreload">` for modules that will be dynamically imported via `import()` later. This ensures the module is already in the cache when the user triggers the interaction requiring it.' },
  { level: 'expert', q: 'What is "Aspect-Ratio" property usage?', a: 'Allows you to reserve space for an element (like an image or video) before it even loads, preventing layout shifts (CLS). `aspect-ratio: 16 / 9; width: 100%;`.' },
  { level: 'expert', q: 'Explain "CSS Shapes" (`shape-outside`).', a: 'Allows text to wrap around complex shapes (circles, polygons) defined for a floated element, going beyond simple rectangular wrap behavior.' },
  { level: 'expert', q: 'What is the "Columns" property in CSS Layout?', a: 'A native multi-column layout system (like a newspaper) that automatically flows content across columns. `column-count: 3; column-gap: 20px;`.' },
  { level: 'expert', q: 'How does `pointer-events: none` work?', a: 'It makes an element "invisible" to mouse and touch interactions. Clicks "pass through" it to whatever is behind. Useful for decorative overlays or icons inside buttons.' },
  { level: 'expert', q: 'What is "Scroll-Behavior: Smooth"?', a: 'A pure CSS way to enable smooth scrolling when a user clicks an anchor link (`#hash`). `html { scroll-behavior: smooth; }`.' },
  { level: 'expert', q: 'Explain "Scroll-Margin" for anchor links.', a: 'Native property that adds an offset above a target element when it is jumped to via an anchor link. Crucial for fixed headers: `h2 { scroll-margin-top: 100px; }`.' },
  { level: 'expert', q: 'What is "Flex-Basis" vs "Width"?', a: '`flex-basis` defines the initial size of a flex item before free space is distributed. `width` is a more rigid constraint. In a flex container, `flex-basis` takes precedence over `width`.' },
  { level: 'expert', q: 'Explain "Atomic CSS" methodology.', a: 'Building UIs by composing small, single-purpose classes. While it results in more classes in HTML, it minimizes total CSS volume as classes are reused thousands of times across a project.' },
  { level: 'expert', q: 'What is "Container Units" (cqw, cqh)?', a: 'Similar to viewport units (`vw`/`vh`), but relative to the size of the nearest ancestor defined as a container. `10cqw` is 10% of the container\'s width.' },
  { level: 'expert', q: 'How to handle "Modal Dialogue" vs "Alert Dialogue" semantics?', a: '`role="dialog"` is for general interactive popups. `role="alertdialog"` is for critical information that requires a specific user choice (like a deletion confirmation).' },
  { level: 'expert', q: 'What is `aria-modal="true"`?', a: 'Tells the screen reader that the content below the dialog is not available while the dialog is open, helping them focus exclusively on the modal content.' },
  { level: 'expert', q: 'Explain "Aria-Describedby".', a: 'An attribute that points to the ID of another element that provides more context for a form input or button. Announced by screen readers after the element\'s name.' },
  { level: 'expert', q: 'What is "Shadow Parts" (`::part`) selector?', a: 'Allows a global CSS file to style a specific element inside a Shadow DOM, provided the custom element has exported that portion using the `part` attribute.' },
  { level: 'expert', q: 'How do "Web Components" handle accessibility?', a: 'Native accessibility features like `tabindex` and `aria-*` attributes work normally. However, developers must take extra care to ensure focus management and aria-references cross the Shadow DOM boundary correctly.' },
  { level: 'expert', q: 'What is "Scroll-Padding" in Flex/Grid?', a: 'Allows padding the scrollable area itself, which is very useful for ensuring that items in a horizontal scroll gallery aren\'t snapped flush to the edge of the container.' },
  { level: 'expert', q: 'Explain "Grid Areas" logic.', a: 'Naming specific regions of a grid: `grid-template-areas: "header header" "nav main"`. This allows for highly semantic and readable layout definitions that are easy to re-arrange in media queries.' },
  { level: 'expert', q: 'What is `scrollbar-gutter`?', a: 'A property that reserves space for the scrollbar, preventing layout shifts when a page goes from having no scrollbar to having one (or vice versa).' },
  { level: 'expert', q: 'How do "Search Engine Optimization" (SEO) and HTML relate?', a: 'Beyond semantic tags, SEO relies on correct heading hierarchy (H1-H6), structured metadata (OpenGraph/JSON-LD), and fast performance (Core Web Vitals), which is heavily tied to how HTML/CSS are delivered.' },
];
