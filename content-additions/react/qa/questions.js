export const reactQA = [
  // BEGINNER (25)
  { level: 'basic', q: 'What is React?', a: 'A declarative, efficient, and flexible JavaScript library for building user interfaces, primarily maintained by Meta (Facebook).' },
  { level: 'basic', q: 'What is JSX?', a: 'A syntax extension for JavaScript that looks similar to XML/HTML. It is transformed by transpilers (like Babel) into `React.createElement` calls.' },
  { level: 'basic', q: 'What is a component?', a: 'Independent and reusable bits of code. They serve the same purpose as JavaScript functions, but work in isolation and return HTML (JSX) via a render function.' },
  { level: 'basic', q: 'What are props?', a: 'Short for properties. They are read-only components that must be kept pure. They are used to pass data from one component to another.' },
  { level: 'basic', q: 'What is state in React?', a: 'An object that holds some information that may change over the lifetime of the component. When state changes, the component responds by re-rendering.' },
  { level: 'basic', q: 'What is the Virtual DOM?', a: 'A lightweight JavaScript representation of the DOM. React updates the Virtual DOM first, compares it to the previous version (diffing), and then applies the minimal necessary changes to the real DOM (reconciliation).' },
  { level: 'basic', q: 'What is reconciliation?', a: 'The process through which React updates the Browser DOM. When a component’s state changes, React calculates the difference between the new and old Virtual DOMs and only updates the changed nodes.' },
  { level: 'basic', q: 'What is the "key" prop?', a: 'A special string attribute you need to include when creating lists of elements. Keys help React identify which items have changed, been added, or been removed, optimizing the reconciliation process.' },
  { level: 'basic', q: 'What are React Hooks?', a: 'Functions that let you "hook into" React state and lifecycle features from function components. They do not work inside classes.' },
  { level: 'basic', q: 'What does useState do?', a: 'It declares a state variable that you can update directly. It returns a pair: the current state value and a function that lets you update it.' },
  { level: 'basic', q: 'What does useEffect do?', a: 'It lets you perform side effects in function components, such as data fetching, subscriptions, or manually changing the DOM.' },
  { level: 'basic', q: 'What is a functional component vs a class component?', a: 'Function components are simpler, writing UI as pure functions. Class components require extending `React.Component` and using `this`. Hooks made function components the modern standard.' },
  { level: 'basic', q: 'What is an event in React?', a: 'React events are named using camelCase, rather than lowercase. With JSX you pass a function as the event handler, rather than a string.' },
  { level: 'basic', q: 'What are synthetic events?', a: 'React\'s cross-browser wrapper around the browser\'s native event. They have the same interface as native events but work identically across all browsers.' },
  { level: 'basic', q: 'How do you conventionally name boolean props?', a: 'They are usually named like an assertion (e.g., `isOpen`, `hasLoaded`, `disabled`). Passing a boolean prop without a value defaults it to `true` (e.g., `<Button disabled />`).' },
  { level: 'basic', q: 'What is children in React?', a: 'A special prop (`props.children`) that allows you to pass components as data to other components, just like nesting standard HTML elements.' },
  { level: 'basic', q: 'What is a Fragment?', a: '`<React.Fragment>` or `<>` lets you group a list of children without adding extra nodes (like a `<div>`) to the DOM.' },
  { level: 'basic', q: 'How do you conditionally render an element?', a: 'Using JavaScript logical operators like `&&` (if true, render right side) or the ternary operator `condition ? true : false`.' },
  { level: 'basic', q: 'Why is standard `class` attribute written as `className` in React?', a: 'Because `class` is a reserved keyword in JavaScript. JSX is transformed into standard JS, requiring the use of the DOM API property `className`.' },
  { level: 'basic', q: 'What is a controlled component?', a: 'A form element whose value is controlled entirely by React state rather than the DOM.' },
  { level: 'basic', q: 'What is an uncontrolled component?', a: 'A component where the form data is handled by the DOM itself. You use a `ref` to access the value from the DOM directly when needed.' },
  { level: 'basic', q: 'What does preventDefault() do?', a: 'It prevents the default action that belongs to the event from occurring. Usually used in form submissions to stop the browser from reloading the page.' },
  { level: 'basic', q: 'Why shouldn\'t you mutate state directly in React?', a: 'Because React compares objects by reference. If you mutate state directly, the reference stays the same, and React doesn\'t know it needs to re-render. Always use the setter function.' },
  { level: 'basic', q: 'What is prop drilling?', a: 'The process of passing props from a higher-level component down to a lower-level component through many layers of intermediate components that don\'t actually need the data.' },
  { level: 'basic', q: 'What is React Context?', a: 'A way to pass data through the component tree without having to pass props down manually at every level. Useful for global data like themes or user authentication.' },

  // INTERMEDIATE (30)
  { level: 'intermediate', q: 'Explain the dependency array in useEffect.', a: 'If provided, the effect runs only when one of those dependencies has changed since the last render. If empty `[]`, it runs once on mount. If omitted entirely, it runs after EVERY render.' },
  { level: 'intermediate', q: 'What happens if you omit a changing variable from the useEffect dependency array?', a: 'The effect might use stale closures (old values of the variable from a previous render), leading to bugs that are very difficult to track down.' },
  { level: 'intermediate', q: 'What is the cleanup function in useEffect?', a: 'The function returned by the effect. It runs before the component unmounts AND before the effect runs again on a subsequent re-render. Crucial for removing event listeners or clearing timers.' },
  { level: 'intermediate', q: 'What is useCallback?', a: 'A hook that returns a memoized callback function. It\'s used to prevent functions from being recreated on every render, which is useful when passing callbacks to optimized child components.' },
  { level: 'intermediate', q: 'What is useMemo?', a: 'A hook that returns a memoized value. It\'s used to cache the results of expensive calculations between renders.' },
  { level: 'intermediate', q: 'When should you NOT use useMemo and useCallback?', a: 'Don\'t use them indiscriminately. Memoization has a performance cost (memory allocation and dependency checking). Only use them if the child is wrapped in `React.memo` or if the calculation is genuinely expensive.' },
  { level: 'intermediate', q: 'What is React.memo()?', a: 'A Higher Order Component (HOC) that memorizes the rendered output of a component, skipping re-renders if its props haven\'t changed (shallow comparison).' },
  { level: 'intermediate', q: 'What is useRef?', a: 'A hook that returns a mutable ref object whose `.current` property is initialized to the passed argument. It persists for the full lifetime of the component and mutating it does NOT cause a re-render.' },
  { level: 'intermediate', q: 'List modern ways to manage global state in React.', a: 'Zustand, Context API (for low-frequency updates), Redux Toolkit, Jotai, Recoil, and using Server State managers like React Query/TanStack Query.' },
  { level: 'intermediate', q: 'Why is using index as a key a bad idea?', a: 'If the list order changes (sorting, deleting, inserting), the index stays the same for that position in the DOM. React will get confused, potentially matching wrong state to the wrong UI elements or failing to animate.' },
  { level: 'intermediate', q: 'What is the difference between element and component in React?', a: 'An element is a plain object describing what you want to appear on the screen (`<div />` or `<MyComp />`). A component is a function or class that accepts input and returns a React element.' },
  { level: 'intermediate', q: 'What is the Custom Hook pattern?', a: 'Extracting stateful logic from a component into a reusable JavaScript function whose name starts with "use". They can call other hooks internally.' },
  { level: 'intermediate', q: 'What is StrictMode in React?', a: 'A tool for highlighting potential problems in an application. In development mode, it intentionally double-invokes components and lifecycle methods to help find side effects and deprecations.' },
  { level: 'intermediate', q: 'How do you lazy load a component in React?', a: 'Using `React.lazy()` combined with `<Suspense fallback={<Spinner />}>`. It allows you to dynamically import a component file only when it is actually needed to be rendered.' },
  { level: 'intermediate', q: 'What are higher-order components (HOCs)?', a: 'A function that takes a component and returns a new component, wrapping it with additional injected functionality. Mostly replaced in modern React by custom hooks.' },
  { level: 'intermediate', q: 'What is a render prop?', a: 'A technique for sharing code between components using a prop whose value is a function. The component uses that function to determine what to render. (e.g. `<Mouse render={mouse => <Cat mouse={mouse} />} />`)' },
  { level: 'intermediate', q: 'What is a Portal in React?', a: 'It provides a first-class way to render children into a DOM node that exists entirely outside the DOM hierarchy of the parent component (e.g., `createPortal(child, container)`). Perfect for Modals and Tooltips.' },
  { level: 'intermediate', q: 'What is the "forwardRef" API?', a: 'A way to let a component take a `ref` they receive and pass it further down ("forward" it) to a child DOM element. Required if a parent needs to measure or focus an inner button of a child component.' },
  { level: 'intermediate', q: 'What is useReducer?', a: 'An alternative to `useState`. It is usually preferable when you have complex state logic that involves multiple sub-values or when the next state depends heavily on the previous one.' },
  { level: 'intermediate', q: 'Why might a React update be batched?', a: 'React 18+ automatically batches multiple state updates within the same event loop (even inside promises or timeouts) into a single re-render for better performance.' },
  { level: 'intermediate', q: 'What is the difference between shallow rendering and full DOM rendering in testing?', a: 'Shallow rendering tests a component in isolation without rendering its children. Full DOM rendering mounts the full tree. Modern testing (RTL) strongly prefers full mounting.' },
  { level: 'intermediate', q: 'How does React Router work under the hood?', a: 'It listens to changes in the browser\'s History API. When the URL changes, it calculates which `<Route>` components match the path and renders them, blocking the browser from doing a full page refresh.' },
  { level: 'intermediate', q: 'What is the Context Consumer vs useContext?', a: `useContext is the modern Hook approach used inside a functional component. Context.Consumer is the older render-prop approach required in class components.` },
  { level: 'intermediate', q: 'How do you handle API calls in React components natively?', a: 'You traditionally use fetch/axios inside a `useEffect` with an empty dependency array to call the API on mount, updating state on resolution, while managing `isLoading` and `error` states manually.' },
  { level: 'intermediate', q: 'Why is TanStack Query (React Query) recommended over raw useEffect?', a: 'It automatically handles caching, background fetching, retries, deduplication, and simplifies loading/error state management significantly over manual useEffects.' },
  { level: 'intermediate', q: 'What is error boundary in React?', a: 'A classical component (using `componentDidCatch`) that catches JavaScript errors anywhere in its child component tree, logs those errors, and displays a fallback UI instead of crashing the whole app.' },
  { level: 'intermediate', q: 'What causes the "Warning: Cannot update a component while rendering a different component" error?', a: 'It happens when you try to update the state of a parent component directly synchronously inside the render body of a child component, usually by accidentally invoking a prop function instead of passing it by reference.' },
  { level: 'intermediate', q: 'How do you handle routing protection (Protected Routes)?', a: 'By creating a wrapper component that checks an authentication token. If valid, it returns the children. If invalid, it uses `<Navigate>` to redirect to the login page.' },
  { level: 'intermediate', q: 'What is code splitting?', a: 'Splitting your code into multiple smaller bundles instead of one massive file. React `lazy()` and dynamic imports load those bundles on demand, accelerating initial page load.' },
  { level: 'intermediate', q: 'What is hydration in React?', a: 'When React boots up on the client and attaches event listeners to the static HTML that was already sent by a server during Server-Side Rendering (SSR).' },

  // ADVANCED (25)
  { level: 'advanced', q: 'What is the React 19 Compiler?', a: 'An optimizing compiler that automatically memorizes your React code, effectively removing the need for manual `useMemo` and `useCallback` by analyzing component data dependencies at build time.' },
  { level: 'advanced', q: 'What are React Server Components (RSC)?', a: 'A new architecture where components render exclusively on the server at build time or request time. They have ZERO JavaScript sent to the client, cannot use hooks, but can directly access databases.' },
  { level: 'advanced', q: 'What is the difference between a Server Component and SSR?', a: 'SSR renders HTML on the server, but still sends the JS code to the client to hydrate into interactive components. RSC NEVER sends its JS to the client at all, acting purely as structured data.' },
  { level: 'advanced', q: 'What are Server Actions (React 19)?', a: 'A feature allowing you to define asynchronous functions that execute on the server. You can call these directly from client-side `<form action={myServerAction}>` or event handlers without configuring API routes.' },
  { level: 'advanced', q: 'What is useOptimistic (React 19)?', a: 'A hook that lets you eagerly update the UI assuming a server action will succeed, creating instantaneous feedback, while automatically rolling back if the action fails.' },
  { level: 'advanced', q: 'What does the use() hook do (React 19)?', a: 'A hook that lets you read the value of a Promise or Context. Unlike other hooks, `use()` can be called conditionally (inside an if-statement) and integrates deeply with Suspense.' },
  { level: 'advanced', q: 'What is useFormStatus (React 19)?', a: 'A hook representing the pending state of an enclosing `<form>`. It replaces the need to manually manage loading state variables when submitting forms via Actions.' },
  { level: 'advanced', q: 'What is useActionState (React 19)?', a: 'A hook that manages state returned from a form Action. It takes an action and an initial state, returning the current state, a new action to dispatch, and a pending boolean.' },
  { level: 'advanced', q: 'What is Suspense for Data Fetching?', a: 'Allows components to "pause" rendering while waiting for an asynchronous operation (like fetching data) to finish, letting a parent `<Suspense>` boundary render a fallback UI until the data is ready.' },
  { level: 'advanced', q: 'What is the useLayoutEffect hook?', a: 'It\'s identical to useEffect, but it fires synchronously *after* all DOM mutations but *before* the browser repaints the screen. Used for reading layout dimensions from the DOM to avoid visual flicker.' },
  { level: 'advanced', q: 'What is the useSyncExternalStore hook?', a: 'A specialized hook designed to let React safely subscribe to external data stores (like Redux or Zustand) in a way that is compatible with React\'s concurrent rendering features without tearing.' },
  { level: 'advanced', q: 'What is Concurrent React?', a: 'A set of architectural changes allowing React to interrupt a heavy rendering task to handle high-priority events (typing), then resume the background rendering. Uses `useTransition` and `useDeferredValue`.' },
  { level: 'advanced', q: 'What does useTransition do?', a: 'It lets you mark certain state updates as non-urgent (transitions). If an urgent update (typing) comes in, React interrupts the transition. The hook provides an `isPending` flag for UI feedback.' },
  { level: 'advanced', q: 'What does useDeferredValue do?', a: 'It lets you defer updating a part of the UI that is not critical. E.g., you immediately update a search input value, but you defer the value passed to an expensive search results list.' },
  { level: 'advanced', q: 'Explain tearing in UI frameworks.', a: 'Tearing happens when a UI framework yields rendering to the main thread, an external state store updates, and the framework resumes rendering with the NEW state, resulting in half the screen using old state and half using new state. `useSyncExternalStore` fixes this.' },
  { level: 'advanced', q: 'How does the Virtual DOM reconciliation algorithm actually work?', a: 'It assumes two rules: 1. Two elements of different types will produce different trees (React tears down the old tree unconditionally). 2. Developers can hint at which child elements may be stable using `key` props.' },
  { level: 'advanced', q: 'What is the danger of Context API causing redundant re-renders?', a: 'Any component consuming a Context re-renders if ANY value in that Context object changes. If you bundle high-frequency updates with static values, the entire tree re-renders unnecessarily. Solution: Split contexts or use Zustand.' },
  { level: 'advanced', q: 'Explain the Compound Component pattern implementation.', a: 'You create a parent `<Select>` component that holds internal state and provides it via Context. You then create child components like `<Select.Option>` that consume that context. You assign `Select.Option = OptionComponent` to bundle the imports.' },
  { level: 'advanced', q: 'How does React ensure event delegation?', a: 'Instead of attaching an event listener (like click) to every DOM node, React attaches a single event listener at the root of the document. When an event fires, React intercepts it, wraps it in a Synthetic Event, and dispatches it through its virtual tree.' },
  { level: 'advanced', q: 'Can you use Hooks inside class components?', a: 'No, hooks rely on the call stack tracking mechanism of function components. If a class component needs hook functionality, you must wrap it in a function-based HOC that calls the hook and passes the data as props.' },
  { level: 'advanced', q: 'What is a stale closure in React?', a: 'A situation where a function (like inside useEffect or a setTimeout) references variables from an older render cycle because the function was not updated. Fixed by correctly mapping the dependency array or using a ref.' },
  { level: 'advanced', q: 'How do you force a React functional component to re-render?', a: 'By convention: `const [, forceRender] = useState({});` then calling `forceRender({})`. However, needing to force a render is almost always indicative of bad architecture or direct DOM mutation.' },
  { level: 'advanced', q: 'What is React Native compared to React DOM?', a: 'React generates a Virtual DOM tree. `react-dom` is the renderer that translates that tree to HTML DOM nodes. `react-native` is the renderer that translates that exact same tree structure into Native iOS and Android UI views.' },
  { level: 'advanced', q: 'How do you optimize a huge list in React?', a: 'Instead of rendering 10,000 DOM nodes, you use "windowing" or "virtualization" (libraries like `react-window` or `react-virtuoso`). They only render the 20 nodes currently visible on the screen, swapping contents as the user scrolls.' },
  { level: 'advanced', q: 'What is a hydration mismatch?', a: 'In SSR, the server sends static HTML. On client load, React compares this HTML to what it expects to render. If they differ (e.g., the server printed "Time is 10:00", client prints "Time is 10:01"), React throws a hydration error and might replace the entire DOM tree.' },

  // --- NEW ADDITIONS (50+ ADDITIONAL ITEMS) ---
  // STYLING & UI
  { level: 'intermediate', q: 'What is Tailwind CSS and why is it popular in React?', a: 'A utility-first CSS framework. It\'s popular because it removes the need to name classes, keeps styles colocated with JSX, and results in tiny CSS bundles via JIT compilation.' },
  { level: 'advanced', q: 'Compare CSS Modules vs Styled Components.', a: 'CSS Modules provide scoped CSS using standard syntax with file-based isolation. Styled Components (CSS-in-JS) provide dynamic styling based on props but incur a small runtime performance cost.' },
  { level: 'intermediate', q: 'What are Design Tokens?', a: 'Visual atoms of a design system (colors, spacing, typography) stored as raw values (often JSON), used to ensure consistency across different platforms and themes.' },
  { level: 'advanced', q: 'What is the "Slot Pattern" in React?', a: 'A pattern where a component accepts other components as props (e.g., `<Card header={<Header />} body={<Body />} />`) rather than just using `children`, allowing for more rigid layout control.' },

  // FORMS
  { level: 'intermediate', q: 'Why use React Hook Form over manual state management?', a: 'It uses "uncontrolled" components via refs under the hood, preventing the entire form from re-rendering on every keystroke, leading to better performance in large forms.' },
  { level: 'advanced', q: 'What is Zod and how does it integrate with React forms?', a: 'A TypeScript-first schema declaration and validation library. It is used to define form schemas and automatically generate types and validation error messages for form libraries.' },
  { level: 'intermediate', q: 'How do you handle multi-step forms in React?', a: 'Typically by managing a `step` state and rendering different sub-components, or by using a library like `react-hook-form` with a shared parent state.' },

  // TESTING
  { level: 'intermediate', q: 'What is React Testing Library (RTL)?', a: 'A testing utility that encourages testing components as a user would (e.g., finding elements by text or labels) rather than testing implementation details like state.' },
  { level: 'advanced', q: 'What is MSW (Mock Service Worker)?', a: 'A library that intercepts API requests at the network level using Service Workers, allowing you to mock API responses in both tests and local development without changing your code.' },
  { level: 'expert', q: 'How do you test a component wrapped in multiple Context Providers?', a: 'By creating a custom `render` function that wraps the component under test in all necessary providers (Theme, Auth, QueryClient, etc.).' },
  { level: 'advanced', q: 'Explain the "Triple A" pattern in testing.', a: 'Arrange (setup state/mocks), Act (perform user action), Assert (check expected outcome).' },
  { level: 'expert', q: 'How do you test asynchronous state updates in RTL?', a: 'Using `findBy*` queries (which return promises and retry) or the `waitFor()` utility to wait for the UI to settle after an async action.' },
  { level: 'advanced', q: 'What is "Shallow Rendering" and why is it often discouraged?', a: 'Rendering only the top-level component without its children. It\'s discouraged because it doesn\'t reflect how the user interacts with the app and makes tests more brittle to refactoring.' },

  // ARCHITECTURE & PATTERNS
  { level: 'expert', q: 'What are "Server Actions" security considerations?', a: 'Since they become POST endpoints, you must manually perform authorization, authentication, and input validation (e.g., with Zod) inside the server action function.' },
  { level: 'expert', q: 'What is the "Module Pattern" for React state?', a: 'Isolating logic into standalone helper modules or custom hooks to keep components strictly focused on UI rendering (separation of concerns).' },
  { level: 'advanced', q: 'How do you prevent "prop drilling" without using Context?', a: 'Via component composition: pass the child component itself as a prop or `children` from the top level, so intermediate components don\'t need to know about the child\'s data.' },
  { level: 'expert', q: 'What is "Inversion of Control" in React components?', a: 'The idea that a component shouldn\'t decide HOW something is rendered, but rather provide a way for the consumer to provide the rendering logic (labels, icons, callbacks).' },
  { level: 'advanced', q: 'Explain the difference between useMemo and React.memo.', a: '`useMemo` caches a specific VALUE (like a filtered list). `React.memo` is an HOC that caches a rendered COMPONENT to prevent re-renders.' },

  // REACT 19 & LATEST
  { level: 'expert', q: 'How does the React Compiler (v19) handle "manual" optimizations?', a: 'It ignores them if it can optimize better, but it\'s designed to be compatible with existing `useMemo`/`useCallback` until they are eventually removed from the codebase.' },
  { level: 'advanced', q: 'What is the new `ref` prop in React 19?', a: 'In React 19, `ref` can be passed as a normal prop to function components, meaning `forwardRef` is no longer required for simple ref passthrough.' },
  { level: 'expert', q: 'What are "Async Transitions" in React 19?', a: 'The ability for `startTransition` to accept an `async` function. React will wait for the promise to resolve, managing the `isPending` state throughout the duration of the async operation.' },
  { level: 'advanced', q: 'What is the `<title>` and `<meta>` support in React 19?', a: 'React 19 natively supports rendering metadata tags anywhere in the component tree; it automatically hoists them to the `<head>` of the document.' },
  { level: 'expert', q: 'What is "Streaming SSR"?', a: 'Sending HTML chunks from the server to the browser as they are ready, instead of waiting for the entire page to render. Suspense boundaries are used to define which parts can be streamed later.' },

  // MISC / ECOSYSTEM
  { level: 'intermediate', q: 'What is Vite and why replace Webpack with it?', a: 'Vite uses native Browser ES modules for development, providing near-instant hot module replacement (HMR) regardless of project size.' },
  { level: 'advanced', q: 'What is "Micro-Frontend" in React?', a: 'Splitting an app into independent pieces that can be developed and deployed by different teams, often using Module Federation or iframes.' },
  { level: 'expert', q: 'How do you handle memory leaks in React?', a: 'By cleaning up listeners, intervals, and subscriptions in the `useEffect` cleanup function, and by avoiding setting state on unmounted components (though React 18+ handles this better).' },
  { level: 'advanced', q: 'What is "Debouncing" vs "Throttling" in React?', a: 'Debouncing waits until a user stops an action (typing). Throttling limits the action to once every X milliseconds (scrolling).' },
  { level: 'expert', q: 'How do you implement a generic component in TypeScript?', a: 'By using generic type parameters: `function List<T>({ items }: { items: T[] })`. This ensures type safety for the list items regardless of their specific type.' }
];
