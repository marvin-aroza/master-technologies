export const nextQA = [
  // BEGINNER (15)
  { level: 'basic', q: 'What is Next.js?', a: 'A React framework created by Vercel that provides building blocks to create fast, full-stack web applications, offering features like SSR, SSG, routing, and API endpoints out of the box.' },
  { level: 'basic', q: 'What is the main difference between React and Next.js?', a: 'React is a library for building UIs, usually resulting in Client-Side Rendered (CSR) Single Page Applications. Next.js is a framework built ON TOP of React that handles routing, server-side rendering, and tooling.' },
  { level: 'basic', q: 'What is SSR (Server-Side Rendering)?', a: 'Rendering HTML on the server for each distinct request. The client receives a fully populated HTML document, improving SEO and initial page load speed.' },
  { level: 'basic', q: 'What is SSG (Static Site Generation)?', a: 'Rendering HTML on the server ONCE at build time. The static HTML is then cached on a CDN and served instantly to all users.' },
  { level: 'basic', q: 'What is the App Router in Next.js 13+?', a: 'A new file-system based router built on top of React Server Components, replacing the old `pages` directory. It uses folders to define routes and `page.tsx` for UI.' },
  { level: 'basic', q: 'What is a Server Component vs a Client Component?', a: 'Server Components run purely on the server and never ship their JS to the browser. Client Components (marked with `"use client"`) run on both server and client (hydration), allowing event listeners (onClick) and state (useState).' },
  { level: 'basic', q: 'How do you create a Client Component?', a: 'Add the `"use client"` directive at the very top of the file, before any imports. Note: This marks the component AND all its imported children as client-side.' },
  { level: 'basic', q: 'What is the <Image> component?', a: 'An optimized replacement for `<img>`. It automatically prevents Cumulative Layout Shift (CLS), lazy loads images, and optimizes sizes and formats (like WebP/AVIF) dynamically.' },
  { level: 'basic', q: 'What is the <Link> component?', a: 'An extension of the HTML `<a>` tag that enables client-side navigation between Next.js routes. It pre-fetches linked pages in the background for instant transitions.' },
  { level: 'basic', q: 'What is `next/font`?', a: 'A module that automatically optimizes fonts. It downloads them at build time and hosts them alongside your static assets, eliminating external network requests (like to Google Fonts) and preventing layout shift.' },
  { level: 'basic', q: 'What does a `layout.tsx` file do?', a: 'It provides shared UI for all routes within its directory and subdirectories. Crucially, it does not re-render or lose state when navigating between its children.' },
  { level: 'basic', q: 'What is an API Route (Route Handler)?', a: 'A way to build a backend API directly within Next.js. In the App Router, this is done by creating a `route.ts` file that exports HTTP methods like `GET`, `POST`, `PUT`, etc.' },
  { level: 'basic', q: 'How do you handle 404 pages in the App Router?', a: 'By creating a `not-found.tsx` file in a directory. It will automatically be rendered if the URL doesn\'t match a route, or if you explicitly call the `notFound()` function.' },
  { level: 'basic', q: 'What is the `public` directory used for?', a: 'Serving static assets like images, fonts, or `robots.txt` that can be mapped directly to the base URL (`/image.png`).' },
  { level: 'basic', q: 'How do you define dynamic routes?', a: 'By wrapping a folder name in square brackets, e.g., `[id]`. Inside that folder\'s `page.tsx`, you can access the dynamic value via `props.params.id`.' },

  // INTERMEDIATE & ADVANCED (30)
  { level: 'intermediate', q: 'What is ISR (Incremental Static Regeneration)?', a: 'A feature that allows you to update static pages AFTER you have built your site. You can generate static pages in the background without needing to rebuild the entire application.' },
  { level: 'intermediate', q: 'How do you use ISR in the App Router?', a: 'By exporting a route segment config: `export const revalidate = 60;` (revalidate every 60 seconds), or using the `next: { revalidate: 60 }` option in a `fetch()` call.' },
  { level: 'intermediate', q: 'What is On-Demand Revalidation?', a: 'Manually purging the cache for a specific page or tag. Instead of waiting for a timer (ISR), you call `revalidatePath("/blog")` or `revalidateTag("posts")` when your database updates.' },
  { level: 'intermediate', q: 'What is the difference between Parallel Routes and Intercepting Routes?', a: 'Parallel Routes (`@folder`) let you render multiple pages in the same layout simultaneously. Intercepting Routes (`(.)folder`) let you hijack navigation to render a route within the current layout (like a modal) instead of doing a hard redirect.' },
  { level: 'intermediate', q: 'Where should you place data fetching in the App Router?', a: 'Ideally in Server Components at the highest level possible. Use `await fetch()` directly in the async component body, and pass data down to Client Components via props.' },
  { level: 'intermediate', q: 'How does Next.js handle data deduplication?', a: 'If you call `fetch("url")` multiple times across different components during the same server render pass, Next.js intercepts it and only makes ONE actual network request.' },
  { level: 'intermediate', q: 'Can you pass functions from a Server Component to a Client Component?', a: 'No, functions are not serializable. Server Components can only pass JSON-serializable data (strings, numbers, simple objects/arrays) to Client Components. The exception is Server Actions.' },
  { level: 'advanced', q: 'What is a Server Action?', a: 'An asynchronous function marked with `"use server"` that can be passed directly to Client Components (like a form action or button click). Next.js automatically creates a hidden API endpoint for it.' },
  { level: 'advanced', q: 'How do you handle loading states for fetching data?', a: 'Create a `loading.tsx` file next to your page. Next.js instantly renders this fallback UI while the page\'s async Server Component is awaiting data resolution.' },
  { level: 'advanced', q: 'What is Next.js Middleware?', a: 'Code that executes on the Edge before a request completes. It allows you to rewrite, redirect, add headers, or set cookies before the router even initializes.' },
  { level: 'advanced', q: 'What are the limitations of the Edge Runtime?', a: 'It does not support native Node.js APIs (like `fs`, `child_process`, or `crypto` via node) and requires APIs like databases to be accessible over HTTP/Edge-compatible drivers.' },
  { level: 'advanced', q: 'Explain the difference between Node.js runtime and Edge runtime in Next.js.', a: 'Node.js runtime gives you access to the full Node ecosystem but suffers from cold starts. Edge runtime runs on Vercel\'s CDN globally, offering zero cold starts and instant execution, but restricted API access.' },
  { level: 'advanced', q: 'How do you generate dynamic metadata for SEO?', a: 'Export an async `generateMetadata({ params })` function from your `page.tsx` or `layout.tsx`. You can fetch data there to return specific titles and Open Graph tags.' },
  { level: 'advanced', q: 'What happens if `generateMetadata` and default `page.tsx` both fetch the same data API?', a: 'Because Next.js automatically dedupes `fetch` requests, the database or API is only hit once, making it completely safe and performant to request the same data in both places.' },
  { level: 'advanced', q: 'How do you handle environment variables in Next.js?', a: 'Variables in `.env` are private by default. To expose a variable to the browser (Client Components), you must prefix it with `NEXT_PUBLIC_`.' },
  { level: 'advanced', q: 'What is Turbopack?', a: 'An incremental bundler optimized for Next.js, written in Rust. It serves as an ultra-fast replacement for Webpack during development (`next dev --turbo`).' },
  { level: 'advanced', q: 'What is the significance of the "standalone" output mode?', a: 'Configured in `next.config.js`. It creates a folder containing strictly the files and `node_modules` required to run production. Highly recommended for optimizing Docker deployments (cutting gigabytes down to megabytes).' },
  { level: 'advanced', q: 'How does Partial Prerendering (PPR) work in Next.js?', a: 'An experimental feature (v14/15) where the compiler wraps dynamic parts of your page (like a shopping cart) in Suspense. At build time, it static-generates the static shell, and at runtime, it streams in the dynamic hole.' },
  { level: 'expert', q: 'What are the 4 layers of caching in the Next.js App Router?', a: '1. Request Memoization (dedupes fetches during render). 2. Data Cache (persists fetch data across requests). 3. Full Route Cache (persists static HTML/RSC payloads on the server). 4. Router Cache (persists RSC payloads in the browser memory for instant backward/forward navigation).' },
  { level: 'expert', q: 'How do you force a route to be completely dynamic (skip all caching)?', a: 'Export `export const dynamic = "force-dynamic";` from the segment, or use dynamic functions like `cookies()`, `headers()`, or URL `searchParams`.' },
  { level: 'expert', q: 'How does Streaming work in Next.js App Router?', a: 'Instead of waiting for all data to load on the server before sending HTML, Next.js immediately sends the static HTML parts. As async Server Components finish fetching, Next.js streams the HTML chunks over the same HTTP connection.' },
  { level: 'advanced', q: 'How do you implement i18n (Internationalization) in the App Router?', a: 'Typically by using incredibly dynamic routes like `app/[lang]/page.tsx` and using Middleware to detect the user\'s accept-language header, automatically redirecting them to the correct `/[lang]` path.' },
  { level: 'advanced', q: 'Why shouldn\'t you use React Context if you can avoid it in App Router?', a: 'Because Context boundaries force all components "under" them to become Client Components (if they consume it). This defeats the purpose of Server Components. Pass props, use Server Components, or push Context as deep into the tree as possible.' },
  { level: 'expert', q: 'What is the "poisoning" rule in Server Components?', a: 'If you import a Client Component into a Server Component, that\'s fine. But if you import a Server Component INTO a Client Component directly, it becomes a Client Component. You must pass Server Components as `children` props to bypass this.' },
  { level: 'expert', q: 'What is the `server-only` package?', a: 'A utility package you can import. If a developer accidentally imports that file into a Client Component, it throws an immediate build error. Vital for files containing database passwords or secret keys.' }
];
